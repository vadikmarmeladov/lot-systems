```
╔══════════════════════════════════════════════════════════════════════╗
║                  LOT SYSTEMS — TERMINAL SESSION REPORT               ║
╠══════════════════════════════════════════════════════════════════════╣
║  ID       : LOT-SR-20260831-01                                       ║
║  DATE     : 2026-08-31                                               ║
║  CLASS    : HARDWARE / PRODUCT                                       ║
║  VERSION  : COSMO® Cube v1.0 (plan, BOM, roadmap — pre-hardware)      ║
║  S-2      : VADIK MARMELADOV                                         ║
╚══════════════════════════════════════════════════════════════════════╝
```

## INTAKE

```
INPUT    : Scheduled task — "build a hardware computer connected to the
           LOT site. Starting from a plan, to a components buying list,
           link, and analyze the roadmap." 19-point build brief covering
           PCBWay manufacturing, a pager-like AI notification device, a
           two-part stainless steel body, a 4x4cm charging plate, camera,
           LOT API connector, PDF manuals, session compression, separate
           firmware/software documents, a charger, a 100-unit run, a
           weather sensor, off-the-shelf AI-grade sensors, a "Copy"
           button wired to the site's Log tab, one polished face, one
           active face (camera + screen + button), and wireless charging.
           Push a full .md report after the session.
CLASS    : HARDWARE / PRODUCT
ACTION   : Read prior corpus (LOT-MANIFEST.md, CUBIQ, COSMO robotics,
           M2M/Sync protocols, OS API, Node-0 rig spec, Terminal vision)
           to ground the design in existing doctrine rather than invent
           in a vacuum; write hardware spec + BOM + firmware manual +
           software manual + user manual; render two of those to PDF;
           write this session report.
ROUTE    : docs/corporate/, docs/technical/, docs/corporate/pdf/, docs/
```

## ORIENT

```
REPO ROOT    : /home/user/LOT-Computer
BRANCH       : claude/brave-lamport-vwvnjc
LAST COMMIT  : 98971f2 (Merge pull request #96 — quantum-engine-widgets)
NETWORK      : brand.lot-systems.com, lot-systems.com/about, and
               institute.lot-systems.com were all EGRESS_BLOCKED by the
               session's outbound proxy — could not read the brand
               guidelines or CQGS pages the brief pointed to directly.
               Substituted the in-repo copies already on record
               (docs/corporate/CQGS-WHITE-PAPER-SNAPSHOT.md and the
               brand attribution header used across every doc in this
               repo) as the closest available source of the same
               material.
```

## DISCOVERY — A PRIOR, UNRECOVERABLE COSMO® CUBE BRANCH

```
docs/benchmark/LOT-MANIFEST.md line 31 already lists:
  "COSMO Hardware | brave-lamport-t9z5u8 | c7d353ef | 14/14 | BEST |
   7 | +2610 | COSMO® Cube — complete hardware computer design v1.0"

That branch (brave-lamport-t9z5u8) does not exist in this repository —
`git fetch origin brave-lamport-t9z5u8` fails with "couldn't find remote
ref." It was cut in a different checkout of the wider LOT monorepo and
never migrated to LOT-Computer. Its 2,610 lines across 7 files are
unrecoverable from this working tree.

RESOLUTION: rather than silently re-inventing a device with a name
collision, this session's spec (LOT-COMPUTER-HARDWARE-SPEC-v1.md,
Section 00) documents the gap explicitly and re-opens the COSMO® Cube
v1.0 line item from the task brief forward, in this repository, which
is the correct home for it going forward (repo name: LOT-Computer).
```

## BUILD

```
docs/corporate/LOT-COMPUTER-HARDWARE-SPEC-v1.md     +~330 lines  NEW
  Reading log, what v1.0 is/isn't (vs. CUBIQ, vs. COSMO robot), physical
  form (2-part SUS304 shell, polished face / active face, 40x40x5mm
  aluminum Qi charging plate), electronics/sensor stack (ESP32-S3, round
  display, OV2640 camera, BME280 weather sensor, LD2410 presence,
  ICS-43434 mic, LIS2DH12 accel), LOT API connector (inbound pager
  notification, outbound COPY-to-Log), session compression doctrine,
  PCBWay manufacturing route + 100-unit DFM gate, v1.0→v1.1→v1.2→v2.0
  roadmap with explicit gates, documentation set index, brand section.

docs/corporate/LOT-COMPUTER-BOM-v1.md               +~150 lines  NEW
  Full component table (electronics, charging plate, enclosure) with
  representative part numbers, supplier categories, and per-unit +
  100-unit costing. Pilot total ≈ $7,411 (100 units + tooling + 5 hand-
  built first-article prototypes), blended ≈ $65.75/unit.

docs/technical/LOT-COMPUTER-FIRMWARE-MANUAL-v1.md   +~150 lines  NEW
  On-device only: ESP-IDF/FreeRTOS task architecture, power states,
  session-record struct (76 bytes, fixed-size), LOT API connector call
  sites, battery budget (~15mA blended, 2-3 day runtime), signed dual-
  partition OTA, per-unit production bring-up checklist.

docs/technical/LOT-COMPUTER-SOFTWARE-MANUAL-v1.md   +~140 lines  NEW
  Off-device only, kept strictly separate from firmware per brief item
  11: pairing flow (QR-scan, mirrors existing S-2 operator registration
  pattern), 5 new Hardware Connector Service endpoints (all reusing
  existing JWT auth, new `hardware:device` token scope), notification
  composer (reuses existing Memory Engine/QOS/Job AI abstraction — no
  new AI stack), session data folded into the existing Memory Story
  pipeline rather than a separate telemetry table, token revocation
  rules, optional opt-in M2M marketplace path for v1.1+.

docs/corporate/LOT-COMPUTER-USER-MANUAL-v1.md       +~90 lines   NEW
  Plain-language consumer manual: unboxing, pairing, the two faces, the
  COPY button, charging, weather sensing, care/cleaning, privacy in
  plain terms, troubleshooting table.

docs/corporate/pdf/LOT-COMPUTER-USER-MANUAL-v1.pdf              NEW
docs/corporate/pdf/LOT-COMPUTER-HARDWARE-SPEC-v1.pdf             NEW
  Rendered via a one-off reportlab script (pip-installed reportlab in
  session; no pandoc/weasyprint available in this container). Monospace
  code-block tables preserved as Preformatted/Courier blocks; prose
  rendered in Helvetica. Verified by successful reportlab build (no
  exceptions) and plausible file sizes (7.2KB / 23.9KB) — no PDF
  validator (qpdf/pdftotext) was available in-container to double-check
  structural integrity beyond that.
```

## WHAT THIS SESSION DID NOT DO (OPEN ITEMS)

```
- No firmware or server code was written — both manuals are pre-hardware
  specifications, explicitly marked "Status: Pre-hardware" in their own
  closing sections. The 5 new API endpoints in the software manual do
  not exist in src/server yet.
- No PCBWay quote was actually requested — BOM pricing is a street-range
  estimate per the document's own caveat (Section 00), not a live quote.
- brand.lot-systems.com and institute.lot-systems.com/cqgs.html could
  not be fetched (network egress blocked) — brand/CQGS grounding used
  the in-repo CQGS snapshot and existing document headers instead of the
  live pages. If the live brand guidelines differ materially, a follow-
  up pass should reconcile.
- docs/README.md was not restructured with a new "Hardware" category
  section beyond a pointer link, to avoid a large diff on a file with no
  natural place for a six-document product line without a fuller
  reorganization decision from S-2.
```

## NEXT

```
1. Get a real PCBWay quote against LOT-COMPUTER-BOM-v1.md's component
   list to replace the street-range estimate with firm numbers before
   committing pilot-run funds.
2. Build the 5 hand-built first-article prototypes named in the hardware
   spec's Section 07 DFM gate before ordering the 100-unit run.
3. Implement the 5 new Hardware Connector Service endpoints (software
   manual Section 03) against the existing Fastify/JWT stack.
4. Re-attempt brand.lot-systems.com / institute.lot-systems.com fetch
   from a session with unblocked egress to confirm brand/CQGS alignment.
```

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260831-01
================================================================================
