# LOT SYSTEMS — ASSEMBLY DOCTRINE
rev A

Distilled, stable principles, written in current lexicon notation where a
token has earned its place (see LOT-LEXICON.md). Session reports stay
verbatim; this file is the compressing layer — cite a clause instead of
restating the prose once a finding is confirmed stable across sessions.

--------------------------------------------------------------------------------

[D-001 rev A]  On /story: bare command keeps the undated "recent" window for
               backward compatibility; an explicit period argument
               (day|week|month|year) scopes the query window, sample size,
               word budget, and prompt framing together as one unit — never
               change the window without changing the framing, or the model
               compresses a year of data with a one-day voice.
               Source: LOT-SR-20260803-01.
