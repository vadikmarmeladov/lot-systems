# LOT SYSTEMS — ASSEMBLY LEXICON

Evolving controlled vocabulary. A concept earns a token only after it has
appeared in 3 or more prior session reports OR has been folded into doctrine
twice (see `docs/assembly/LOT-LEDGER.md` for the session history a token
claim must be checked against). Tokens are never deleted; superseded tokens
are marked `DEPRECATED -> <new token>`.

Bootstrapped 2026-08-10 (LOT-SR-20260810-01). Opens empty: no corpus-wide
recurrence count has yet been computed across the existing session-report
history, so seeding starting tokens now would be an unearned vocabulary,
not a compressed one. Tokens are minted going forward as sessions confirm
real recurrence.

| TOKEN | MEANING | REV | SINCE |
|-------|---------|-----|-------|
