# LOT SYSTEMS — ASSEMBLY LEXICON
Evolving controlled vocabulary. A concept earns a token only after it has
appeared in 3+ prior session reports OR been folded into doctrine twice
(see docs/assembly's SELF-ASSEMBLY protocol, minting rule). Tokens are never
deleted; superseded tokens are marked DEPRECATED -> <new token> so old
reports remain legible.

Bootstrapped on LOT-SR-20260802-01 (first Benchmark run to touch this file).
No tokens are seeded pre-emptively — auditing which concepts already cross
the 3-report threshold across the full docs/assembly/ history is its own
pass and was out of scope for this session. Populate honestly, from usage,
as future sessions mint or reuse tokens below.

| TOKEN | MEANING | REV | SINCE |
|-------|---------|-----|-------|
