# LOT® AI — Documentation & Brand Review

**Date:** 2026-08-12
**Type:** Scheduled daily documentation/brand sync
**Operator:** Automated review (Claude Code session)

---

## 1. Access status

This session runs behind a network egress proxy that restricts outbound HTTP(S) to
an allowlist. All three requested external targets were **blocked**:

| Target | Result |
|---|---|
| `https://lot-systems.com/about` | `EGRESS_BLOCKED` — domain not on the proxy allowlist |
| `https://brand.lot-systems.com` | `EGRESS_BLOCKED` — domain not on the proxy allowlist |
| `https://institute.lot-systems.com` | `EGRESS_BLOCKED` — domain not on the proxy allowlist |

Direct WebFetch could not retrieve any of the three pages. As a fallback, web
search (which does not go through the same domain-restricted fetch path) was
used to gather what public information is indexed. That is a materially
weaker substitute — search snippets, not the actual pages — so items below
marked "unverified" should be confirmed manually.

**Action needed:** if this task is meant to run unattended every day, `lot-systems.com`
and its subdomains (`brand.`, `institute.`) need to be added to this environment's
egress allowlist, or the task needs to run somewhere with broader network access.
Until then, this daily review can only partially complete its brief.

## 2. What was found via search (unverified — pages themselves unreachable)

- LOT is described publicly as a subscription service (self-care, wardrobe,
  home/kids essentials) built around a **Memory Engine**: an AI companion that
  retains context across sessions and can switch AI providers mid-conversation
  without losing the user's "Memory Story" (stored in LOT's own database, not
  the provider's).
- Positioning line found: *"Your story. Your data. Your AI provider of choice."*
- `brand.lot-systems.com` currently resolves to a page titled **"Usership — LOT
  Systems"**, describing Founding Supporter tiers (dashboard access, magazine
  subscription, product subscriptions, community events, early AI features).
  No design-system/COSMO® style-guide content surfaced in search snippets —
  that page may live at a different path, or the styleguide content may be
  behind navigation the search index didn't crawl.
- `institute.lot-systems.com` exists (indexed as "LOT Institute") but no
  snippet content about "Robot Persons" or the requested
  Coffee → Widget → Subscription → Design System → Style → Community flow
  surfaced. **Could not confirm or deny anything about that flow or about
  LOT® Robot Persons™ capabilities this run** — needs a direct page fetch,
  which this session cannot make.

## 3. Repository state (this can be verified directly)

Latest commit on `master` as of this review: `98971f2` (2026-08-05, PR #96).
No commits since then to review as "new" — the repo has been quiet for the
week leading into today.

Current system snapshot per `docs/wiki/LOT-WIKI-v87.md` (Field Manual v113,
Day 1073+, COSMO® Year 3 / Day 765):

- QIE patterns: 151 (P1–P151)
- Physiological archetypes: 51
- Background jobs: 48
- Badge Codex: v31, 781 badges across 70+ categories, 8 rarity tiers
- Ecosystem automation nodes: 6 — `CAR · HOME · CPU · PHN · WCH · ROBOT`
  (the `ROBOT` node = home automation / ambient intelligence; this is the only
  in-repo reference resembling "Robot Persons" and it predates today — no new
  capability was added to it this run)
- Widgets: 43
- Engineering doctrines: 11 (most recent: Doctrine 11, Circadian Architecture)

No repo-side change since the last session report (`docs/SESSION_REPORT_2026_08_05_WIKI_v87.md`)
that would affect current integrations.

## 4. Flags for current projects/integrations

1. **Network access gap (the main finding today):** this scheduled task cannot
   reach `lot-systems.com` or its subdomains from this environment. If the
   intent is a genuine daily brand/doc sync, that's currently not happening —
   only a degraded, search-snippet-based approximation is possible. Worth
   fixing the allowlist before relying on future runs of this task.
2. Nothing in the reachable repo content indicates a breaking change to the
   Coffee → Widget → Subscription → Design System → Style → Community flow —
   but this could not be positively confirmed either, since the page(s)
   describing it were unreachable.
3. No new LOT® AI language/ecosystem features found this run beyond the
   existing Aug 5 snapshot.

---

*Generated by an automated documentation-review session. Items marked
"unverified" should be treated as leads, not confirmed facts.*
