================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260831-01
TITLE:    BASICS TAB — M1 OF 3, OPEN TAB LIVE (LOT-FM-001)
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  (unbenchmarked — see 04)
BRANCH:   claude/beautiful-johnson-3cv5fo
DATE:     2026-08-31
TIME:     01:04 UTC
RESULT:   UNVERIFIED — BUILD GATE COULD NOT RUN (see 04)
================================================================================

--------------------------------------------------------------------------------
00 // PREFLIGHT
--------------------------------------------------------------------------------
REPO:        OK        DOCS WRITABLE: OK
REMOTE:      origin    REACHABLE
NODE_MODULES: ABSENT — yarn/npm install both blocked by org egress policy
             (registry.yarnpkg.com CONNECT rejected 403; npm fallback to
             registry.npmjs.org hit ERESOLVE on an unrelated pre-existing
             nanostores peer conflict between package.json and package-lock.json).
             No install path available in this sandbox.

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        LOT-FM-001 / SELF-ASSEMBLY DIRECTIVE — BASIC (RATION) 90-DAY
                 BUILD, scheduled S-2 directive. Doctrine: issue-not-sell, civilian
                 ration subscription, USD 100/mo, ceiling USD 40 landed, ledger-
                 is-the-marketing, quartermaster voice. 3-month build envelope:
                 M1 ledger/doctrine (read-only), M2 upgrade/roster, M3 issue/
                 fulfillment.
CLASSIFICATION:  ENGINEERING — new feature (client UI, no server/DB change)
ACTION TAKEN:    Built M1 only — "the System exists, read-only." M2 (upgrade
                 control, roster intake, recurring billing) and M3 (load engine,
                 supplier quotes, printed manifest, fulfillment) are NOT started;
                 no billing, roster, or shipping infrastructure exists in this
                 repo, so both are new subsystems for future sessions, not wiring.
ROUTED TO:       src/client/ (feature), docs/benchmark/ (this report)

--------------------------------------------------------------------------------
02 // DISCOVERY
--------------------------------------------------------------------------------
LOT-MANIFEST.md already carried a "Basics Tab" row (branch beautiful-johnson-
56p7ov, BEST, +293 lines, "OPEN TAB live") — that branch no longer exists on
the remote (pruned) and master carries none of it: grep for OPEN TAB / BASICS M1
/ ration ledger across origin/master returned nothing outside the manifest/
doctrine bookkeeping files. Treated as a from-scratch build on this branch, not
a continuation. Layout.tsx already carried "Basics" as a disabled nav placeholder
(label with no route) for both logged-in and logged-out link sets — that is the
intended mount point.

--------------------------------------------------------------------------------
03 // M1 BUILD — WHAT SHIPPED
--------------------------------------------------------------------------------
COMPONENT:   src/client/components/Basics.tsx (new)
             Public, read-only, no network calls, no store subscriptions —
             pure static doctrine content. Renders:
               - Doctrine statement (4 lines, quartermaster/imperative)
               - Status-line component (BUILD / TAB STATUS / ENROLLMENT rows)
               - Price line: RATE USD 100.00/MONTH — ISSUED, NOT SOLD
               - 23-item ration ledger: NO / NOMENCLATURE / CADENCE columns,
                 fixed-width grid-cols-[4ch_1fr_18ch] for column alignment.
                 COGS withheld per doctrine (nomenclature + cadence only).
STYLE:       Scoped to this component only (no global Tailwind/theme change):
             font-mono font-bold (Tailwind's default mono stack lists
             "Liberation Mono" before Courier New — matches the house-style
             ask without a font asset or config change); uppercase + tight
             tracking; border-2/border-t-2/border-b-2 (Tailwind's default
             borderWidth scale already includes 2px — no new scale entry
             needed); zero rounded-* classes (square corners); bg-acc/text-bac
             inversion (same token pair Layout.tsx already uses for the active
             nav tab) on the two structural bands (title, price line) — full
             inversion for primary hierarchy, existing text-acc/NN opacity
             convention (used everywhere else in this app: StatusPage, Logs,
             About) for secondary/tertiary text, so Basics reads as consistent
             with the rest of the app rather than a foreign skin. No color, no
             icons, no marketing copy.
WIRING:      RouteName ('basics' added) + nav entry activated (route: 'basics'
             replacing the disabled placeholder) in Layout.tsx; Routes type +
             path map ('/basics') in router.ts; PersistentRoute + import +
             TabPanel in app.tsx TabPanels, following the exact Settings/Logs
             pattern (About.tsx and /status are a different, non-tab entry
             point — not the template used here).
NOT BUILT:   Upgrade control/state machine, roster intake, recurring billing,
             STAND DOWN downgrade, issue log, load engine, supplier quotes,
             printed manifest, fulfillment dispatch — all explicitly M2/M3.
             SubscribeWidget.tsx (existing $99 Usership / $15 R&D external
             link-out) was left untouched; it is the only existing "upgrade"
             precedent in the codebase and is the reference point for M2.

--------------------------------------------------------------------------------
04 // BUILD GATE — NOT RUN (environment limitation, disclosed rather than faked)
--------------------------------------------------------------------------------
node_modules is absent in this sandbox and both installers failed for reasons
unrelated to this change:
  yarn install --frozen-lockfile   -> registry.yarnpkg.com CONNECT 403
                                      (org egress policy denial, not retried
                                      per proxy operating rules)
  npm install (registry.npmjs.org) -> ERESOLVE: nanostores@0.9.5 (root) vs
                                      nanostores@^0.7.0 peer of
                                      @nanostores/react@0.4.1 — pre-existing
                                      package.json/lock mismatch, unrelated to
                                      Basics
`npm run client:build` / `server:build` could therefore not be executed. GATE
is NOT claimed GREEN. Mitigation performed instead: manual review against
every import/token used elsewhere in a working file (cn from #client/utils,
useDocumentTitle from #client/utils/hooks, bg-acc/text-bac pairing from
Layout.tsx's active-tab class, border-2 from Tailwind's default scale,
text-acc/NN opacity from StatusPage.tsx) — every class and import in
Basics.tsx has a working precedent elsewhere in this exact codebase. One real
bug was caught in review, not by a build: `first:border-t-0` on the ledger
rows targeted the wrong DOM child (the header row is the actual first-child,
not the first ration item), which would have doubled the border between
header and row 1 — fixed to an index (`i > 0`) check before commit.
ACTION FOR NEXT SESSION: run `npm run build` (or CI) on this branch before any
further work lands on top of it; do not treat this report's push as a green
benchmark.

--------------------------------------------------------------------------------
05 // FILES CHANGED (this session)
--------------------------------------------------------------------------------
PATH                                                     STATUS
src/client/components/Basics.tsx                         ADDED
src/client/components/ui/Layout.tsx                      MODIFIED
src/client/entries/app.tsx                                MODIFIED
src/client/stores/router.ts                              MODIFIED
docs/benchmark/LOT-SR-20260831-01.md                     ADDED

--------------------------------------------------------------------------------
06 // NEXT SESSION (M2 — UPGRADE & ROSTER)
--------------------------------------------------------------------------------
Per LOT-FM-001: UPGRADE control + state machine (USERSHIP/AI -> PENDING ->
ON STRENGTH -> STEADY STATE), roster intake (sizing/shipping/cadence start),
recurring $100/mo additive billing, STAND DOWN downgrade, issue log scaffold.
None of billing (Stripe/etc.), roster/shipping data model, or a state-machine
store exist yet anywhere in this repo for any feature — M2 is new
infrastructure, not a wire-up, and is the right size for its own session
rather than an extension of this one. UserTag enum (Usership/Pro/etc. in
src/shared/types/index.ts) and SubscribeWidget.tsx's external link-out are the
only existing scaffolding to build on.

--------------------------------------------------------------------------------
07 // PUSH
--------------------------------------------------------------------------------
COMMIT:      (this session's commit — see git log)
BRANCH:      claude/beautiful-johnson-3cv5fo
PUSH:        origin/claude/beautiful-johnson-3cv5fo

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260831-01
================================================================================
