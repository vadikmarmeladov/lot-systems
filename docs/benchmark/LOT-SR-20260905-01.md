================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260905-01
TITLE:    LOT® Email — /email log trigger, Sync Mail tab, Cohort→Community ship
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  98971f20
BRANCH:   claude/determined-turing-ab29i4
DATE:     2026-09-05
TIME:     04:45 UTC
RESULT:   GREEN
================================================================================

--------------------------------------------------------------------------------
00 // PREFLIGHT
--------------------------------------------------------------------------------
REPO:        OK        DOCS WRITABLE: OK
REMOTE:      origin    REACHABLE
LAST GREEN:  benchmark-20260805-01 (CORRECTION: step 00's first tag listing ran
             before a full `git fetch origin` and returned empty, which this
             report originally and wrongly read as "first run ever." Tags
             exist back to 20260601. Recording the error rather than quietly
             fixing it — a shallow/partial local clone can make preflight
             lie about repo history; always fetch before trusting an empty
             result.)

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        Scheduled task — design/build/deploy the simplest LOT Email
                 system: "/email to Hitomi" in Log, new messages appear in
                 Sync, integrated with Lot Chat and Cohort Dating via LOT
                 Community.
CLASSIFICATION:  ENGINEERING (SHIP — see 07)
ACTION TAKEN:    Read the live repo before writing anything new. LOT-MANIFEST.md
                 already listed "LOT Mail" as BEST at branch
                 determined-turing-f6bw7r (11/11 iterations, /email trigger +
                 Sync inbox + Cohort integration) with a 2026-06-27 note
                 claiming it and five sibling branches were "incorporated into
                 master in prior sessions." That note was wrong for this
                 branch: `git branch -r` showed determined-turing-f6bw7r and
                 relaxed-hamilton-eRBVA (its superseded predecessor) both
                 still on the remote, and a grep of master for /email,
                 lot_mails, EmailInbox, MailWidget returned nothing. The
                 feature was designed and iterated on eleven times but never
                 landed. Rather than re-derive the same design from scratch,
                 cherry-picked the BEST branch's two commits per Ship Mode.
ROUTED TO:       docs/benchmark/ (this report); code changes routed by file
                 type per existing tree (src/client, src/server, migrations/).

--------------------------------------------------------------------------------
02 // CHECK A        03 // BUILD        04 // CHECK B (gate)
--------------------------------------------------------------------------------
COMMAND                              A        B
-------                              --       --
npx tsc --noEmit tsconfig.server.json  PASS   PASS
npx tsc --noEmit tsconfig.json (client) 128 pre-existing errors (baseline) | 128 (same set, 0 new)
npm run server:build                 —        PASS
npm run client:build (esbuild+postcss) —       PASS (2 pre-existing duplicate-key warnings in badges.ts, unrelated)
BUILD:                                npm run build -> OK
GATE:                                 GREEN
FIX LOG:
  - stores.goTo('log') -> stores.goTo('logs') in ChatCatalystWidget.tsx —
    the cherry-picked branch used a router key that never existed (router
    path is /log but the Routes key is 'logs'); would have been a silent
    no-op navigation for the Cohort "Mail" button.
  - sync.listen('lot_mail', (data) => ...) in Sync.tsx typed explicitly as
    LotMailSyncPayload instead of inheriting implicit-any (the SyncEvents
    generic on EventEmitter is itself broken pre-existingly — TS2558 in
    src/client/sync.ts — so every other sync.listen callback in the file is
    already implicit-any; this one alone is now typed correctly).
  - Added a Log.create('lot_mail_sent') call to POST /mail, and added
    'lot_mail_sent' to the /logs displayableEvents whitelist + a render
    case in Logs.tsx. Without this the send would be invisible in the
    operator's own Log history (direct_message_sent has the same gap
    today, unfixed, out of scope for this report) — and "compose a message
    in Log" felt incomplete if sending it left no trace in that Log.
CONFLICT RESOLUTION (cherry-pick, not a merge — see 07): three files
conflicted against 87 days of intervening master history —
src/client/utils/logTriggers.ts, src/client/components/Sync.tsx,
src/client/components/Logs.tsx. All three conflicts were pure interleaving
(new command alongside existing commands, new tab alongside existing chat
tab, new Block alongside existing response Blocks) — nothing on either side
was dropped or overwritten. Diffed against a stashed baseline afterward to
confirm: identical pre-existing TS error set, one-for-one line-number shift
only.

--------------------------------------------------------------------------------
05 // FILES CHANGED
--------------------------------------------------------------------------------
PATH                                          STATUS
migrations/20260612120000_add-lot-mail.cjs    ADDED (lot_mails table)
src/server/models/lot-mail.ts                 ADDED
src/server/models/index.ts                    MODIFIED (register LotMail)
src/server/routes/api.ts                      MODIFIED (POST /mail, GET /mail/inbox,
                                               GET /mail/unread-count, PUT /mail/:id/read,
                                               lot_mail SSE case, lot_mail_sent Log trace + whitelist)
src/client/components/EmailInbox.tsx          ADDED (Sync "Mail" tab contents)
src/client/components/Sync.tsx                MODIFIED (Chat|Mail tab strip, unread badge,
                                               lot_mail SSE listener)
src/client/components/Logs.tsx                MODIFIED (email-compose trigger dispatch,
                                               compose panel, /system help line, MAIL:
                                               history render case)
src/client/utils/logTriggers.ts               MODIFIED (email-compose trigger: /email,
                                               /mail, ✉️/✉/📧)
src/client/components/ChatCatalystWidget.tsx  MODIFIED (Cohort match "✉ Mail" button ->
                                               Log compose prefill — the
                                               Cohort-Dating-through-Community path;
                                               fixed goTo('log') -> goTo('logs') bug)
src/client/queries.ts                         MODIFIED (useLotMailInbox, useLotMailUnreadCount,
                                               useSendLotMail, useMarkMailRead)
src/shared/types/index.ts                     MODIFIED (LotMail, LotMailRecord,
                                               LotMailSyncPayload, SyncEvents.lot_mail)
src/shared/constants/index.ts                 MODIFIED (MAX_LOT_MAIL_LENGTH)
src/client/components/About.tsx               MODIFIED (LOT Mail row: BEST/ship-candidate
                                               -> green-gated on this branch, corrected
                                               false "incorporated" framing)
docs/benchmark/LOT-MANIFEST.md                MODIFIED (LOT Mail row + ship-queue correction)
docs/benchmark/LOT-DOCTRINE.md                MODIFIED (Manifest Hygiene fold)
docs/benchmark/LOT-LEDGER.md                  APPENDED
docs/benchmark/LOT-SR-20260905-01.md          ADDED (this file)

--------------------------------------------------------------------------------
06 // SELF-ASSEMBLY
--------------------------------------------------------------------------------
LEDGER:      appended
LEXICON:     0 reused, 0 minted — "LOT MAIL" and "LOT COMMUNITY" are first
             uses this report; lexicon rule requires 3+ report occurrences
             or 2 doctrine folds before minting. Not decreeing a vocabulary
             for a feature that hasn't shipped to master yet.
DOCTRINE:    1 clause folded (Manifest Hygiene) rev unchanged (addendum, not
             reorganize)
WORDS:       ~660 (median 540) — trend ↑ (cherry-pick + conflict-resolution
             narrative runs longer than a pure feature report; expect this
             to compress on the next LOT Mail touch)

--------------------------------------------------------------------------------
07 // PUSH
--------------------------------------------------------------------------------
COMMIT:      BENCHMARK: ENGINEERING — LOT® Email ship: /email trigger, Sync
             Mail tab, Cohort→Community integration [VM]
HASH:        see tag benchmark-20260905-01 (raw hash is self-referential once
             this file's content is included in the commit it describes;
             the tag is the stable pointer)
TAG:         benchmark-20260905-01 (created LOCALLY only — see note below)
PUSH:        origin/claude/determined-turing-ab29i4 PUSHED

DEVIATION FROM STANDARD SHIP MODE (recorded, not concealed): Ship Mode as
written targets master via a disposable staging branch. This session's
harness-level branch policy restricts pushes to the single assigned branch
claude/determined-turing-ab29i4 and forbids pushing anywhere else without
explicit permission that no one was present to give (this is a scheduled,
unattended run). That policy sits above this skill's instructions. So: the
cherry-pick, conflict resolution, and green gate all happened exactly as Ship
Mode prescribes, but the landing branch is the assigned branch, not master.
LOT-MANIFEST.md and About.tsx both say this plainly rather than claiming a
master merge that didn't happen. Merging claude/determined-turing-ab29i4 (or
this feature specifically) into master is a follow-up action for S-2 or a
session explicitly authorized to touch master.

TAG PUSH FAILED: `git push origin benchmark-20260905-01` (and the equivalent
refspec form) returned a consistent `HTTP 403` + mid-transfer disconnect on
every one of 4 attempts (immediate 2s/4s/8s/16s backoff), while the branch
push moments earlier succeeded cleanly and instantly. Consistent, immediate,
and specific to the tag ref rather than the branch ref — that shape reads as
a permission or tag-protection restriction on this token, not a network
flake, so retries stopped per the proxy runbook's "don't retry a 403"
guidance rather than being hammered further. The tag exists locally at this
commit; it is NOT on the remote. A session with broader repo permissions (or
S-2 pushing it manually — `git push origin benchmark-20260905-01` from a
checkout of this branch) needs to land it for the rollback lattice to be
complete for this run.

--------------------------------------------------------------------------------
08 // POST-PUSH VERIFICATION
--------------------------------------------------------------------------------
RESULT:      GREEN (branch); TAG PUSH BLOCKED (see note above)
NOTES:       Tag benchmark-20260905-01 is local-only — push it manually to
             complete the rollback lattice for this session.
             Feature is code-complete and build-verified but NOT live for any
             user — it sits on an unmerged feature branch, and even after a
             master merge, `migrations:up` has to run against the production
             DB before the lot_mails table exists (this session has no DB
             access to run or verify that). Known gaps, stated plainly:
             reply threading and a standalone MailWidget (both named in the
             pre-existing About.tsx description) were never built in the
             source branch and are not built here either — the shipped
             surface is send + inbox + unread badge + Cohort compose-prefill,
             which is the actual "simplest" version the task asked for.
             direct_message_sent has the same missing-Log-trace gap that
             lot_mail_sent just got fixed for — worth the same fix in a
             future session. Next session picking up LOT Mail: read this
             report and LOT-MANIFEST.md before trusting either the OLD
             2026-06-27 "incorporated" note or assuming this session's push
             means it's live for operators.

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260905-01
================================================================================
