================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260630-02
TITLE:    Calendar Widget — Reliable Time-Tracking + Military-Grade Due Alerts
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  997bc3f → (applied below)
BRANCH:   claude/dreamy-babbage-0t6kzl
DATE:     2026-06-30
TIME:     23:58 UTC
RESULT:   GREEN
================================================================================

--------------------------------------------------------------------------------
00 // PREFLIGHT
--------------------------------------------------------------------------------
REPO:        OK        DOCS WRITABLE: OK
REMOTE:      origin    REACHABLE
LAST GREEN:  NONE (no benchmark tags exist on origin or local — Plan B
             unavailable this run; this session anchors benchmark-20260630-02)

NOTE: node_modules was absent on session start (fresh container). `npm ci`
failed on a pre-existing nanostores peer-dependency conflict in the lockfile
(@nanostores/react@0.4.1 wants nanostores@^0.7, root pins @0.9.5); resolved
with `npm ci --legacy-peer-deps`, matching how this lockfile has always had
to be installed. Not a regression — confirmed by reproducing on bare HEAD.

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        Standing instruction — continue Calendar widget development
                 independently. Keep minimalist UI. Make it reliably track
                 time and log into the Log with military-grade notifications.
CLASSIFICATION:  ENGINEERING
ACTION TAKEN:    CalendarWidget now surfaces overdue + due-today entries that
                 were previously invisible once their date passed. Added a
                 dual-path notification: client computes due/overdue live on
                 every render; new Job 25 (daily-calendar-due-scan, 07:00 UTC)
                 persists a calendar_due Log entry per affected user so the
                 alert survives a day the operator never opens the widget.
                 Wired the new event through displayableEvents, a CAL-ALERT:
                 Logs.tsx handler, and a new QIE pattern (P87).
ROUTED TO:       docs/benchmark/

--------------------------------------------------------------------------------
02 // CHECK A        03 // BUILD        04 // CHECK B (gate)
--------------------------------------------------------------------------------
COMMAND                              A        B
-------                              --       --
npm run server:build (tsc + esm fix) PASS     PASS
npm run client:css:build             PASS     PASS
npm run client:js:build              PASS     PASS
npm run build                        PASS     PASS
tsc --project tsconfig.json --noEmit 55 pre-existing errors, unchanged count
                                      before/after this diff (verified via
                                      git stash on bare HEAD) — none in
                                      changed files' new lines
BUILD:      yarn build -> OK (client 2.15s + server 5.16s)
GATE:       GREEN
FIX LOG:    npm ci → npm ci --legacy-peer-deps (pre-existing lockfile
            constraint, not introduced this session)

NOTES: Pre-existing carried-forward warnings, unaffected by this session:
  - badges.ts:1827/3112 duplicate key "first_signal"
  - browserslist caniuse-lite data 8 months old
  - tsconfig.json (client, not a repo-defined check) has 55 pre-existing
    strict-mode errors across 16 files, none touched this session

--------------------------------------------------------------------------------
05 // FILES CHANGED
--------------------------------------------------------------------------------
PATH                                                             STATUS
src/client/components/CalendarWidget.tsx                         MODIFIED
  - getTimeStatus(): TODAY / T-${n}D / OVERDUE ${n}D per entry
  - overdueEntries memo — entries past their date are no longer dropped
    from the view entirely (prior code only rendered date >= today)
  - CAL-ALERT banner: live due-today/overdue counts above "Add date"
  - dueTodayCount memo + useEffect → recordCalendarDueSignal()

src/client/stores/intentionEngine.ts                              MODIFIED
  - recordCalendarDueSignal() helper (source 'log', signal 'calendar_due')
  - Pattern 87 calendar-due-active: calendar_due signal in last 24h with
    overdueCount > 0 → confidence 0.6-0.85, suggestedWidget calendarWidget
  - calendarWidget dependency-map entry: + 'log' source (was missing
    despite calendar_entry already using source 'log')

src/client/components/Logs.tsx                                    MODIFIED
  - CAL-ALERT: handler for calendar_due (DUE TODAY / OVERDUE rows,
    COCKPIT-RULE format — instrument readings, no narration)

src/client/components/PatternRecognitionWidget.tsx                MODIFIED
  - getPatternName(): 'calendar-due-active' display string

src/server/routes/api.ts                                          MODIFIED
  - displayableEvents: + calendar_due (CRITICAL: event types absent from
    this whitelist never reach the client even though Log.create succeeds
    — same failure class documented in SR-20260630-01)

src/server/scheduled-jobs.ts                                      MODIFIED
  - Job 25 daily-calendar-due-scan (07:00 UTC): scans calendar_entry logs
    (30d lookback), writes one calendar_due Log per user with due-today
    or overdue entries (dueTodayCount, overdueCount, dueTodayText)
  - Registered in checkAndRunScheduledJobs() + initializeScheduledJobs()
    console manifest + hourly gate comment (hour 7 already open)

docs/benchmark/LOT-SR-20260630-02.md                               ADDED

--------------------------------------------------------------------------------
06 // SELF-ASSEMBLY
--------------------------------------------------------------------------------
LEDGER:      appended (see below)
LEXICON:     3 reused (MILITARY PURITY, COCKPIT-RULE, QIE) · 0 minted this
             session (DUAL-PATH NOTIFICATION folded once — needs a second
             doctrine fold or 3rd report appearance to earn a token) rev M
DOCTRINE:    2 clauses folded, rev M (no reorganize, letter unchanged):
             1. Dual-Path Time-Tracking Notification — client live-compute
                + server daily persisted scan, neither substitutes the other
             2. WARNING: Number(userId) on a UUID column silently breaks
                Log.create in 11 existing scan jobs (vitality-peak,
                badge-scan, signal-momentum, cognitive-depth, longitudinal-
                drift, QOS-mode-watch, and others) — NaN userId, swallowed
                by per-user try/catch, job reports false success. Job 25
                uses the plain string key and was verified correct. Not
                remediated elsewhere this session — flagged for a dedicated
                next session, each job's fix should be checked against live
                data rather than batch-patched blind.

WORDS:  964, measured via `wc -w` on this file (median 495 per last self-
        report) — trend ↑. NOTE: spot-checking prior reports shows their
        self-reported WORDS figures undercount actual `wc -w` by ~40-45%
        (SR-20260630-01 claimed 312, actual 545; SR-20260622-01 claimed
        310, actual 617) — the metric has not been measured consistently
        across sessions. This report uses the literal `wc -w` count;
        future sessions should do the same so the trend line is real.

--------------------------------------------------------------------------------
07 // PUSH
--------------------------------------------------------------------------------
COMMIT:  BENCHMARK: ENGINEERING — Calendar reliable time-tracking + CAL-ALERT military notifications [VM]
HASH:    (applied below)
TAG:     benchmark-20260630-02
PUSH:    origin/claude/dreamy-babbage-0t6kzl PUSHED

--------------------------------------------------------------------------------
08 // POST-PUSH VERIFICATION
--------------------------------------------------------------------------------
RESULT:   GREEN
NOTES:    - Dependencies were not pre-installed in this container; `npm ci
            --legacy-peer-deps` is required until the nanostores peer range
            in package.json is reconciled (root pins ^0.9.0, @nanostores/
            react@0.4.1 wants ^0.7.0) — worth a one-line package.json fix
            in a future session so plain `npm ci` works again.
          - No benchmark-* tags existed anywhere (local or origin) before
            this session, despite multiple prior reports claiming to create
            them — this session actually pushes benchmark-20260630-02,
            anchoring the rollback lattice for real going forward.
          - Top priority for next session: the Number(userId) UUID bug
            above. It likely means several "reliable" background
            notifications (vitality_peak, signal_momentum, etc.) have been
            silently failing to write since they were built. Verify against
            a live Log table before patching.
          - Browser/live-DB verification of the Calendar UI was not
            possible in this sandboxed session (no database credentials
            configured) — verified by full build + strict typecheck diff
            + manual trace of every changed code path instead. Flag for a
            human or DB-attached session to confirm visually.

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260630-02
================================================================================
