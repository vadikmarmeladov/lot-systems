================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260817-01
TITLE:    Widget Wiring Health Scan — QIE / Memory / Portrait / System Tab
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  98971f2
BRANCH:   claude/charming-albattani-ofbejf
DATE:     2026-08-17
TIME:     03:41 UTC
RESULT:   GREEN
================================================================================

--------------------------------------------------------------------------------
00 // PREFLIGHT
--------------------------------------------------------------------------------
REPO:        OK        DOCS WRITABLE: OK
REMOTE:      origin    REACHABLE
LAST GREEN:  NONE (no local benchmark-* tags exist — tags are back-filled on
             master by the benchmark-tag-lattice GitHub Action; this sandbox's
             git relay rejects tag-ref pushes, per that workflow's own header
             comment. Plan B unavailable this run; not needed — gate stayed
             green throughout.)
node_modules: ABSENT at session start. `yarn install` failed (registry.
             yarnpkg.com rejected by proxy, 403). `npm ci` succeeded via
             registry.npmjs.org (proxy-allowlisted) with --legacy-peer-deps —
             root package.json carries an unresolved nanostores@0.9.5 vs.
             @nanostores/react peer (^0.7.0) conflict, pre-existing, noted
             for a future DEPLOYMENT-class pass, not touched this session.

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        Scheduled directive — full widget health scan: wiring/data/
                  testing; QIE, Memory, Story, System tab, Portrait wiring;
                  interactive-widget context sync + military Log posting;
                  "15-month" UI reveal cadence; loading speed; lag test
                  (buttons/sounds/controls/loading); usage data logging.
CLASSIFICATION:  ENGINEERING
ACTION TAKEN:    Read-path audit of System.tsx's widget stack (43 widget
                  components, WIDGETS.md architecture doc), the QIE
                  (intentionEngine.ts, 5800+ lines), the Memory/Story
                  pipeline, and the Portrait page (PublicProfile.tsx) against
                  their actual server contracts. Found and fixed 3 confirmed
                  server↔client wiring gaps. Verified the prior lag-fix pass
                  (SR-20260719-01: logs LIMIT 500, staleTime tiers, render-
                  isolation) is still intact — no regression found in the
                  files this session touched. No "15-month" artifact exists
                  anywhere in docs/ or src/; that line is read as a request
                  to review the widget visibility-gating cadence, which
                  WIDGETS.md already documents (time/cooldown/activity/
                  subscription/mode/pattern-gated) — marked PROVISIONAL, no
                  such roadmap doc was found to audit against.
ROUTED TO:       docs/benchmark/ (this report is the artifact of record)

--------------------------------------------------------------------------------
02 // CHECK A        03 // BUILD        04 // CHECK B (gate)
--------------------------------------------------------------------------------
COMMAND                               A        B
-------                               --       --
npm run client:build (esbuild+css)    PASS     PASS
npm run server:build (tsc typecheck)  PASS     PASS
BUILD:                                npm run build -> OK
GATE:                                 GREEN (clean both passes)
FIX LOG:
  F1  src/server/models/user.ts + src/shared/types/index.ts — useProfileView()
      never picked `joinedAt` from the user row, so GET /me never sent it.
      MonthlyPulseWidget (mounted in System.tsx:558) computes its fire
      condition from user.joinedAt; with it always undefined, monthNumber
      was permanently 0 and the widget's useEffect gate (monthNumber < 1)
      never passed — the widget built in SR-20260707-01 has never fired for
      any user. Added 'joinedAt' to the useProfileView() pick list and to
      the shared UserProfile type.
  F2  src/client/stores/intentionEngine.ts:3332 — QIE pattern P147 (quantum-
      presence-field) referenced an undeclared `dayMs`, a ReferenceError on
      every evaluation. analyzeIntentions() is called unguarded (no try/
      catch) from System.tsx's main useEffect on every logs change; any
      operator who reaches P142 (adaptive-signal-web) + P137 (quantum-
      coherence-peak) simultaneously — a real, reachable state per existing
      pattern logic — throws inside that effect, which stops quantumState,
      the assembly recompute, and every widget reading intentionEngine
      (Quantum State, Pattern Recognition, Signal Stream, System Progress)
      from updating for that render. Replaced with the file's standard
      inline 24*60*60*1000 (used at 15+ other sites; no local dayMs const
      exists anywhere in the file).
  F3  src/client/queries.ts — useCreateMemory's response type declared only
      { response: string }, but /api/memory/answer (api.ts) returns
      { response, insight, answerCount }, and MemoryWidget.tsx already
      destructures and renders `insight` on success. Runtime was correct;
      the type hid a real field, so a future refactor could delete or
      rename `insight` on either side without the build catching it.
      Widened the type to match the server payload. (Also corrected a
      stale comment on the useLogs staleTime — /api/logs has carried
      limit: 500 since SR-20260719-01; the comment still said "no LIMIT".)

--------------------------------------------------------------------------------
05 // FILES CHANGED
--------------------------------------------------------------------------------
PATH                                          STATUS
src/server/models/user.ts                     MODIFIED
src/shared/types/index.ts                     MODIFIED
src/client/stores/intentionEngine.ts          MODIFIED
src/client/queries.ts                         MODIFIED
docs/benchmark/LOT-SR-20260817-01.md          ADDED

--------------------------------------------------------------------------------
05b // OPEN FINDINGS (not fixed this session — recorded for the next pass)
--------------------------------------------------------------------------------
- src/client/utils/badges.ts has two duplicate object keys — `quarter_drop`
  (3214 and 5501) and `elixir_found` (4825 and 6883) — flagged by both
  esbuild (duplicate-object-key warning on every client build) and tsc
  (TS1117). The second definition silently wins; one badge ID's original
  content is unreachable. Left unresolved: fixing it correctly needs a
  design call (rename one badge vs. merge intent) that a mechanical pass
  shouldn't make.
- Root tsconfig.json (client, strict) currently reports ~90 pre-existing
  type errors across AdminUser.tsx, Logs.tsx, PublicProfile.tsx (residual,
  beyond F1/F3), UserMetricsWidget.tsx, router.ts, badges.ts and others.
  None gate the build — client:js:build runs through esbuild, which
  transpiles without type-checking — so this is a blind spot: only
  tsconfig.server.json is a real CI gate. Recommend a dedicated ENGINEERING
  pass to burn this list down; each entry is a candidate for the same class
  of silent wiring gap fixed above.
- No live browser session was run this pass (no dev server / Playwright
  pass against the running app) — the loading-speed and lag findings above
  are static-analysis-verified (staleTime tiers, query limits, effect
  scoping), not measured against a rendered page. Flagging explicitly
  rather than claiming a UI lag measurement that wasn't taken.

--------------------------------------------------------------------------------
06 // SELF-ASSEMBLY
--------------------------------------------------------------------------------
LEDGER:      appended (20260817-01)
LEXICON:     1 reused (GREEN-GATE). 1 new CANDIDATE — TYPE-DRIFT (client-side
             type declaration falls behind the server's actual payload;
             TypeScript can't catch it because only the server tsconfig
             gates the build; the widget keeps working if the field is used,
             or silently never fires if the gap is on the read side, as in
             F1). First appearance — not yet earned lexicon status (needs
             3+ recurrence per protocol).
DOCTRINE:    rev N->O. 1 clause added — Client Build Is Not Type-Checked:
             npm run client:build compiles via esbuild (client.build.ts),
             which transpiles TypeScript without verifying it; only
             `npm run server:build` (tsc --project tsconfig.server.json)
             is a real gate. A clean CHECK A/B on this repo does not mean
             the client is type-error-free — root tsconfig.json must be
             run separately (`npx tsc --noEmit`) to see client-side drift,
             and it currently carries ~90 pre-existing errors (05b).
WORDS:       ~700 (median 628 of last 15 reports) — trend ≈ (in line with
             SR-20260719-01's 612; report density holding steady, not yet
             compressing further)

--------------------------------------------------------------------------------
07 // PUSH
--------------------------------------------------------------------------------
COMMIT:      BENCHMARK: ENGINEERING — widget wiring health scan: joinedAt/
             MonthlyPulse, QIE P147 dayMs crash, Memory insight type [VM]
HASH:        (recorded post-commit)
TAG:         benchmark-20260817-01 (local tag attempted; expected to be
             rejected by this sandbox's git relay per benchmark-tag-lattice
             .yml's documented HTTP 403 — backfilled by that Action once
             this lands on master)
PUSH:        origin/claude/charming-albattani-ofbejf PUSHED

--------------------------------------------------------------------------------
08 // POST-PUSH VERIFICATION
--------------------------------------------------------------------------------
RESULT:      GREEN
NOTES:       Three confirmed wiring fixes: one dead widget revived
             (MonthlyPulseWidget — never fired since it shipped, SR-
             20260707-01), one live crash path closed in the QIE (P147,
             reachable by real long-tenure operators), one type/runtime
             drift closed on the Memory answer flow. Two findings left open
             for the next session (badges.ts duplicate keys — needs design
             judgment; the ~90-error client tsc backlog — needs a dedicated
             pass). No live-browser lag measurement was taken; say so rather
             than claim one.

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260817-01
================================================================================
