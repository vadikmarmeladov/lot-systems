================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260825-01
TITLE:    Basics tab — LOT-FM-001 BASIC ration module, integrated to master
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
LAST GREEN: benchmark-20260805-01
BRANCH:   claude/beautiful-johnson-b8ifka
DATE:     2026-08-25
RESULT:   GREEN
================================================================================

--------------------------------------------------------------------------------
00 // PREFLIGHT
--------------------------------------------------------------------------------
REPO:        OK        DOCS WRITABLE: OK
REMOTE:      origin    REACHABLE
TASK:        Scheduled routine — "research and develop the Basics tab" per
             LOT-FM-001 self-assembly directive (90-day / 3-month build spec).

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        LOT-FM-001 self-assembly directive (scheduled task body) —
                  quartermaster doctrine spec for the BASIC civilian ration
                  subscription: 23-item load, terminal visual spec, OPEN TAB,
                  UPGRADE state machine, 3-month build (Ledger & Doctrine ->
                  Upgrade & Roster -> Issue & Fulfillment).
CLASSIFICATION:   ENGINEERING — feature integration, not new build.
DISCOVERY:        The full 3-month build already existed, complete, on a
                  stale feature branch: origin/claude/nifty-allen-jWyOe,
                  commit 8b07ca96c "feat(basics): implement LOT-FM-001 BASIC
                  ration tab — 3-month build" (2026-05-30). It matched the
                  scheduled directive's Month 1/2/3 spec almost verbatim and
                  was already logged in About.tsx's feature registry
                  ("Basics Tab ... Status: BEST — ship candidate") but never
                  merged. Master had advanced ~90 commits past that branch's
                  base since May 30 (badges v31, QIE v113, etc.) — a raw
                  merge would have reverted three months of unrelated master
                  work, so a naive merge was rejected in favor of an
                  isolated cherry-pick.
ACTION TAKEN:     Cherry-picked the single Basics-feature commit onto current
                  master, resolved 3 conflicts against the codebase's
                  evolution since May 30, verified, and pushed.
ROUTED TO:        src/client/components/Basics/, src/server/models/,
                  src/server/routes/api.ts, migrations/ (existing locations
                  — no new folders required beyond the feature's own).

--------------------------------------------------------------------------------
02 // CONFLICT RESOLUTION
--------------------------------------------------------------------------------
FILE                          CAUSE                          RESOLUTION
----                          -----                          ----------
app.tsx                       Tab-panel architecture changed  Ported Basics into
                               after May 30 (old visited-Set   the current
                               + per-tab <TabPanel route=..>   TabPanel/TabPanels
                               pattern replaced by memoized    memo pattern —
                               TabPanels/TabPanel components,  added 'basics' to
                               commit 7b246061 "Fix slow tab   PersistentRoute
                               switching").                    union + one
                                                               <TabPanel> entry.
queries.ts                    Both sides append a new hook    Kept both blocks
                               section at the same insertion   sequentially (QI /
                               point (QI, Assembly, Prayer,    Assembly / Prayer
                               Story vs. Basic Ration).         / Story, then
                                                               Basic Ration).
api.ts                        Same additive-append shape as    Closed the /story
                               queries.ts, but mid-function —   handler properly
                               HEAD's chunk ended inside the    (}, ), }) before
                               /story catch block.              opening the Basic
                                                               Ration route block.
Layout.tsx, router.ts,        Simple additive changes on both  Auto-merged clean,
index.css, shared/types.ts,   sides, no overlap.               no manual edit.
models/index.ts

Verification: grepped the full src/ tree for leftover conflict markers post-
resolution — none found.

--------------------------------------------------------------------------------
03 // FIXES BEYOND THE PORT
--------------------------------------------------------------------------------
F1  RosterBlock.tsx types    FieldDef.options was `string[]`; SIZING_OPTIONS
                              entries are `as const` tuples (readonly). Widened
                              to `readonly string[]` — 2 new TS4104 errors this
                              session introduced, now 0.
F2  index.css import order   `@import './components/Basics/basics.css'` sat
                              after `@tailwind base/components/utilities`,
                              which is invalid per CSS spec (@import must
                              precede all statements but @charset/@layer).
                              Moved the Basics import above the @tailwind
                              directives — postcss-import warning cleared.

--------------------------------------------------------------------------------
04 // CHECK A        05 // BUILD        06 // CHECK B (gate)
--------------------------------------------------------------------------------
COMMAND                                        A        B
-------                                        --       --
npx tsc --project tsconfig.json --noEmit       diffed   0 new errors
  (client, strict)                             vs base  (128 pre-existing,
                                                          unrelated, unchanged)
npx tsc --project tsconfig.server.json --noEmit PASS    PASS (clean, 0 errors)
npm run client:js:build (esbuild)              PASS     PASS
npm run client:css:build (postcss)             WARN->0  PASS (import-order
                                                          warning fixed)
npm run server:build (tsc + esm-fix)           PASS     PASS
GATE:                                                   GREEN
FIX LOG:                                                F1, F2 (above)

No live DB available in this environment (DB_HOST unset) — runtime/API smoke
test against a real Postgres instance was not performed. Build + strict
typecheck + manual trace of every Basics component against its query/route
counterpart stand in for it this session.

--------------------------------------------------------------------------------
07 // WHAT SHIPPED (Months 1-3, all present in the ported commit)
--------------------------------------------------------------------------------
MONTH 1 — LEDGER & DOCTRINE
  /basics route + nav tab enabled (was greyed-out stub since c5f08486).
  23-item RATION_MANIFEST (nomenclature + cadence; COGS withheld from client).
  DoctrineBlock: USD 100/mo price line, issue-not-sell doctrine text.
  LOT-BASIC terminal token system (basics.css): Liberation Mono / Courier New
  fallback stack, white ground / black ink, 2px rules, square corners
  (border-radius: 0 !important scoped to .lot-basic), inversion-only
  hierarchy (.lb-inv).
  StatusLine component: live state + next-issue date + issue count.

MONTH 2 — UPGRADE & ROSTER
  UpgradeBlock: USERSHIP -> PENDING -> ON_STRENGTH -> STEADY_STATE.
  STAND DOWN: destroys the BasicSubscription row, cancels SCHEDULED issues,
  drops the ration, retains the underlying Usership/AI plan.
  RosterBlock: sizing (shirt/shoe) + shipping address intake form.
  basic_subscriptions table (migration 20260530000001) + BasicSubscription
  model. Routes: GET /api/basic/subscription, POST /api/basic/upgrade,
  POST /api/basic/roster, POST /api/basic/stand-down.

MONTH 3 — ISSUE & FULFILLMENT
  IssueLogBlock + NextIssueBlock: per-issue load engine. Cadence math: 17
  base monthly items + 4 on quarterly months + 2 on semi-annual months,
  landing on 23 in the first full cycle.
  ManifestCard: full-screen printable issue manifest.
  basic_issues table (migration 20260530000002) + BasicIssue model, indexed
  on subscriptionId/userId. Routes: GET /api/basic/issues,
  POST /api/basic/issues/:id/advance (PACKED/SHIPPED/DELIVERED/CANCELLED;
  DELIVERED auto-advances subscription to STEADY_STATE and schedules the
  next issue).
  Margin ceiling (RATION_COGS_CEILING_USD = 40, RATION_MARGIN_FLOOR_PCT = 60)
  encoded as constants; enforcement against real supplier COGS is a
  fulfillment-ops process this repo does not (and should not) automate —
  no payment processor or supplier integration is wired, by design: this
  session ships the product surface, not live billing or real shipments.

--------------------------------------------------------------------------------
08 // FILES CHANGED (this session)
--------------------------------------------------------------------------------
PATH                                                        STATUS
migrations/20260530000001_add-basic-subscriptions.cjs       ADDED
migrations/20260530000002_add-basic-issue-log.cjs           ADDED
src/client/components/Basics/Basics.tsx                     ADDED
src/client/components/Basics/DoctrineBlock.tsx               ADDED
src/client/components/Basics/IssueLogBlock.tsx               ADDED
src/client/components/Basics/ManifestCard.tsx                ADDED
src/client/components/Basics/ManifestLedger.tsx              ADDED
src/client/components/Basics/NextIssueBlock.tsx              ADDED
src/client/components/Basics/RosterBlock.tsx                 ADDED (+ F1)
src/client/components/Basics/StatusLine.tsx                  ADDED
src/client/components/Basics/UpgradeBlock.tsx                 ADDED
src/client/components/Basics/basics.css                      ADDED
src/client/components/Basics/constants.ts                    ADDED
src/client/components/Basics/index.ts                        ADDED
src/server/models/basic-issue.ts                             ADDED
src/server/models/basic-subscription.ts                       ADDED
src/client/components/ui/Layout.tsx                           MODIFIED (merge)
src/client/entries/app.tsx                                    MODIFIED (merge)
src/client/index.css                                          MODIFIED (merge + F2)
src/client/queries.ts                                         MODIFIED (merge)
src/client/stores/router.ts                                   MODIFIED (merge)
src/server/models/index.ts                                    MODIFIED (merge)
src/server/routes/api.ts                                      MODIFIED (merge)
src/shared/types/index.ts                                     MODIFIED (merge)
docs/benchmark/LOT-SR-20260825-01.md                          ADDED

--------------------------------------------------------------------------------
09 // NEXT SESSION
--------------------------------------------------------------------------------
- Run the DB migrations against a live environment and smoke-test the
  /basics tab end to end (currently untestable here — no DB_HOST).
- Update the About.tsx feature registry "Basics Tab" row: status moves from
  "on feature branch nifty-allen-jWyOe, BEST" to "shipped to master,
  LOT-SR-20260825-01."
- Confirm real COGS quotes against RATION_COGS_CEILING_USD (40) once a
  supplier is sourced — this is a business decision outside this session's
  scope, deliberately left undone.
- STAND DOWN currently hard-deletes the BasicSubscription row rather than
  reverting it to a USERSHIP-state record; fine for Month 2 semantics
  (drops ration, retains AI) but worth a second look before real users hit
  it, since it also loses shipping/sizing history on re-upgrade.
================================================================================
