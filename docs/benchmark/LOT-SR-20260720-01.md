================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260720-01
TITLE:    BASICS tab — LOT-FM-001 Month 1 recovered, ported, and verified live
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  1.3.0 (39ba0440)
BRANCH:   claude/beautiful-johnson-j9j6ux
DATE:     2026-07-20
TIME:     01:15 UTC
RESULT:   GREEN
OPERATOR: LOT Self-Assembly Routine (automated, scheduled)
================================================================================

--------------------------------------------------------------------------------
00 // PREFLIGHT
--------------------------------------------------------------------------------
REPO:        OK        DOCS WRITABLE: OK
REMOTE:      origin    REACHABLE
LAST GREEN:  add997e6 (Merge PR #88, master)
BRANCH BASE: claude/beautiful-johnson-j9j6ux forked clean off master @ add997e6

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        S-2 directive — LOT-FM-001 Self-Assembly, BASIC (ration) module,
                  90-day build. "Research and develop the (Basics) tab."
CLASSIFICATION:  PRODUCT — new tab, Month 1 of 3 (OPEN TAB / read-only ledger)
ACTION TAKEN:    Located a complete, unmerged Month-1 build already on the
                  branch lineage (origin/claude/beautiful-johnson-56p7ov,
                  commit 6815f550, marked BEST in LOT-MANIFEST.md), verified
                  it against current doctrine, ported it forward onto this
                  branch, closed one type-safety gap, and confirmed the tab
                  renders correctly in both ON STRENGTH and public states.
ROUTED TO:       src/client/components/Basics.tsx + basics/doctrine.ts (code);
                  this report (docs/benchmark/)

--------------------------------------------------------------------------------
02 // RESEARCH FINDING — the work already existed
--------------------------------------------------------------------------------
Before writing any code, an Explore pass over the repo (tab routing, design
tokens, prior ration/Usership references, data models, house component
patterns) surfaced that LOT-FM-001 Month 1 had already been built, GREEN-
gated, and session-reported on 2026-06-12 — on branch
claude/beautiful-johnson-56p7ov — and never merged to master or any ship
branch. LOT-MANIFEST.md had it catalogued as BEST among three competing
iterations (nifty-allen-jWyOe x6, beautiful-johnson-56p7ov, eager-clarke-
wTEM6). The nav placeholder ("Basics", no route, greyed out per commit
c5f08486 "Gray out coming-soon nav tabs") was the only trace on master.

Given the branch-name lineage match (claude/beautiful-johnson-j9j6ux is a
continuation of the same beautiful-johnson series), the correct action was
to recover and ship that existing BEST iteration rather than re-derive it —
consistent with LOT-DOCTRINE's Manifest Hygiene principle (superseded work
is provenance, not waste; BEST iterations get shipped, not rebuilt).

--------------------------------------------------------------------------------
03 // BUILD
--------------------------------------------------------------------------------
Cherry-picked 6815f550 onto claude/beautiful-johnson-j9j6ux. Two conflicts,
both from architecture drift since June (TabPanel/TabPanels render-isolation
refactor, SR-20260602-01 / SR-20260719-01):

  docs/benchmark/LOT-SR-20260612-01.md   filename collision with an unrelated
                                          report from a different branch that
                                          reused the same date+seq on master.
                                          Kept master's report; this session's
                                          work gets its own new-dated file
                                          (Manifest Hygiene: never overwrite
                                          history).
  src/client/entries/app.tsx             Old commit inlined five <TabPanel>
                                          calls directly in App's render; HEAD
                                          now mounts a single memoized
                                          <TabPanels/> component (perf fix,
                                          SR-20260602-01/20260719-01). Resolved
                                          by adding the 'basics' TabPanel
                                          inside the current TabPanels body
                                          instead of reintroducing the old
                                          inline pattern.

One self-inflicted defect caught by typecheck before commit: the conflict
resolution left a duplicate `import { Basics }` line — fixed before staging.

FOLLOW-UP FIX (separate commit): Basics.tsx gated ON STRENGTH on the bare
string literal 'Basic' against user.tags — there was no UserTag.Basic enum
member. Added it to UserTag (src/shared/types/index.ts) and to
USER_TAGS_BY_ID (src/shared/constants/index.ts) so the check is type-safe
and the tag is visible in the admin tag editor ahead of M2 roster intake.

--------------------------------------------------------------------------------
04 // CHECK A/B (gate)
--------------------------------------------------------------------------------
COMMAND                                        RESULT
-------                                        ------
npm install --legacy-peer-deps                 OK (registry.yarnpkg.com is
                                                 blocked by org egress policy
                                                 in this sandbox; registry.
                                                 npmjs.org via npm is not —
                                                 switched package managers,
                                                 no repo config changed)
npx tsc --noEmit -p tsconfig.json              PASS on all Basics-touched
                                                 files (app.tsx, Basics.tsx,
                                                 doctrine.ts, router.ts,
                                                 Layout.tsx, shared/types,
                                                 shared/constants). 89
                                                 pre-existing errors remain
                                                 elsewhere in the tree,
                                                 unrelated to this change and
                                                 not introduced by it (this
                                                 repo has no typecheck gate in
                                                 its build pipeline today).
npx esr client.build.ts -prod (esbuild)        PASS — client bundle clean
postcss index.css                              PASS
npx tsc -p tsconfig.server.json (server build) PASS
GATE:                                          GREEN

--------------------------------------------------------------------------------
04B // LIVE RENDER VERIFICATION
--------------------------------------------------------------------------------
Full server run requires a provisioned Postgres instance and auth session not
available in this sandbox. Built a scoped harness instead: a temporary entry
point imported the real Basics.tsx, seeded the actual nanostores atoms
(stores.me, stores.isMirrorOn) with representative users, bundled with the
project's real esbuild config/aliases, served the real compiled
dist/client/css/index.css, and screenshotted with headless Chromium across
three states (NONE / USERSHIP / ON STRENGTH). All three rendered correctly:
23-item ledger grouped by NUTRITION(10)/HEALTH(7)/HYGIENE(5)/EQUIPMENT(1),
doctrine lines, USD 100.00/MO. price line, status line switching PLAN/RATION
text correctly, upgrade block appearing only for non-ON-STRENGTH users and
disappearing once BASIC is active. Zero console errors. Harness files
deleted before commit — nothing shipped.

--------------------------------------------------------------------------------
05 // MONTH 1 DELIVERABLES (LOT-FM-001 envelope)
--------------------------------------------------------------------------------
  [x] OPEN TAB surface — Basics tab live and routable at /basics
  [x] 23-item ration manifest as ledger (nomenclature + spec + cadence;
      COGS withheld from the public surface)
  [x] Doctrine statement — 4 lines, quartermaster voice, zero marketing copy
  [x] Price line — USD 100.00 / MO.
  [x] Status-line component — reads user tags, reports PLAN + RATION state
  [x] Terminal tokens — font-mono, 2px/1px rule hierarchy, square corners,
      uppercase tracked labels, tabular-nums, white ground / acc ink only
  [x] Upgrade scaffold visible to non-ON-STRENGTH users; enrollment
      explicitly gated to M2 ("ENROLLMENT OPENS — M2 BUILD CYCLE")
  [x] UserTag.Basic formalized (was a bare string literal)

EXIT CONDITION: "A stranger can read what LOT issues and on what terms.
Read-only. Live." — ACHIEVED.

--------------------------------------------------------------------------------
06 // STATUS AGAINST THE 90-DAY ENVELOPE
--------------------------------------------------------------------------------
M1 (this session):  DONE — read-only OPEN TAB, live on this branch.
M2 (next):           UPGRADE control + state machine
                      (USERSHIP/AI -> PENDING -> ON STRENGTH -> STEADY STATE).
                      Roster intake (sizing, shipping address, cadence start).
                      Recurring $100/mo additive billing on top of Usership.
                      STAND DOWN downgrade (drops ration, retains AI).
                      Issue log scaffold.
                      Exit: a Usership member can go ON STRENGTH and back
                      OFF, end to end.
M3 (final):           Month-by-month load engine per cadence. Supplier quotes
                      confirmed against the USD 40.00 COGS ceiling. Printed
                      manifest card generation. First issue scheduled and
                      dispatched. Issue log accrues, NEXT ISSUE advances.
                      Exit: first real ration ships to a real subscriber;
                      margin verified >=60%.

--------------------------------------------------------------------------------
07 // M2 SCOPE NOTES (for the next build cycle)
--------------------------------------------------------------------------------
No billing or enrollment backend exists anywhere in the codebase today —
User.stripeCustomerId is a bare column, never read or written. M2 is real
work, not wiring:
  - POST /api/basics/upgrade  — Stripe subscription create (additive to
    Usership, not standalone), tag Basic on success webhook, PENDING state
    until first roster intake (size/address) is captured.
  - POST /api/basics/stand-down — cancel the additive subscription, drop
    Basic tag, retain Usership.
  - Roster intake form — sizing, shipping address, cadence start date;
    reuses User.address/city/country/phone already on the User model.
  - src/client/stores/basics.ts — new nanostores atom for enrollment state
    (PENDING/ON STRENGTH/STAND DOWN), mirroring the existing
    stores/plannerWidget.ts pattern, so Basics.tsx and Settings.tsx read the
    same live state.
  - A prior alt-design branch (origin/claude/eager-clarke-wTEM6, final
    commit 97de1d52) already prototyped a BasicsPlanSection enrollment
    widget embedded in Settings.tsx with Enroll/pending/enrolled/stand-down
    states — worth reviewing as a structural reference for M2's roster
    intake UI, though its visual design should be discarded in favor of the
    terminal-grid tokens already established in Basics.tsx/doctrine.ts.

--------------------------------------------------------------------------------
08 // PUSH
--------------------------------------------------------------------------------
Commits this session (claude/beautiful-johnson-j9j6ux):
  1d918e7b  BASICS M1: OPEN TAB live — 23-item ration ledger, doctrine, status line
  39ba0440  Formalize UserTag.Basic for the BASIC ration enrollment flag
  (this report)

================================================================================
END REPORT
================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
================================================================================
