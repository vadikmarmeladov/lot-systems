================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260720-01
TITLE:    Astrology block — Logs-correlated personalization + cross-widget sync
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  add997e   (no benchmark-* tag reachable from HEAD — see note in 06)
BRANCH:   claude/practical-curie-zsl6wg
DATE:     2026-07-20
TIME:     22:21 UTC
RESULT:   GREEN
================================================================================

--------------------------------------------------------------------------------
00 // PREFLIGHT
--------------------------------------------------------------------------------
REPO:        OK        DOCS WRITABLE: OK
REMOTE:      origin    REACHABLE
LAST GREEN:  NONE FOUND — `git tag --list 'benchmark-*'` returns empty on this
             clone even after `git fetch --tags`. Prior reports (e.g.
             LOT-SR-20260719-01) record tags/hashes (benchmark-20260719-01,
             1fddbba) that do not resolve here. Recorded honestly rather than
             fabricating a rollback target; this session's tag
             (benchmark-20260720-01) becomes the first verifiable Plan-B
             anchor going forward.

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        Scheduled routine — S-2 instruction to continue evolving the
                  Astrology block (ambient zodiac hour / moon phase / rokuyo,
                  not a natal chart) for user personalization and widget
                  synchronization, sync it with Logs entries, and push a
                  session report each run.
CLASSIFICATION:  ENGINEERING
ACTION TAKEN:    (1) One shared getAstrologySnapshot() in astrology.ts as the
                  single source of ambient zodiac/moon/rokuyo data. (2) New
                  correlateLogsWithRokuyo() — personalization layer that reads
                  the user's own emotional_checkin Logs and correlates logged
                  mood valence against the Rokuyo day it was logged on,
                  surfaced in System.tsx's Astrology block. (3) QuantumSignWidget
                  deduped its own day-of-year calc onto the shared util and its
                  "astrology" patch now keys off the real Rokuyo index instead
                  of an unrelated day-of-year rotation.
ROUTED TO:       docs/benchmark/   (report); code pushed on branch above.

--------------------------------------------------------------------------------
02 // CHECK A        03 // BUILD        04 // CHECK B (gate)
--------------------------------------------------------------------------------
COMMAND                                    A        B
-------                                    --       --
npx tsc --noEmit -p tsconfig.server.json   PASS     PASS
npm run client:js:build (esbuild, prod)    PASS     PASS
npm run client:css:build (postcss)         PASS     PASS
npm run server:build (tsc + esm-fix)       PASS     PASS
BUILD:                                     npm run build -> OK (all four above)
GATE:                                       GREEN
FIX LOG:                                    (none — clean gate on first pass)
NOTE:                                       Root tsconfig.json (client+server
                                            combined, strict) has ~60
                                            pre-existing errors unrelated to
                                            this change (confirmed via targeted
                                            grep — zero hits in astrology.ts /
                                            System.tsx / QuantumSignWidget.tsx
                                            diffs). Client build itself does
                                            not run tsc (esbuild strips types),
                                            so it is not part of the repo's
                                            actual green gate; recorded here so
                                            the next session doesn't re-diagnose
                                            it as new.

--------------------------------------------------------------------------------
05 // WHAT CHANGED
--------------------------------------------------------------------------------
SRC-OF-TRUTH   getAstrologySnapshot(date) in shared/utils/astrology.ts returns
               {westernZodiac, hourlyZodiac, rokuyo, rokuyoIndex, moonPhase,
               moonIllumination, moonEmoji, dayOfYear}. System.tsx's astrology
               useMemo now calls it directly instead of recomputing four
               separate calls; QuantumSignWidget imports getDayOfYear() from
               the same module instead of inlining the day-of-year formula a
               second time.
ROKUYO-CORR    New correlateLogsWithRokuyo(logs) in the same module: buckets
               emotional_checkin logs by the Rokuyo day of their createdAt,
               scores each logged emotionalState on a -2..+2 valence table,
               and — only once at least 2 Rokuyo days have 3+ check-ins each —
               returns {best, challenging, sampleSize}. Returns null below that
               threshold; never fabricates a pattern from thin data. The
               ambient Rokuyo cycle itself stays identical for every user;
               only the correlation is personal, and it is derived strictly
               from that user's own Logs history (useLogs()).
SURFACE        System.tsx astrologyView 'astrology' pane now shows a second,
               dimmed line under the existing zodiac/moon/rokuyo string: "Your
               logged mood runs brightest on <Rokuyo> days[, hardest on
               <Rokuyo>] (<N> check-ins)" — only rendered when the correlation
               is non-null. The compact non-paid layout's Astrology block is
               left as ambient-only, consistent with its existing "no AI, just
               essentials" design intent (comment at System.tsx:382).
CROSS-SYNC     QuantumSignWidget's "astrology" patch (Lunar Reset / Solar
               Return / Mercury Direct / Venus Transit) now indexes off
               snapshot.rokuyoIndex % 4 instead of an independent
               dayOfYear % 4 — so it can no longer silently disagree with the
               Rokuyo value the Astrology block is showing the same user in
               the same session. Its "psychology" patch is unaffected (still
               dayOfYear-keyed; it isn't an astrology-dimension concept).

--------------------------------------------------------------------------------
05b // FILES CHANGED
--------------------------------------------------------------------------------
PATH                                                     STATUS
src/shared/utils/astrology.ts                            MODIFIED
src/client/components/System.tsx                         MODIFIED
src/client/components/QuantumSignWidget.tsx               MODIFIED
docs/benchmark/LOT-SR-20260720-01.md                      ADDED

--------------------------------------------------------------------------------
06 // SELF-ASSEMBLY
--------------------------------------------------------------------------------
LEDGER:      appended (20260720-01)
LEXICON:     0 minted. 1 reused: GREEN-GATE. 2 recorded as first-appearance
             CANDIDATES (PROVISIONAL — earn a token only on 3+ recurrence):
               AMBIENT-SRC   one shared pure function is the only legal source
                             of a same-for-all-users ambient value (weather,
                             zodiac/moon/rokuyo, calendar-day cycles); widgets
                             read it, never recompute it independently.
               LOG-CORR      a personalization feature that reads useLogs(),
                             buckets by some ambient dimension, and returns
                             null below a minimum-sample threshold rather than
                             a low-confidence guess.
DOCTRINE:    rev N (unchanged number of clauses touched substantively; one
             clause ADDED). New clause below.
COMPRESSION: ~430 raw words (this INTAKE + 05 + 06 prose) -> retained as 2
             CANDIDATE tokens + 1 doctrine clause + 3 file paths for the next
             session that touches ambient/astrology code. Ratio not yet
             meaningful (first appearance) — will compute against future
             recurrence once AMBIENT-SRC / LOG-CORR either mint or drop.

New doctrine clause (see docs/benchmark/LOT-DOCTRINE.md):

## Ambient Data Single-Source

Any value that is the same for every user at a given moment (weather, the
Astrology block's zodiac/moon/rokuyo, calendar-day cycles) must be computed by
exactly one shared function. Widgets that need it call that function; they
must never re-derive the same formula locally, even approximately — two
independent day-of-year or rokuyo calculations will eventually disagree by one
day at a timezone or leap-year boundary, and the user sees two widgets
contradict each other in the same session. Personalization built on top of an
ambient value (e.g. correlating a user's own Logs against it) is a separate,
per-user layer that reads the shared ambient function's output — it never
forks the ambient calculation itself, and it must degrade to "no opinion"
(null / hidden) rather than a low-confidence guess when the user's own history
is too thin to support one.
(SR-20260720-01: getAstrologySnapshot() introduced as the shared source for
zodiac/moon/rokuyo, replacing independent recomputation in System.tsx and
QuantumSignWidget; correlateLogsWithRokuyo() introduced as the personalization
layer, gated at 2 qualifying Rokuyo buckets x 3+ check-ins each.)

--------------------------------------------------------------------------------
07 // PUSH
--------------------------------------------------------------------------------
COMMIT:      BENCHMARK: ENGINEERING — Astrology Logs-correlation + widget sync [VM]
HASH:        (recorded post-commit — see git log)
TAG:         benchmark-20260720-01
REMOTE:      origin/claude/practical-curie-zsl6wg   PUSHED

--------------------------------------------------------------------------------
08 // POST-PUSH VERIFICATION
--------------------------------------------------------------------------------
RESULT:      GREEN
NOTES:       This is the first benchmark run on this branch/environment where
             `benchmark-*` tags actually resolve locally — future sessions can
             use benchmark-20260720-01 as a real Plan-B rollback target. Next
             natural extension (not done here — out of scope for this pass):
             extend correlateLogsWithRokuyo-style personalization to the moon
             phase dimension once enough cross-cycle data exists, and consider
             surfacing the same correlation read-only on QuantumSignWidget's
             "patches" view so the two widgets agree on more than just the
             ambient value.

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260720-01
================================================================================
