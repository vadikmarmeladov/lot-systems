================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260704-01
TITLE:    LOT® Mail — Bring /email Candidate to Green, Wire Sync + Cohort
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  cc33b8b (no benchmark-* tag exists yet — see NOTES)
BRANCH:   claude/determined-turing-4ayxl4
DATE:     2026-07-04
TIME:     04:20 UTC
RESULT:   GREEN
================================================================================

--------------------------------------------------------------------------------
00 // PREFLIGHT
--------------------------------------------------------------------------------
REPO:        OK        DOCS WRITABLE: OK
REMOTE:      origin    REACHABLE
LAST GREEN:  NONE — `git tag --list 'benchmark-*'` returns empty locally AND on
             the remote. 28 GREEN benchmarks are recorded in LOT-LEDGER.md across
             prior sessions but no corresponding git tags exist. Plan B (guarded
             rollback) was unavailable this run. Flagged for next session — see
             NOTES.

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        S-2 mission: "Design, develop, and deploy the simplest LOT®
                 Email system in LOT® style. Compose a message in Log with
                 /email to Hitomi. New messages appear in Sync. Incorporate
                 with LOT Chat (Sync) and Cohort Dating through LOT Community."
CLASSIFICATION:  ENGINEERING
ACTION TAKEN:    This exact system was already designed and built on 2026-06-12
                 (branch claude/determined-turing-f6bw7r, commit 19f7906,
                 documented in LOT-SR-20260612-01.md) but was never folded into
                 master's ledger/lexicon and never shipped — it sat as a BEST
                 ship candidate in LOT-MANIFEST.md. This session cherry-picked
                 that implementation onto the current branch, resolved three
                 merge conflicts against ~3 weeks of master drift (Logs.tsx
                 gained /system /story /how triggers; Sync.tsx switched its
                 chat-message state from `messages` to an `sseMessages`-merge
                 pattern), green-gated the result, and records it properly in
                 the self-assembly ledger for the first time.
ROUTED TO:       docs/technical/ (no artifact beyond code + this report)

--------------------------------------------------------------------------------
02 // CHECK A        03 // BUILD        04 // CHECK B (gate)
--------------------------------------------------------------------------------
COMMAND                              A        B
-------                              --       --
tsc --project tsconfig.server.json   PASS     PASS
client:css:build                     PASS     PASS
client:js:build                      PASS     PASS
fix-esm-imports.js                   PASS     PASS
migration syntax check (node require) —      PASS
BUILD:      yarn build -> OK
GATE:       GREEN
FIX LOG:
  1. `node_modules/` was absent (fresh container). `npx tsc` fell back to a
     stray global TypeScript 6.0.2, which hard-errors on this repo's
     moduleResolution=node10 as deprecated (exit 2) — a false RED unrelated to
     any code change (confirmed identical failure on unmodified master via
     `git stash`). Fix: `npm install --legacy-peer-deps` (this repo requires
     the legacy flag — nanostores peer-dep range conflict). Resolved TypeScript
     to the pinned 5.9.3; both CHECK A and CHECK B green afterward. No
     tsconfig change made — the repo's config was never broken.
  2. Cherry-pick conflicts in logTriggers.ts, Logs.tsx (x2 hunks), Sync.tsx
     (state-shape only, JSX merged clean) — resolved as unions, not overwrites.
  3. Golden-path test against the mission's own literal example — typing
     "/email to Hitomi." — showed the trailing period would be captured into
     toName and sent to the server verbatim, where an exact-match recipient
     lookup would 404 on "Hitomi." vs the real "Hitomi". Fixed by stripping
     trailing sentence punctuation (`.,!?;:`) from the parsed name in
     Logs.tsx before it ever reaches compose state or the API call.

NOTES: Pre-existing non-blocking warnings, unchanged by this session:
  - tsconfig.server.json: moduleResolution=node10 deprecated (TS 7.0 future)
  - badges.ts:1827/3112: duplicate key "first_signal"
  - browserslist / baseline-browser-mapping data are stale (cosmetic)

--------------------------------------------------------------------------------
05 // FILES CHANGED
--------------------------------------------------------------------------------
PATH                                                             STATUS
migrations/20260612120000_add-lot-mail.cjs                       ADDED
  - lot_mails table: fromUserId, toUserId, subject, body, read, timestamps
  - indexes: (toUserId, read, createdAt), (fromUserId, createdAt)
src/server/models/lot-mail.ts                                    ADDED
  - Sequelize model, standard repo pattern (matches answer.ts, log.ts)
src/server/models/index.ts                                       MODIFIED
  - LotMail registered in models map + Models type
src/server/routes/api.ts                                         MODIFIED
  - POST /api/mail (recipient lookup by first/last name, iLike, self-send
    guard), GET /api/mail/inbox (50 most recent + sender join), GET
    /api/mail/unread-count, PUT /api/mail/:id/read
  - SSE switch: `lot_mail` case routes only to the intended toUserId
src/shared/types/index.ts                                        MODIFIED
  - LotMail, LotMailRecord, LotMailSyncPayload; SyncEvents.lot_mail
src/shared/constants/index.ts                                    MODIFIED
  - MAX_LOT_MAIL_LENGTH = 2000
src/client/queries.ts                                            MODIFIED
  - useLotMailInbox, useLotMailUnreadCount, useSendLotMail, useMarkMailRead
src/client/utils/logTriggers.ts                                  MODIFIED
  - 'email-compose' trigger: emojis ✉️ ✉ 📧, keywords /email /mail — merged
    alongside master's newer /system /story /how triggers (union, not override)
src/client/components/Logs.tsx                                   MODIFIED
  - /email to [Name] [body] trigger opens inline compose panel (To: prefilled,
    body prefilled if given); Send -> POST /api/mail; confirmation line;
    /system help text gained an `/email to X` entry
src/client/components/Sync.tsx                                   MODIFIED
  - Chat | Mail tab strip; Mail tab shows live unread badge, clears on open;
    lot_mail SSE event appended alongside existing chat_message /
    chat_message_like listeners (no listener replaced, all three coexist)
src/client/components/EmailInbox.tsx                             ADDED
  - Inbox list (newest first), unread at full opacity / read at 60%, click to
    expand + mark read, relative timestamps
src/client/components/ChatCatalystWidget.tsx                     MODIFIED
  - Cohort-suggestion card gains a secondary "✉ Mail" button beside the
    existing Connect action; pre-fills `/email to [Name] ` in Log and records
    a `connection_accepted` QIE signal (type: 'email') — this is the Cohort
    Dating -> LOT Community wiring the mission asked for

--------------------------------------------------------------------------------
06 // SELF-ASSEMBLY
--------------------------------------------------------------------------------
LEDGER:      appended
LEXICON:     1 minted (LOT MAIL — earned: named in 3+ prior reports before
             today: SR-20260605-01, SR-20260612-06, LOT-MANIFEST, LOT-WEEKLY-
             2026-W23 — but never itself entered the lexicon or shipped)
             rev unchanged (lexicon has no rev letter; reused PLANNER-CONTEXT
             pattern of REV column = A for first mint)
DOCTRINE:    1 clause folded (ENV-PREFLIGHT) rev M -> N
WORDS:       969 (median 396) — trend ↑ (conflict-resolution + fix-log detail;
             not a compression regression, a one-off wide diff)

--------------------------------------------------------------------------------
07 // PUSH
--------------------------------------------------------------------------------
COMMIT:      BENCHMARK: ENGINEERING — LOT Mail: /email in Log, Sync tab, Cohort wiring [VM]
HASH:        2ecc618c
TAG:         none this session — no benchmark-* tag lineage exists to extend;
             starting one requires S-2 decision (see NOTES), not made unilaterally
PUSH:        origin/claude/determined-turing-4ayxl4 PUSHED

--------------------------------------------------------------------------------
08 // POST-PUSH VERIFICATION
--------------------------------------------------------------------------------
RESULT:      GREEN
NOTES:       For the next session —
  1. No benchmark-* tags exist on origin despite 28 GREEN ledger entries
     referencing them. Either the tagging step was silently skipped across
     many sessions, or tags were pruned at some point. Recommend S-2 decide
     whether to retroactively anchor a tag at current master tip
     (`git tag benchmark-20260704-00 <master-hash> && git push --tags`) to
     re-establish the rollback lattice, per the bootstrap instructions.
  2. This session pushed to a feature branch (claude/determined-turing-4ayxl4),
     not master — the operating harness for this session restricts pushes to
     the assigned branch. LOT Mail is GREEN and ready; a future "Ship LOT Mail"
     pass (Ship Mode, S0-S10) should cherry-pick this branch's commit into
     master via staging, then mark LOT-MANIFEST.md's LOT Mail row SHIPPED.
  3. Any container that runs this repo cold must `npm install
     --legacy-peer-deps` before trusting `tsc` output — see FIX LOG item 1 and
     new DOCTRINE clause ENV-PREFLIGHT.

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260704-01
================================================================================
