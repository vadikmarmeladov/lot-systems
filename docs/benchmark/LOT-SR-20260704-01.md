================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260704-01
TITLE:    Calendar Time-Tracking + Delete Path + CAL-ALERT: Military Reminders
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  cc33b8b
BRANCH:   claude/dreamy-babbage-vkrun1
DATE:     2026-07-04
TIME:     00:10 UTC
RESULT:   GREEN
================================================================================

--------------------------------------------------------------------------------
00 // PREFLIGHT
--------------------------------------------------------------------------------
REPO:        OK        DOCS WRITABLE: OK
REMOTE:      origin    REACHABLE
LAST GREEN:  NONE (no benchmark-* tags exist in this repo yet — Plan B unavailable
             this run; not needed, gate held green throughout)

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        S-2 request: continue the Calendar widget independently — keep
                 the minimalist UI, make it reliable, track time, and log
                 military-grade event notifications into the Log stream.
CLASSIFICATION:  SELF-ASSEMBLY
ACTION TAKEN:    Added time-of-day to entries, a delete path, an hourly
                 background job that detects due/overdue entries, a new
                 military log renderer for it, and a reliability fix to the
                 save path (failed writes no longer discard typed input).
                 Green-gated and pushed to the ship branch.
ROUTED TO:       docs/assembly/

--------------------------------------------------------------------------------
02 // CHECK A        03 // BUILD        04 // CHECK B (gate)
--------------------------------------------------------------------------------
COMMAND                              A        B
-------                              --       --
tsc --project tsconfig.server.json   PASS     PASS
fix-esm-imports.js                   PASS     PASS
client:css:build                     PASS     PASS
client:js:build                      PASS     PASS
BUILD:                yarn build -> OK
GATE:                 GREEN
FIX LOG:               1. Environment had no node_modules — ran `npm ci
                          --legacy-peer-deps` (pre-existing peer conflict
                          between @nanostores/react@0.4.1 and
                          nanostores@0.9.5; the flag is npm's own suggested
                          fix, not a dependency change).
                       2. `dayjs.Dayjs` used as a type in scheduled-jobs.ts
                          before the local wrapper's named `Dayjs` type was
                          imported — caught before running tsc, fixed to
                          `import type { Dayjs } from '#server/utils/dayjs'`.

NOTES: Pre-existing non-blocking warnings, unrelated to this session:
  - badges.ts:1827/3112 duplicate key "first_signal" (carried in, not touched)
  - browserslist / baseline-browser-mapping data several months old (cosmetic)
  registry.yarnpkg.com is blocked by org egress policy in this environment —
  used npm (registry.npmjs.org, already allow-listed) instead of yarn install.

--------------------------------------------------------------------------------
05 // FILES CHANGED
--------------------------------------------------------------------------------
PATH                                                             STATUS
src/client/components/CalendarWidget.tsx                          MODIFIED
  - CalendarEntry: +id, +time (HH:mm | null)
  - Native time input beside the note/task/call text field
  - Sort key: date + time (was date-only)
  - Remove action per entry (day view) -> useDeleteLog()
  - Upcoming list: shows time; tags "overdue" / "due soon" inline
  - handleAddEntry: form only clears onSuccess; onError keeps typed
    text/time and shows an inline message (was: cleared immediately,
    silently losing input on failure)

src/client/queries.ts                                             MODIFIED
  - +useDeleteLog(): DELETE /api/logs/:id

src/server/routes/api.ts                                          MODIFIED
  - +DELETE /logs/:id — 404 unless log.userId matches caller AND
    log.event === 'calendar_entry'
  - +calendar_reminder added to displayableEvents (was write-only,
    invisible to GET /logs without this)

src/server/scheduled-jobs.ts                                      MODIFIED
  - +Job 25: hourly-calendar-reminder-check. Derives due_soon (<=60min) /
    due_now / due_today (all-day) / overdue from calendar_entry logs in a
    [today-2d, today+2d] window; writes calendar_reminder once per
    (sourceLogId, stage), deduped against the last 3 days.
  - Scheduler tick: removed the fixed-hour outer gate before
    checkAndRunScheduledJobs() — every job already gates on its own
    day/hour internally; the outer gate was silently skipping hours
    1, 2, 17, 19, 21 for any job that might need them. Job 25 does.

src/client/components/Logs.tsx                                    MODIFIED
  - +CAL-ALERT: handler for calendar_reminder (stage label + entry text
    + type/date/time, military data-row format)
  - CAL: handler (calendar_entry) now prints time when present

src/client/stores/intentionEngine.ts                               MODIFIED
  - recordCalendarSignal(entryType, date, time?) — time is new, optional,
    backward compatible; sole call site updated in the same commit

src/client/components/SystemProgressWidget.tsx                    MODIFIED
  - +v73 SESSION_REPORTS entry
  - USERSHIP_TRANSMISSION updated to v73

docs/assembly/2026-07-04_LOT-assembly_v73-calendar-time-reminders.md  ADDED
  - Full technical writeup: signal extracted, delta list, per-item build
    notes, file table, test results, deferred items

--------------------------------------------------------------------------------
06 // SELF-ASSEMBLY
--------------------------------------------------------------------------------
LEDGER:      appended (see docs/benchmark/LOT-LEDGER.md)
LEXICON:     3 reused (COCKPIT-RULE, MILITARY PURITY, GREEN-GATE)
             0 minted this run — CAL-ALERT: and Job 25 are concrete
             implementations, not yet-recurring concepts; mint on a 3rd
             appearance per the earn-don't-decree rule
DOCTRINE:    0 clauses folded this run (COCKPIT-RULE clause already covers
             the CAL-ALERT: renderer's data-row-only body; no new clause
             needed)  rev current
WORDS:       768 (median 396) — trend ↑ (this session touched 7 files across
             client+server+scheduler; length reflects breadth, not un-compressed
             prose — no clause was padded to hit a number)

--------------------------------------------------------------------------------
07 // PUSH
--------------------------------------------------------------------------------
COMMIT:      BENCHMARK: SELF-ASSEMBLY — Calendar time-tracking + delete path +
             CAL-ALERT: military reminders (Job 25) [VM]
HASH:        a53f28cd
TAG:         benchmark-20260704-01
PUSH:        origin/claude/dreamy-babbage-vkrun1 PUSHED

--------------------------------------------------------------------------------
08 // POST-PUSH VERIFICATION
--------------------------------------------------------------------------------
RESULT:      GREEN
NOTES:       No benchmark-* tags existed anywhere in this repo's history before
             this run — the rollback lattice described in the protocol was
             never actually anchored. This run's tag is the first one. Next
             session: confirm the tag survived the push and is visible via
             `git ls-remote --tags origin`, since this is now the sole Plan-B
             target until a second tag exists.
             Next feature recommendation: in-place edit for a calendar entry
             (date/time/text), scoped the same way as the new DELETE route —
             the id is already threaded end-to-end, only the route is missing.

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260704-01
================================================================================
