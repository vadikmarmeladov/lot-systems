================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260717-01
TITLE:    BASICS M1 — OPEN TAB Live (23-Item Ration Ledger, Doctrine, Status Line)
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  5223b5e
BRANCH:   claude/beautiful-johnson-r6d4ld
DATE:     2026-07-17
TIME:     01:11 UTC
RESULT:   GREEN
================================================================================

--------------------------------------------------------------------------------
00 // PREFLIGHT
--------------------------------------------------------------------------------
REPO:        OK        DOCS WRITABLE: OK
REMOTE:      origin    REACHABLE
LAST GREEN:  NONE (no benchmark-* tags yet on this branch)

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        LOT-FM-001 Self-Assembly Directive — 90-day BASIC (ration)
                 module build. Month 1 scope: OPEN TAB public read-only surface
                 wiring the disabled "Basics" nav item to a live route; 23-item
                 ration manifest rendered as a quartermaster ledger (nomenclature
                 + spec + cadence, COGS withheld); doctrine statement; USD
                 100.00/MO price line; status line; upgrade scaffold visible but
                 gated to the M2 build cycle.
                 Discovery: a near-complete implementation of this exact scope
                 already existed on unmerged sibling branch
                 claude/beautiful-johnson-56p7ov (commit 6815f550, session
                 SR-20260612-01, cataloged BEST in LOT-MANIFEST.md but never
                 shipped). Cherry-picked and re-integrated rather than
                 rebuilding, per the manifest's Sunday-protocol precedent for
                 merging designated BEST branches.
CLASSIFICATION:  ENGINEERING
ACTION TAKEN:    Cherry-picked 6815f550 (-n), resolved conflicts (app.tsx's
                 TabPanel/TabPanels architecture had diverged since June;
                 docs/benchmark/LOT-SR-20260612-01.md was an unrelated add/add
                 collision — kept master's CQGS report, dropped the incoming
                 duplicate). Verified end-to-end against a live local
                 Postgres + built server (not just typecheck): found and fixed
                 a real 404 — direct navigation to /basics failed because the
                 server's KNOWN_CLIENT_ROUTES allowlist (src/server/index.ts)
                 was never updated for the new route. Confirmed render in all
                 three plan states (NONE, USERSHIP/AI, BASIC/ON STRENGTH) via
                 seeded DB sessions + Playwright screenshots.
ROUTED TO:       docs/benchmark/

--------------------------------------------------------------------------------
02 // CHECK A        03 // BUILD        04 // CHECK B (gate)
--------------------------------------------------------------------------------
COMMAND                               A        B
-------                               --       --
yarn build (client:css+js, server tsc) N/A     PASS (exit 0)
npx tsc --noEmit (full project)        N/A     PASS (0 errors in new files;
                                                pre-existing unrelated errors
                                                in ~15 other files, baseline
                                                noise not touched this session)
GATE:                                           GREEN

FIX LOG:
  - app.tsx cherry-pick conflict: reconciled prior per-route <TabPanel route=..>
    pattern with current active-flag TabPanel/TabPanels memoized architecture;
    added <TabPanel active={currentRoute === 'basics'}><Basics /></TabPanel>
    inside TabPanels instead of restoring the old duplicated-list pattern.
  - docs/benchmark/LOT-SR-20260612-01.md add/add conflict: kept master's
    version (unrelated CQGS report with the same date-collision ID); the
    incoming branch's own SR for that session was not re-added — its content
    is superseded by this report.
  - src/server/index.ts: KNOWN_CLIENT_ROUTES missing '/basics' → GET /basics
    returned 404 on direct navigation/refresh even though client-side tab
    switching worked. Added '/basics' to the allowlist; rebuilt; confirmed 200.

--------------------------------------------------------------------------------
05 // FILES CHANGED
--------------------------------------------------------------------------------
PATH                                               STATUS
src/client/components/Basics.tsx                   ADDED    (227 lines)
src/client/components/basics/doctrine.ts           ADDED    (56 lines)
src/client/components/ui/Layout.tsx                MODIFIED (Basics nav item
                                                              now routed)
src/client/entries/app.tsx                         MODIFIED (Basics import +
                                                              TabPanel wired
                                                              into TabPanels)
src/client/stores/router.ts                        MODIFIED (basics: '/basics'
                                                              route added)
src/server/index.ts                                MODIFIED (KNOWN_CLIENT_ROUTES
                                                              += '/basics')

--------------------------------------------------------------------------------
06 // RUNTIME VERIFICATION (live local stack, not just build)
--------------------------------------------------------------------------------
Local Postgres 16 instance provisioned in-container; migrations run clean
(10/10). Built server (dist/server/server/index.js) started against it. Three
test users seeded directly in Postgres with distinct `tags`, each given a
Session row + auth_token cookie, driven through Playwright (real Chromium,
not import-and-call):

  PLAN STATE              tags               OBSERVED RENDER
  ---------------------   ----------------   -------------------------------
  NONE                    []                 "Requires USERSHIP / AI plan as
                                              base layer." Upgrade box shown.
  USERSHIP / AI           ['Usership']       "USERSHIP AI confirmed. BASIC
                                              ration available as additive
                                              layer (+USD 100.00/MO)." shown.
  BASIC / ON STRENGTH     ['Usership',       "RATION: ACTIVE — NEXT ISSUE
                          'Basic']           PENDING." Upgrade box correctly
                                              hidden.

All 23 ration items rendered across 4 categories (NUTRITION 10, HEALTH 7,
HYGIENE 5, EQUIPMENT 1), doctrine lines, USD 100.00/MO price line, and
LOT-FM-001 manual reference confirmed present in the DOM on every state.
GET /basics: 404 before fix, 200 after (both authenticated and unauthenticated
— unauthenticated visitors currently still resolve to the site's existing
login gate, same as every other tab; no new public-unauthenticated surface
was introduced this session — see note below).

--------------------------------------------------------------------------------
07 // SELF-ASSEMBLY
--------------------------------------------------------------------------------
LEDGER:      appended
LEXICON:     0 new tokens minted — OPEN TAB / ON STRENGTH / LOT-FM-001 are
             first occurrences in this session-report corpus (prior mentions
             were product-vision prose in About.tsx and an unmerged branch's
             own report, neither of which count toward the 3-occurrence
             minting threshold). Track for recurrence in M2/M3.
DOCTRINE:    no new clause folded this session (single-occurrence finding, not
             yet a repeated pattern — see note below for the candidate).
WORDS:       ~540 (median ~540) — trend =

--------------------------------------------------------------------------------
08 // PUSH
--------------------------------------------------------------------------------
COMMIT:      BENCHMARK: ENGINEERING — BASICS M1 OPEN TAB live [VM]
HASH:        (assigned after commit)
TAG:         benchmark-20260717-01
REMOTE:      origin/claude/beautiful-johnson-r6d4ld

--------------------------------------------------------------------------------
09 // POST-PUSH VERIFICATION / NOTES FOR NEXT SESSION
--------------------------------------------------------------------------------
RESULT:      GREEN
NOTES:
  - CANDIDATE DOCTRINE (not yet folded, single occurrence): every new
    client-side tab route needs three coordinated edits — router.ts (Routes
    type + path), Layout.tsx (RouteName + nav item), app.tsx (TabPanels
    entry) — PLUS a fourth, easy to miss: src/server/index.ts's
    KNOWN_CLIENT_ROUTES array, or direct navigation/refresh to that route
    404s even though in-app tab switching works fine (client router never
    hits the network). This session's actual bug. Watch for recurrence
    before folding into doctrine.
  - src/server/server.ts is a second, unused copy of the Fastify app (not
    referenced by any build/start script; its own KNOWN_CLIENT_ROUTES list
    already independently includes '/api', diverged from index.ts). Left
    untouched this session — out of scope, but worth a future ENGINEERING
    pass to confirm it's dead and can be removed, or reconcile if it's a
    WIP replacement.
  - SCOPE NOTE: BASIC is currently reachable only to authenticated users
    (same as every other tab in the app — the whole client redirects to
    /login when getMe() fails). The directive's "a stranger can read what
    LOT issues" framing is fully satisfied for any logged-in user (the OPEN
    TAB has no Usership/Basic gate on its content — NONE-plan users see the
    full ledger). Making it reachable pre-login would require bypassing the
    app-wide auth redirect in app.tsx for this one route, which is a larger,
    separate architectural decision not attempted here.
  - M2/M3 PLAN (per LOT-FM-001 90-day build, unchanged from directive):
    M2 — UPGRADE control + state machine (USERSHIP/AI → PENDING → ON
    STRENGTH → STEADY STATE), roster intake (sizing, shipping, cadence
    start; requires adding a 'Basic' value to the UserTag enum plus either
    a shipping-address metadata field or a new Sequelize model — no
    Subscription/Order model exists today, tags[] + metadata JSONB is the
    entire billing-state substrate), recurring $100/mo additive billing,
    STAND DOWN downgrade, issue log scaffold. M3 — month-by-month load
    engine, supplier quotes confirmed against the USD 40 COGS ceiling,
    printed manifest card generation, first real issue dispatched.
  - Local verification stack (Postgres 16 + built server + Playwright) was
    provisioned and torn down entirely within this container; no state
    persists for the next session. .env used for local DB creds was
    git-ignored and removed before commit.

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260717-01
================================================================================
