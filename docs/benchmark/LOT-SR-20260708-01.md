================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260708-01
TITLE:    BASICS MODULE — LOT-FM-001 M1+M2+M3 (OPEN TAB, roster, issue engine)
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  5223b5e2
BRANCH:   claude/beautiful-johnson-2sinrr
DATE:     2026-07-08
TIME:     01:20 UTC
RESULT:   GREEN
================================================================================

--------------------------------------------------------------------------------
00 // PREFLIGHT
--------------------------------------------------------------------------------
REPO:        OK        DOCS WRITABLE: OK
REMOTE:      origin    REACHABLE
LAST GREEN:  NONE (no prior benchmark-* tag in this repo — first anchor this run)

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        LOT-FM-001 self-assembly directive — BASIC (RATION) 90-day build.
                  Full doctrine, 23-item civilian ration load, visual spec (LiberationMono-
                  Bold, white/black inversion, 2px rules, square corners, IBM 3270 register),
                  OPEN TAB public surface, UPGRADE state machine (Usership AI -> BASIC).
CLASSIFICATION:   ENGINEERING (feature build against a supplied product spec)
ACTION TAKEN:     Researched existing app architecture (routing, auth, tag model, billing
                  surface) before writing code. Found a prior M1-only attempt on stale
                  branch claude/beautiful-johnson-56p7ov (manifest: BEST, 8/8, never
                  merged — no common ancestor with current master). Did not cherry-pick
                  (disjoint history); re-derived the same M1 design against current
                  app.tsx/Layout.tsx/router.ts conventions, then built M2 + M3 fresh.
ROUTED TO:        docs/benchmark/ (this report); source under src/client, src/server,
                  src/shared (no dedicated docs/technical/basics note needed — the code
                  and this report are the spec of record).

--------------------------------------------------------------------------------
02 // CHECK A        03 // BUILD        04 // CHECK B (gate)
--------------------------------------------------------------------------------
COMMAND                                    A        B
-------                                    --       --
npx tsc --noEmit -p tsconfig.server.json   PASS     PASS
npm run build (client+server)              PASS     PASS
Live smoke test (local postgres 16,        PASS     PASS
  real HTTP session, full state-machine
  cycle over curl — see §04a)
BUILD:                npm run build -> OK
GATE:                  GREEN
FIX LOG:
  - COGS estimate recalibrated: initial retail-price guesses summed to
    ~$66/issue (breach); replaced with wholesale/bulk landed-cost estimates
    summing to $32.25 worst case (all 23 lines) / $27.55 monthly-only —
    both clear the $40 ceiling with margin to spare. Marked ESTIMATE.
  - Fixed a real repo-wide bug found in .gitignore: line 50 `server/` (meant
    to exclude compiled dist/server output, already covered by line 2
    `dist/`) had no path anchor and was silently swallowing any NEW file
    under src/server/ from git tracking. My two new server files vanished
    from `git status` until this was found and removed. Any other agent
    adding new src/server/* files before this fix would have silently lost
    that work at commit time.
  - Extended two exhaustive `Record<LogEvent, string>` maps (memory.ts,
    story-generator.ts) after adding 4 new LogEvent literals — tsc caught
    both, one-line fix each.

--------------------------------------------------------------------------------
04a // LIVE VERIFICATION (local, not part of CI — see /verify)
--------------------------------------------------------------------------------
Spun up local Postgres 16 + a throwaway DB, ran migrations, booted the real
compiled server (dist/server/server/index.js), and drove the full flow over
HTTP with curl (session cookie from real /auth/email/code exchange, OTP read
directly from the email_codes table since no RESEND_API_KEY is configured):

  GET  /basics                (no auth)         -> 200, serves basics.js (OPEN TAB)
  GET  /api/basics/state       (no auth)         -> 401 (auth gate confirmed)
  POST /api/basics/enroll      (no usership tag) -> 403 "requires USERSHIP / AI"
  POST /api/basics/enroll      (usership tag)    -> 200, status PENDING
  POST /api/basics/confirm                       -> 200, status ON_STRENGTH,
                                                     cadence + billing scaffold set
  POST /api/basics/dispatch-issue  x3             -> cycle 0 (23 lines, $32.25,
                                                     67.8% margin), cycle 1 (20
                                                     lines, $27.55, 72.5%), cycle 2
                                                     (same) -> status flips to
                                                     STEADY_STATE at cycle 3
  POST /api/basics/stand-down                    -> status NONE, billing STOPPED,
                                                     `usership` tag verified retained
  POST /api/basics/dispatch-issue (post stand-down) -> 409 (blocked correctly)
  logs table                                     -> 6 audit rows, one per
                                                     transition, correct text/event

Torn down after verification: server killed, test DB + role dropped, local
.env removed (not committed — .gitignore already excludes .env).

--------------------------------------------------------------------------------
05 // FILES CHANGED
--------------------------------------------------------------------------------
PATH                                                    STATUS
.gitignore                                              MODIFIED (removed unsafe `server/` rule)
scripts/build/client.build.ts                           MODIFIED (added basics.tsx entrypoint)
src/client/components/Basics.tsx                        ADDED
src/client/components/ui/Layout.tsx                     MODIFIED (nav: Basics -> route)
src/client/entries/app.tsx                              MODIFIED (Basics tab panel wired)
src/client/entries/basics.tsx                            ADDED (standalone OPEN TAB entry)
src/client/queries.ts                                    MODIFIED (basics query/mutation hooks)
src/client/stores/router.ts                              MODIFIED (basics route)
src/server/basics/cogs.ts                                ADDED (server-only COGS ceiling gate)
src/server/index.ts                                      MODIFIED (public /basics route)
src/server/models/user.ts                                MODIFIED (basics in profile view)
src/server/routes/api.ts                                 MODIFIED (registered basics routes)
src/server/routes/basics-api.ts                          ADDED (enroll/confirm/stand-down/dispatch)
src/server/utils/memory.ts                               MODIFIED (LogEvent map extended)
src/server/utils/memory/story-generator.ts               MODIFIED (LogEvent map extended)
src/shared/constants/basics.ts                           ADDED (23-item manifest + load engine)
src/shared/types/index.ts                                MODIFIED (BasicsState + LogEvent types)

--------------------------------------------------------------------------------
06 // SELF-ASSEMBLY
--------------------------------------------------------------------------------
LEDGER:      appended
LEXICON:     0 minted (first BASICS session on a live branch — mint deferred
             per doctrine's 3-occurrence rule, not decreed early)
DOCTRINE:    0 folded (too early — one data point)
WORDS:       886 (median ~540) — trend ↑ (deep first-session build, live
             verification section included; expect contraction on iteration
             sessions)

--------------------------------------------------------------------------------
07 // PUSH
--------------------------------------------------------------------------------
COMMIT:      BENCHMARK: ENGINEERING — LOT-FM-001 BASICS M1+M2+M3 (OPEN TAB, roster
             state machine, issue engine) [VM]
HASH:        (see push output)
TAG:         benchmark-20260708-01
PUSH:        origin/claude/beautiful-johnson-2sinrr PUSHED

--------------------------------------------------------------------------------
08 // POST-PUSH VERIFICATION
--------------------------------------------------------------------------------
RESULT:      GREEN
NOTES:       Month 1-3 scope built in one session rather than across a literal
             calendar quarter (see companion report LOT-BASICS-90DAY-BUILD-
             REPORT.md for the month-by-month narrative the user asked for).
             Billing is a scaffold (metadata-tracked ledger, no live payment
             processor exists in this codebase — do not represent it as live
             billing to S-2 or to end users until a processor is wired in).
             "DISPATCH NEXT ISSUE" is a manual/self-serve trigger standing in
             for a real supplier/courier integration — flagged in-UI as such.
             Next session candidates: wire a real payment processor if/when
             one is chosen; move the manual dispatch trigger behind an
             admin-only confirmation step once real fulfillment exists;
             consider whether STAND DOWN should also cancel a pending
             next-cycle charge if a real processor lands.

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260708-01
================================================================================
