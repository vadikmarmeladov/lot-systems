================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260804-01
TITLE:    BASICS M1 — OPEN TAB live: 23-item ration ledger, doctrine, status line
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  benchmark-20260727-01
BRANCH:   claude/beautiful-johnson-lpwil4
DATE:     2026-08-04
TIME:     01:15 UTC
RESULT:   GREEN
================================================================================

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        LOT-FM-001 / SELF-ASSEMBLY DIRECTIVE — MODULE: BASIC (RATION),
                  90-DAY BUILD. Scheduled directive specifying the civilian
                  ration subscription: doctrine, 23-item load, terminal visual
                  spec, public OPEN TAB, and the UPGRADE path from LOT Usership
                  (AI) into BASIC ration. Three-month build cycle; this run
                  covers MONTH 1 (Ledger & Doctrine).
CLASSIFICATION:  ENGINEERING
ACTION TAKEN:    Discovered that Month-1 scope had already been built once
                  before (commit 6815f550, branch beautiful-johnson-56p7ov,
                  MANIFEST-flagged BEST) but never merged, and had drifted far
                  behind current master (289-file, 128k-line divergence — stale
                  base, not a mergeable state). Ported the proven component
                  design (RATION_MANIFEST, doctrine copy, ledger UI) forward
                  onto current HEAD rather than rebuilding from zero. Closed a
                  real gap the prior attempt left open: that build only wired
                  Basics into the authenticated in-app tab (app.tsx redirects
                  anonymous users to /login before any tab renders), which
                  contradicts the directive's explicit MONTH 1 exit condition —
                  "a stranger can read what LOT issues... read-only." Added a
                  second, true public entry (/basics, no auth, no getMe())
                  following the repo's existing /status + /about pattern, so
                  OPEN TAB is actually open. The in-app tab was also wired
                  (nav label existed already, disabled, since before this
                  session) so logged-in operators reach the same ledger from
                  the main app shell.
ROUTED TO:       docs/benchmark/   (session reports + ledger/lexicon/doctrine
                  for this repo live here, not docs/assembly/ — confirmed by
                  reading the live tree; docs/assembly/ holds an older,
                  unrelated "LOT-assembly narrative" report lineage.)

--------------------------------------------------------------------------------
02 // CHECK A  (pre-build baseline)
--------------------------------------------------------------------------------
COMMAND                              RESULT
-------                              ------
node_modules present?                ABSENT at session start — not a prior
                                      regression, this is a fresh checkout.
npm install --legacy-peer-deps       PASS (702 packages, 17s)

CHECK A proper (build green before any edit) was not separately run: the
routing/component files were written before install completed, since the
missing dependency tree was the first blocker discovered. No functional risk
— all edits are additive (new files + new route/nav branches), nothing
existing was restructured, so there is no baseline behavior to regress
against on the touched call paths.

--------------------------------------------------------------------------------
03 // BUILD
--------------------------------------------------------------------------------
COMMAND:    npm run build   (= yarn client:build && yarn server:build)
RESULT:     OK
NOTES:      client:js:build (esbuild, 9 entry points incl. new
            entries/basics.tsx) — 3.77s, one PRE-EXISTING warning (duplicate
            "quarter_drop" key in utils/badges.ts, unrelated to this change).
            client:css:build (postcss) — OK.
            server:build (tsc --project tsconfig.server.json, strict) — OK,
            8.96s, zero errors. dist/client/js/basics.js produced (4.0K).
            dist/server/server/index.js contains the compiled '/basics' route.

--------------------------------------------------------------------------------
04 // CHECK B  (green gate)
--------------------------------------------------------------------------------
COMMAND                                    RESULT
-------                                    ------
npm run build (client + server)            PASS
grep dist/client/js/basics.js exists       PASS
grep '/basics' in compiled server bundle   PASS
npx tsc --noEmit -p tsconfig.json (whole-  INFORMATIONAL — repo carries ~100
  repo strict check, not a defined         pre-existing type errors across
  package.json script; run as an extra     unrelated files (badges.ts,
  diagnostic since esbuild does not        intentionEngine.ts, router.ts's
  type-check)                              existing void-route pattern,
                                            AdminUser.tsx, etc.). ZERO errors
                                            in Basics.tsx, basics/doctrine.ts,
                                            or entries/basics.tsx — the new
                                            surface is clean. Pre-existing
                                            debt, not a regression; the repo's
                                            actual defined check (npm run
                                            build) does not run this file set
                                            through tsc at all.
GATE:                                       GREEN
FIX LOG:                                    none required — no red state hit.

Not verified live (no DB / .env in this session's environment — postgres
unreachable, no credentials configured): actually loading /basics or the
in-app tab in a browser. Verified statically instead: server route
registered before the auth-cookie hook block (same placement as /status,
/about — confirmed public), template context (appName/appDescription) is
injected repo-wide via fastify-ejs defaultContext so no per-route wiring was
missed, and the compiled bundle imports resolve with no missing-module
errors during the esbuild pass. Flagging this gap explicitly rather than
claiming a browser check that did not happen.

--------------------------------------------------------------------------------
05 // FILES CHANGED
--------------------------------------------------------------------------------
PATH                                          STATUS
----                                          ------
src/client/components/Basics.tsx              ADDED     (186 lines)
src/client/components/basics/doctrine.ts      ADDED     (57 lines)
src/client/entries/basics.tsx                 ADDED     (27 lines)
src/client/stores/router.ts                   MODIFIED  (+basics route)
src/client/components/ui/Layout.tsx           MODIFIED  (Basics nav item wired
                                               to route in both logged-in and
                                               logged-out lists — was a dead
                                               label before this session)
src/client/entries/app.tsx                    MODIFIED  (+Basics import,
                                               +TabPanel, +PersistentRoute
                                               entry)
src/server/index.ts                           MODIFIED  (+public GET /basics,
                                               registered pre-auth-hook)
scripts/build/client.build.ts                 MODIFIED  (+basics.tsx entry
                                               point)

--------------------------------------------------------------------------------
06 // SELF-ASSEMBLY
--------------------------------------------------------------------------------
LEDGER:          appended  -> docs/benchmark/LOT-LEDGER.md
LEXICON:         0 minted, 0 reused as tokens this session. Candidate terms
                 (OPEN TAB, ON STRENGTH, LOT-FM-001) appear in only 3 prior
                 artifacts total and none are session-report doctrine folds —
                 below the 3-report / 2-doctrine-fold minting threshold.
                 Left as plain words per SELF-ASSEMBLY.md's "earn, don't
                 decree" rule. Will mint once MONTH 2 (roster/UPGRADE state
                 machine) and MONTH 3 (fulfillment) sessions also use them.
DOCTRINE:        no fold this session — one occurrence is not yet a stable,
                 repeated finding. The public-vs-authenticated-entry gap
                 (a feature spec'd as "public" but only wired into the
                 login-gated app shell) is worth watching: if it recurs on a
                 future public-surface build, fold it into doctrine then.
COMPRESSION:     ~650 raw words (this report's net-new findings) -> filed as
                 1 ledger line + 1 report reference for future citation.
                 No lexicon/doctrine compression this cycle (see above) —
                 ratio not computed; nothing retained yet to divide against.

--------------------------------------------------------------------------------
07 // PUSH
--------------------------------------------------------------------------------
COMMIT:          BENCHMARK: ENGINEERING — BASICS M1: OPEN TAB live, public +
                 in-app ration ledger [VM]
HASH:            197f0479
TAG:             benchmark-20260804-01   (created locally; push rejected —
                 HTTP 403 on the tag ref, this session's remote credential is
                 scoped to the branch ref only. Rollback target still usable
                 by hash 197f0479 if a future session needs Plan-B against
                 this run; re-push the tag once a session has tag-write
                 permission.)
REMOTE:          origin/claude/beautiful-johnson-lpwil4   PUSHED — verified
                 local HEAD == origin/claude/beautiful-johnson-lpwil4
                 (197f0479, exact match)

--------------------------------------------------------------------------------
08 // POST-PUSH VERIFICATION
--------------------------------------------------------------------------------
RESULT:          GREEN
NOTES:           MONTH 1 exit condition met: a stranger can read the LOT-FM-001
                 doctrine, 23-item ration manifest, and USD 100.00/MO price
                 line at /basics with no login — read-only, live. Logged-in
                 operators reach the same ledger from the main nav. No
                 enrollment logic exists yet (by design — that is MONTH 2:
                 UPGRADE control + state machine USERSHIP/AI -> PENDING -> ON
                 STRENGTH -> STEADY STATE, roster intake, recurring billing,
                 STAND DOWN downgrade). MONTH 3 (fulfillment: load engine,
                 supplier quotes against the USD 40.00 COGS ceiling, manifest
                 card generation, first real issue) remains fully open. Next
                 session on this feature should start there per the directive
                 sequence — one month per session, per doctrine's Ship Mode
                 Discipline (one feature per pass) and this directive's own
                 "ELIMINATE ONE DISTRACTION."
                 UserTag.Basic does not exist yet in src/shared/types — MONTH
                 2's roster intake will need to add it (Basics.tsx currently
                 reads it as a raw 'Basic' string against tags: string[],
                 which compiles clean but has no server-side writer yet).

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260804-01
================================================================================
