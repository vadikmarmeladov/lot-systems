================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260802-01
TITLE:    BASICS TAB — LOT-FM-001 Month 1 (OPEN TAB, public) + Month 2 (UPGRADE/ROSTER)
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  benchmark-20260727-01
BRANCH:   claude/beautiful-johnson-ahuh1t
DATE:     2026-08-02
TIME:     01:10 UTC
RESULT:   GREEN
================================================================================

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        LOT-FM-001 / SELF-ASSEMBLY DIRECTIVE — 90-day BASIC (ration)
                 module build order, issued as a scheduled automated session
                 (no live S-2 present). Doctrine + 23-item ration load + terminal
                 grid visual spec + OPEN TAB public surface + UPGRADE state
                 machine, ordered across 3 monthly increments.
CLASSIFICATION:  ENGINEERING
ACTION TAKEN:    Intake of prior BEST candidate (beautiful-johnson-56p7ov, per
                 LOT-MANIFEST.md §01, "Basics Tab ... 8/8 ... BEST") found the
                 branch stale (cut from an old master, ~289 files of unrelated
                 drift) and, on inspection, non-compliant with its own Month 1
                 exit criterion: its Basics.tsx only rendered inside the
                 authenticated app.tsx SPA, which redirects anonymous visitors
                 to /login before any tab renders — so "a stranger can read
                 it" did not actually hold. Rebuilt clean on top of current
                 master: (a) ported the 23-item ration manifest + doctrine
                 constants verbatim from the BEST branch, (b) built a genuine
                 public, unauthenticated OPEN TAB at /open-tab (new standalone
                 entry bundle, same pattern as the existing /status and /about
                 public routes — no #client/stores, no auth dependency), (c)
                 wired the authenticated in-app "Basics" tab into the current
                 (non-stale) Layout/router/app.tsx, and (d) built Month 2's
                 UPGRADE / ROSTER state machine end to end: USERSHIP/AI ->
                 PENDING -> ON STRENGTH, STAND DOWN downgrade, roster intake
                 (operators, cadence day), issue log, new UserTag.Basic, two
                 API routes (POST /api/basics/enroll, POST /api/basics/stand-down).
ROUTED TO:       docs/benchmark/   (existing, active convention for this repo —
                 see note below)

NOTE ON ROUTING CONVENTION: the skill's reference defaults point session
reports and the ledger/lexicon/doctrine trio at docs/assembly/. This repo's
live state has moved that trio to docs/benchmark/ (confirmed: LOT-MANIFEST.md,
LOT-LEDGER.md, LOT-LEXICON.md, LOT-DOCTRINE.md, and all recent LOT-SR-*.md
files live there; docs/assembly/ retains only a legacy LOT-LEDGER.md plus
per-session assembly logs). Followed the live convention per "discover, don't
assume" — filed everything in docs/benchmark/.

NOTE ON BRANCH SPRAWL: the repo carries ~35 "beautiful-johnson-*" branches,
apparently from repeated unmerged prior runs of this same scheduled task, each
starting fresh from master. This session did not attempt to reconcile them —
only the one BEST candidate named in the manifest was intaken. Flagging for
S-2: this family is a strong prune candidate once this session's branch ships.

--------------------------------------------------------------------------------
02 // CHECK A  (pre-build baseline)
--------------------------------------------------------------------------------
COMMAND                                          RESULT
-------                                          ------
node_modules bootstrap (npm ci --legacy-peer-deps) PASS (702 packages; repo's
                                                    package.json carries an
                                                    unresolved peer conflict
                                                    between nanostores@0.9.5
                                                    and @nanostores/react's
                                                    ^0.7.0 peer — pre-existing,
                                                    unrelated to this session)
npm run build (master HEAD, 8f6205e4, stashed)     PASS — GREEN baseline

--------------------------------------------------------------------------------
03 // BUILD
--------------------------------------------------------------------------------
COMMAND:    npm run build  (yarn client:build && yarn server:build)
RESULT:     OK
NOTES:      Added new standalone client entry `basics-open.tsx` to
            scripts/build/client.build.ts entryPoints (mirrors status.tsx /
            about.tsx / public-profile.tsx). Client bundle emitted
            dist/client/js/basics-open.js (3.7 KB). Server tsc emitted clean
            against the new /api/basics/* routes and the UserTag.Basic enum
            addition (USER_TAGS_BY_ID is a Record<UserTag,...> — compiler
            would have failed the build if the new tag were left unmapped;
            it did not, confirming the map was updated correctly).
            Pre-existing, unrelated warning: duplicate object key
            "quarter_drop" in src/client/utils/badges.ts (lines 3103 and
            5390) — not touched this session, not blocking, noting for a
            future ENGINEERING pass.

--------------------------------------------------------------------------------
04 // CHECK B  (green gate)
--------------------------------------------------------------------------------
COMMAND                                          RESULT
-------                                          ------
npm run build (client:build + server:build)        PASS
tsc --project tsconfig.server.json (via server:build) PASS (0 errors against
                                                    new/changed files; two
                                                    pre-existing tsconfig
                                                    deprecation notices
                                                    unrelated to this diff,
                                                    confirmed present on
                                                    unmodified master too)
npm run build (re-run after prettier-formatting     PASS
  revert on the 4 new files, to confirm no
  regression from the format fix)
GATE:                                              GREEN
FIX LOG:                                           1) `npm ci` alone failed on
                                                    an ERESOLVE peer conflict
                                                    (nanostores versions,
                                                    pre-existing in
                                                    package.json) — resolved
                                                    with --legacy-peer-deps,
                                                    no package.json edit.
                                                    2) `npx prettier --write`
                                                    on the 4 new files
                                                    reformatted them to
                                                    double-quote/semicolon
                                                    style (prettier's default,
                                                    since the repo carries no
                                                    .prettierrc) which does not
                                                    match the codebase's actual
                                                    single-quote/no-semicolon
                                                    convention seen throughout
                                                    every sibling file —
                                                    reverted the 4 files back
                                                    to match house convention,
                                                    rebuilt, still green.

--------------------------------------------------------------------------------
05 // FILES CHANGED
--------------------------------------------------------------------------------
PATH                                                        STATUS
----                                                        ------
src/client/components/basics/doctrine.ts                   ADDED
src/client/components/Basics.tsx                            ADDED
src/client/components/BasicsOpenTab.tsx                     ADDED
src/client/entries/basics-open.tsx                          ADDED
scripts/build/client.build.ts                                MODIFIED
src/client/components/ui/Layout.tsx                          MODIFIED
src/client/entries/app.tsx                                   MODIFIED
src/client/queries.ts                                        MODIFIED
src/client/stores/router.ts                                  MODIFIED
src/server/index.ts                                           MODIFIED
src/server/routes/api.ts                                      MODIFIED
src/shared/constants/index.ts                                 MODIFIED
src/shared/types/index.ts                                     MODIFIED
docs/benchmark/LOT-MANIFEST.md                                 MODIFIED
docs/benchmark/LOT-LEDGER.md                                    MODIFIED
docs/benchmark/LOT-DOCTRINE.md                                  MODIFIED

--------------------------------------------------------------------------------
06 // MONTHLY ENVELOPE STATUS (LOT-FM-001)
--------------------------------------------------------------------------------
MONTH 1 — LEDGER & DOCTRINE:  DELIVERED THIS SESSION.
  OPEN TAB live at /open-tab — no auth, static 23-item manifest + doctrine +
  price line, matches the terminal-grid layout of the existing public /status
  page. A stranger can load it and read the full ledger and terms. This
  corrects the prior BEST candidate, which never actually shipped a public
  surface (see 01 // INTAKE).

MONTH 2 — UPGRADE & ROSTER:  DELIVERED THIS SESSION (pulled forward — the
  prior candidate had only a disabled placeholder for this).
  State machine: USERSHIP/AI -> PENDING -> ON STRENGTH is real and wired:
  POST /api/basics/enroll requires the Usership tag + a ship-to address on
  file, captures roster intake (operator count 1-8, cadence day 1-28),
  writes both the PENDING and ON STRENGTH transitions to a per-user issue
  log, and adds UserTag.Basic. POST /api/basics/stand-down reverses it,
  drops UserTag.Basic, keeps Usership untouched, logs the transition. The
  in-app Basics tab surfaces PLAN / RATION / OPERATORS / CADENCE / ISSUE LOG
  and the live enroll/stand-down controls.
  Honest gap, recorded not hidden: PENDING resolves in the same request as
  ON STRENGTH because no billing gate exists anywhere in this codebase yet
  (grep confirms zero Stripe API calls despite a dormant stripeCustomerId
  column — USERSHIP itself is provisioned the same tag-based, externally-
  billed way). STEADY STATE is defined in the type system
  (BasicsState) but nothing transitions a roster into it yet — that requires
  a real first-issue ship event, which is Month 3 scope. Recurring $100/mo
  "additive billing" is UI/doctrine-level only this session (PRICE_LINE
  displayed, no charge is actually made) — wiring a real charge needs a
  payment processor decision from S-2 before it can be built honestly.

MONTH 3 — ISSUE & FULFILLMENT:  NOT STARTED.
  Load engine, supplier quotes vs. COGS ceiling, printed manifest card,
  first issue dispatch, STEADY STATE transition — deferred to the next
  scheduled session per the directive's "ship working increments monthly"
  instruction. Flagging to S-2: real fulfillment requires an actual supplier
  relationship and a shipping/billing decision this agent cannot make alone.

KNOWN GAP AGAINST HOUSE STYLE: LOT-FM-001 specifies LiberationMono-Bold. No
such font asset or webfont link exists anywhere in this repo (verified: zero
"LiberationMono" references in any CSS/JS/config). Used the existing
Tailwind `font-mono` (system monospace stack) as an honest interim
substitute rather than adding a new font dependency unasked — flagging for
S-2 to confirm whether to source and ship the actual typeface.

--------------------------------------------------------------------------------
07 // SELF-ASSEMBLY
--------------------------------------------------------------------------------
LEDGER:          appended  -> docs/benchmark/LOT-LEDGER.md
LEXICON:         0 tokens minted this session. LOT-FM-001 / BASIC / ON
                 STRENGTH / OPEN TAB now have 2 corpus references each
                 (this report + one prior SR + one corporate doc) — below the
                 3-report minting threshold. Held as prose per "earn, don't
                 decree"; likely to mint on the Month 3 session once
                 fulfillment work adds a third recurrence.
DOCTRINE:        1 new section folded — "BASIC Ration Module (LOT-FM-001)":
                 records the OPEN TAB / authenticated-tab split pattern (a
                 public route needs its own standalone entry + server route,
                 mirroring /status; it cannot be a tab inside app.tsx, which
                 is entirely auth-gated) and the "state transitions get
                 logged even when they resolve instantly because no real
                 gate exists yet" honesty rule, so future sessions don't
                 silently collapse PENDING into ON STRENGTH without a trace.
COMPRESSION:     ~640 raw words (this session's net-new findings, full prose)
                 -> ~85 words once expressed via the folded doctrine section
                 + existing GREEN-GATE / USERSHIP / OPERATOR lexicon tokens
                 (ratio ~7.5:1)

--------------------------------------------------------------------------------
08 // PUSH
--------------------------------------------------------------------------------

COMMIT:          BENCHMARK: ENGINEERING — BASICS tab: LOT-FM-001 Month 1 (public OPEN TAB) + Month 2 (UPGRADE/ROSTER state machine) [VM]
HASH:            (recorded post-commit, see git log)
TAG:             benchmark-20260802-01   (next Plan-B rollback target)
REMOTE:          origin/claude/beautiful-johnson-ahuh1t   PUSHED

--------------------------------------------------------------------------------
09 // POST-PUSH VERIFICATION
--------------------------------------------------------------------------------
RESULT:          GREEN
NOTES:           Next session (Month 3 — ISSUE & FULFILLMENT) needs an S-2
                 decision on: (1) real supplier/fulfillment partner for the
                 23-item load, (2) payment processor for the recurring
                 $100/mo charge (currently doctrine/UI only, no billing gate
                 exists in this codebase for any plan, Usership included),
                 (3) whether to source the LiberationMono-Bold typeface.
                 Also flagging the ~35-branch "beautiful-johnson-*" sprawl
                 for a future prune pass once this branch is merged.

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260802-01
================================================================================
