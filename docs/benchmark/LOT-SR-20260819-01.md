================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260819-01
TITLE:    BASICS M2 — UPGRADE + ROSTER state machine; M1 landed on master baseline
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  98971f20
BRANCH:   claude/beautiful-johnson-k91wjf
DATE:     2026-08-19
TIME:     01:10 UTC
RESULT:   GREEN
================================================================================

--------------------------------------------------------------------------------
00 // PREFLIGHT
--------------------------------------------------------------------------------
REPO:        OK        DOCS WRITABLE: OK
REMOTE:      origin    REACHABLE
LAST GREEN:  benchmark-20260805-01
ENV NOTE:    node_modules absent at session start (fresh container). `npm ci`
             failed on an unrelated pre-existing peer-dep conflict
             (@nanostores/react wants nanostores ^0.7, root wants ^0.9.0);
             `npm ci --legacy-peer-deps` succeeded (702 packages). Without
             this, `tsc` silently falls back to a global TS 6.0.2 binary and
             fails on tsconfig option deprecations — confirmed this happens
             identically on a clean master checkout, so it is an environment
             precondition, not a code defect. Recorded here so the next
             session doesn't re-diagnose it from scratch.

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        LOT-FM-001 self-assembly directive (BASIC ration module,
                  90-day / 3-month build: M1 OPEN TAB, M2 UPGRADE+ROSTER,
                  M3 ISSUE+FULFILLMENT) — recurring scheduled-session prompt.
CLASSIFICATION:  ENGINEERING
ACTION TAKEN:    Discovered M1 was already built and marked BEST in
                  LOT-MANIFEST.md eight iterations ago (branch
                  beautiful-johnson-56p7ov, commit 6815f550) but never
                  merged — that branch is ~150 commits stale and this
                  session's fresh branch (k91wjf) carried none of it. Ported
                  M1 (Basics.tsx + basics/doctrine.ts + route wiring) onto
                  the current master baseline by hand rather than cherry-pick
                  (source branch too divergent), then built M2 on top.
ROUTED TO:       docs/benchmark/ (this report)

--------------------------------------------------------------------------------
02 // CHECK A        03 // BUILD        04 // CHECK B (gate)
--------------------------------------------------------------------------------
COMMAND                               A          B
-------                               --         --
npm run server:build (tsc)            N/A*       PASS
npm run client:build (esbuild+css)    N/A*       PASS
npm run build (combined)              N/A*       PASS
BUILD:                                npm run build -> OK
GATE:                                 GREEN
FIX LOG:                              (1) UserProfile type was missing
                                       `metadata`, which /api/me already
                                       returns and app.tsx/us.tsx already
                                       read off it unsound — added
                                       `metadata?: Record<string, any>` to
                                       UserProfile; this also silences two
                                       pre-existing TS2339 errors in
                                       app.tsx/us.tsx under strict full-
                                       project check (verified via
                                       `tsc --noEmit -p tsconfig.json`,
                                       which is not itself part of the
                                       build gate but was used here as an
                                       extra correctness pass since esbuild
                                       does not type-check).
* CHECK A (baseline) could not run before BUILD — node_modules was absent
  at session start; see PREFLIGHT. First all-green run was CHECK B, after
  `npm ci --legacy-peer-deps`.

--------------------------------------------------------------------------------
05 // FILES CHANGED
--------------------------------------------------------------------------------
PATH                                                     STATUS
src/client/components/Basics.tsx                         ADDED
src/client/components/basics/doctrine.ts                 ADDED
src/client/components/basics/state.ts                     ADDED
src/client/stores/router.ts                               MODIFIED
src/client/components/ui/Layout.tsx                       MODIFIED
src/client/entries/app.tsx                                MODIFIED
src/client/queries.ts                                     MODIFIED
src/shared/types/index.ts                                 MODIFIED
src/shared/constants/index.ts                             MODIFIED
src/server/routes/api.ts                                  MODIFIED
src/server/routes/admin-api.ts                            MODIFIED

--------------------------------------------------------------------------------
05b // WHAT SHIPPED (M1 landed + M2 built)
--------------------------------------------------------------------------------
M1 — OPEN TAB (ported forward, previously BEST-but-unmerged)
  /basics route live, nav tab wired (logged-out and logged-in — the ledger
  is public per doctrine: "a stranger can read what LOT issues"). 23-item
  ration manifest rendered as a terminal-grid ledger grouped by category
  (NUTRITION/HEALTH/HYGIENE/EQUIPMENT), doctrine lines, USD 100.00/MO price
  line, status line. Fixed a latent bug while porting: the original status
  check used `tags.includes(UserTag.Usership)` (capitalized enum value)
  against tags the server stores lowercased — always false. Rewritten to
  `.some(t => t.toLowerCase() === 'usership')`, matching every other
  tag-check call site in the codebase.

M2 — UPGRADE + ROSTER (new this session)
  State machine lives in `user.metadata.basics` (JSONB), not a new DB
  column or a UserTag alone — same convention already used for
  `metadata.weeklyStory` — plus a `Basic` UserTag added only at
  ON_STRENGTH/STEADY_STATE for cheap membership checks elsewhere.
    PENDING       self-service: POST /api/basics/enroll (Usership-gated,
                   requires address+phone already on file via Settings,
                   captures sizing S/M/L/XL, computes cadenceStart = 1st of
                   next month).
    ON_STRENGTH   CEO-only: POST /admin-api/basics/:userId/advance — grants
                   the `basic` tag, mirrors the existing `canEditTags()`
                   gate (only vadikmarmeladov@gmail.com), same manual-
                   confirmation posture already used for Usership itself
                   (no in-app payment processor exists anywhere in this
                   codebase — SubscribeWidget just links out to
                   brand.lot-systems.com; BASIC follows that same pattern
                   rather than inventing billing).
    STEADY_STATE  CEO-only: same /advance endpoint, second call.
    STAND_DOWN    self-service: POST /api/basics/stand-down — drops the
                   `basic` tag, keeps Usership/AI tag untouched, retains
                   the metadata record (re-enroll loops back to PENDING).
  GET /admin-api/basics/roster (CEO-only) lists every user with a basics
  record for quartermaster review. No admin UI page consumes it yet — the
  API exists, the dashboard view is deferred; noted honestly rather than
  padded.
  Issue log: `metadata.basics.issueLog[]` scaffold (cycle/status/
  dispatchedAt) — structure only, per directive M2 scope. M3 populates it
  with real dispatches.
  EXIT BAR MET: a Usership/AI member can enroll (PENDING), be confirmed
  ON STRENGTH, reach STEADY STATE, and STAND DOWN — end to end, verified by
  reading the route/handler pair for each transition; no server was run in
  this session to click through it live (no dev DB in this container).

--------------------------------------------------------------------------------
05c // FLAGGED FOR S-2 — NOT ACTED ON
--------------------------------------------------------------------------------
44 branches named claude/beautiful-johnson-* exist on origin (per `git
ls-remote`). This scheduled directive has apparently fired repeatedly
without ever landing work on master — only one (56p7ov) ever produced a
real BEST iteration; the rest are presumed abandoned starts. Branches were
NOT deleted or touched — S-2 should review and prune per the manifest's
"redundant clusters" protocol. This session's own branch (k91wjf) is now
the one carrying live, merged-forward M1+M2 work; recommend it becomes the
new BEST for Basics Tab once reviewed, and stale beautiful-johnson-*
branches (55p7ov included, now superseded) are pruned in a future Sunday
pass.

--------------------------------------------------------------------------------
06 // SELF-ASSEMBLY
--------------------------------------------------------------------------------
LEDGER:      appended (20260819-01)
LEXICON:     1 reused (GREEN-GATE). 2 minted as CANDIDATES (first
             appearance, per the 3+ recurrence rule — not yet earned):
               METADATA-STATE — auxiliary per-user state machines live in
               the existing JSONB `metadata` column, keyed by feature name
               (`metadata.basics`, alongside existing `metadata.weeklyStory`),
               instead of new DB columns or overloading UserTag as the
               sole state carrier. Avoids a migration per feature.
               CEO-GATE-REUSE — a new privileged state transition
               (BASIC roster advance) reuses the existing `canEditTags()`
               single-operator gate rather than inventing a parallel
               authorization check. Same shape as the existing tags-edit
               gate on PUT /admin-api/users/:userId.
DOCTRINE:    rev N (unchanged this session — both candidates above are
             first-appearance, doctrine folds only on 3rd occurrence).
WORDS:       1112 (median ~540) — trend ↑ (environment-recovery note in
             PREFLIGHT plus the 05b/05c detail sections are the outliers;
             this report documents a stale-branch discovery + 2-file M1
             port + full M2 build, wider scope than a typical single-loop
             session)

--------------------------------------------------------------------------------
07 // PUSH
--------------------------------------------------------------------------------
COMMIT:      BENCHMARK: ENGINEERING — BASICS M2 upgrade+roster; M1 landed on master baseline [VM]
HASH:        34a2bb26
TAG:         benchmark-20260819-01 (created locally; push REJECTED — remote
             returned HTTP 403 on the tag ref specifically, 4 attempts with
             backoff, branch push itself succeeded cleanly. Session
             credentials appear scoped to the designated branch only, not
             arbitrary tag refs. Tag exists locally; next session with
             broader push scope should push it — do not recreate it.)
PUSH:        origin/claude/beautiful-johnson-k91wjf PUSHED

--------------------------------------------------------------------------------
08 // POST-PUSH VERIFICATION
--------------------------------------------------------------------------------
RESULT:      GREEN
NOTES:       For the next session: (1) M3 — ISSUE & FULFILLMENT is next per
             LOT-FM-001 (load engine, supplier quotes vs COGS ceiling,
             printed manifest card, first real dispatch). (2) Build a
             roster admin UI page consuming GET /admin-api/basics/roster —
             API-only today. (3) This branch was never run against a live
             dev server/DB this session (container had no DB); the next
             session should smoke-test the four transitions end-to-end
             against a real database before M3 starts writing real
             shipment data. (4) Prune the 44 beautiful-johnson-* branches
             per 05c, S-2 authorization required first.

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260819-01
================================================================================
