================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260705-01
TITLE:    BASICS TAB — LOT-FM-001 90-DAY BUILD (M1+M2+M3 in one session)
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  benchmark-20260703-01
BRANCH:   claude/beautiful-johnson-yb0f1s
DATE:     2026-07-05
TIME:     01:00 UTC
RESULT:   GREEN
================================================================================

--------------------------------------------------------------------------------
00 // PREFLIGHT
--------------------------------------------------------------------------------
REPO:        OK        DOCS WRITABLE: OK
REMOTE:      origin    REACHABLE
LAST GREEN:  benchmark-20260703-01
NOTE:        node_modules absent at session start (fresh checkout) — installed
             via `npm install --legacy-peer-deps` (yarn's default registry,
             registry.yarnpkg.com, is not on the egress allowlist; npm's
             default registry.npmjs.org is).

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        LOT-FM-001 SELF-ASSEMBLY DIRECTIVE — 90-day BASIC (ration)
                 module build spec: doctrine, 23-item civilian ration load,
                 house visual spec (LiberationMono-Bold, IBM 3270 register,
                 square corners, inversion-only), OPEN TAB public surface,
                 UPGRADE path from USERSHIP/AI into BASIC.
CLASSIFICATION:  ENGINEERING (feature build, all 3 months in one session)
PRIOR ART FOUND: MANIFEST already listed "Basics Tab" as BEST on
                 claude/beautiful-johnson-56p7ov (commit 6815f550, M1 only —
                 stale vs current master, ~89k line spurious diff). A second,
                 superseded prototype existed on claude/eager-clarke-wTEM6
                 (clothing-ration variant with issue-log + monthly rotation
                 patterns, also stale). Neither was cherry-picked wholesale;
                 the M1 ledger content was hand-ported from 56p7ov (it matches
                 MANIFEST's designated BEST direction), and the issue-log /
                 cadence-rotation structure was adapted from eager-clarke's
                 approach, rebuilt against current master's router/app.tsx/
                 Layout.tsx (both prototypes predate several refactors there).
ACTION TAKEN:    Built and shipped M1 (ledger + doctrine), M2 (upgrade state
                 machine + roster intake), and M3 (load engine + issue log +
                 printed manifest card + fulfillment) in a single pass —
                 see §05 for full file list.
ROUTED TO:       src/client/components/Basics.tsx + basics/, src/shared/,
                 src/server/ (routes, scheduled-jobs, utils)

--------------------------------------------------------------------------------
02 // CHECK A        03 // BUILD        04 // CHECK B (gate)
--------------------------------------------------------------------------------
COMMAND                                    A        B
-------                                    --       --
tsc --project tsconfig.server.json         PASS     PASS
  (server typecheck, noEmit)
tsc --project tsconfig.server.json         —        PASS (full emit,
  (server build)                                     dist/server rebuilt)
esbuild client bundle                      —        PASS ("Client build
  (scripts/build/client.build.ts -prod)               completed")
postcss client CSS build                   —        PASS (pre-existing
                                                     browserslist/baseline
                                                     warnings only, unrelated)
node smoke test: basics-cogs.js            —        PASS — computeLandedCost/
  (compiled dist output, not source)                 verifyMargin/
                                                     assertCadenceCycleWithin-
                                                     Envelope run against the
                                                     actual built module
GATE:                                       GREEN
FIX LOG:                                    .gitignore bug found + fixed (see
                                             §05) — a bare `server/` pattern
                                             was silently swallowing new files
                                             under src/server/utils/; `dist/`
                                             already covers the compiled
                                             output this rule intended.

NOTE ON LIVE UI VERIFICATION: this sandbox has no running Postgres instance
and no docker daemon, so the Fastify server could not be started end-to-end
(auth flow requires DB). Verification here is build-level (tsc + esbuild +
postcss all green) plus a direct node smoke-test of the compiled COGS math.
Recommend a live click-through (roster intake -> confirm -> stand down,
admin dispatch button) on staging before the Sunday merge pass.

--------------------------------------------------------------------------------
05 // FILES CHANGED
--------------------------------------------------------------------------------
PATH                                                      STATUS
.gitignore                                                MODIFIED
  — Removed bare `server/` ignore rule (dist/ already covers compiled
    output; the bare pattern matched src/server/ too and was silently
    dropping new server source files from `git status`/`git add`)
src/shared/constants/basics.ts                            ADDED
  — RATION_MANIFEST (23 items), DOCTRINE_LINES, PRICE_LINE, MANUAL_REF,
    RATION_COUNT, getIssueLoad()/shipsOnIssue() cadence load engine (M1+M3)
src/server/utils/basics-cogs.ts                           ADDED
  — Server-only landed-cost table (never imported client-side — COGS stays
    withheld from the public ledger per doctrine). computeLandedCost(),
    verifyMargin(), assertCadenceCycleWithinEnvelope(). Verified: worst-case
    (quarterly) issue costs $39.50 landed against a $40 ceiling, 60.5%
    margin against a 60% floor — envelope holds, tight by design (M3)
src/shared/types/index.ts                                 MODIFIED
  — UserTag.Basic added; UserProfile.metadata (was already read ad hoc by
    existing client code without being declared — closed the type gap);
    BasicsStatus / IssueLogStatus / IssueLogEntry / BasicsState types
src/shared/constants/index.ts                             MODIFIED
  — USER_TAGS_BY_ID[UserTag.Basic] entry
src/client/stores/router.ts                               MODIFIED
  — basics: void -> /basics route
src/client/components/ui/Layout.tsx                       MODIFIED
  — Enabled the pre-existing disabled "Basics" nav placeholder
    (route: 'basics' on both mobile/desktop nav arrays)
src/client/entries/app.tsx                                MODIFIED
  — Basics import, 'basics' added to PersistentRoute, <TabPanel> mounted
src/client/index.css                                      MODIFIED
  — .lot-basics-terminal (Liberation Mono / Courier New / monospace, bold —
    no self-hosted font binary, scoped only to this tab, doesn't touch the
    app's existing font-mono usages elsewhere) + @media print rules for the
    manifest card (body.lot-basics-printing toggle)
src/client/components/Basics.tsx                          ADDED (227 base
                                                           + M2/M3 extensions)
  — Full tab: doctrine + price (M1), 23-item ledger grouped by category with
    this-issue highlighting (M1+M3), status line (M1), upgrade/roster section
    driven by the PENDING -> ON_STRENGTH -> STAND_DOWN state machine (M2),
    issue log table + print-manifest trigger (M3)
src/client/components/basics/RosterForm.tsx                ADDED
  — Roster intake: confirms shipping address on file (reuses existing
    Settings address/city/country/phone, does not duplicate that form),
    optional shipping notes, single "STANDARD (1 OPERATOR)" size — no size
    selector, since doctrine fixes one ration per operator (M2)
src/client/components/basics/ManifestPrint.tsx              ADDED
  — Printed manifest card via the browser's own print pipeline (no new
    PDF dependency — pdfkit is a devDependency used by an offline script,
    not something to add to the live server). printManifest() toggles a
    body class; @media print swaps the whole app for the hidden card (M3)
src/client/queries.ts                                       MODIFIED
  — useBasicsRoster / useBasicsConfirm / useBasicsStandDown /
    useDispatchBasicRation mutations
src/server/routes/api.ts                                    MODIFIED
  — POST /api/basics/roster, /confirm, /standdown — self-serve state
    machine, gated on the Usership tag, additive (never touches it)
src/server/routes/admin-api.ts                              MODIFIED
  — POST /admin-api/basics/dispatch — CEO-gated (canEditTags(), same check
    already used for the tags editor). Marks the current SCHEDULED issue
    DISPATCHED, schedules issue N+1 one cadence month out, returns
    verifyMargin() for the next issue so ops sees the cost/margin at the
    moment of dispatch, not after the fact (M3)
src/server/scheduled-jobs.ts                                MODIFIED
  — Job 34: daily-ration-issue-watch (05:00 UTC). Flags ON-STRENGTH
    operators whose scheduled issue date has passed by writing a
    ration_issue_ready log — it never dispatches; a human still packs and
    ships the box, matching doctrine ("the box ships", not "the job ships")
src/client/components/AdminUser.tsx                         MODIFIED
  — Basics status block + "Dispatch ration" button on the admin user page,
    visible only to canEditTags() (CEO), only when a SCHEDULED issue exists

--------------------------------------------------------------------------------
06 // SELF-ASSEMBLY
--------------------------------------------------------------------------------
LEDGER:      appended (see below)
LEXICON:     terms reused: COCKPIT-RULE (ledger-as-instrument-panel framing
             carried into the ration ledger), MANIFEST. 0 minted this pass —
             BASIC / ON-STRENGTH / STEADY-STATE / STAND-DOWN are LOT-FM-001
             doctrine vocabulary, not new house lexicon.
DOCTRINE:    0 clauses touched this pass (no new engineering doctrine minted —
             the COGS-ceiling assertion pattern in basics-cogs.ts is a
             candidate for a future doctrine clause: "envelope assertions
             at boot, not in code review")
WORDS:       ~650 (median 540) — trend up (large multi-month feature)

DESIGN DECISIONS RECORDED (for the next session to know without re-deriving):
  1. Font: no self-hosted LiberationMono binary added. `.lot-basics-terminal`
     requests "Liberation Mono" first, falls back to "Courier New", monospace.
     Scoped to the Basics tab only — the rest of the app's font-mono usages
     are untouched.
  2. Tag mutation bypasses the CEO-only canEditTags() gate for the 3 BASIC
     self-serve endpoints (roster/confirm/standdown) — intentional, since
     this is a dedicated subscription feature, not general tag editing.
     The general PUT /admin-api/users/:userId tags editor remains CEO-only,
     untouched.
  3. "STEADY STATE" is a derived display label (ON_STRENGTH + issueCount >= 1),
     not a 4th persisted status enum value — keeps BasicsStatus to 3 states
     (PENDING / ON_STRENGTH / STAND_DOWN) with one clean transition graph.
  4. "Sizing" in roster intake is fixed at one standard ration (doctrine:
     "one ration per operator per month") — no size selector was built.
  5. Full click-through (roster -> confirm -> dispatch -> stand down) has
     not been verified against a live browser session — no Postgres in this
     sandbox. Server/client build gates are green; the COGS module was
     smoke-tested directly against its compiled output.

--------------------------------------------------------------------------------
07 // PUSH
--------------------------------------------------------------------------------
COMMIT:      (see git log — pushed to claude/beautiful-johnson-yb0f1s)
BRANCH:      claude/beautiful-johnson-yb0f1s
PUSH:        origin/claude/beautiful-johnson-yb0f1s PUSHED

--------------------------------------------------------------------------------
08 // POST-PUSH VERIFICATION
--------------------------------------------------------------------------------
RESULT:      GREEN
NOTES:       All three LOT-FM-001 build months landed in one session, each in
             a working, independently-mergeable state:
               M1 EXIT: a stranger can read the 23-item OPEN TAB ledger,
                 doctrine, and price line, read-only. Live.
               M2 EXIT: a Usership member can submit roster intake, confirm
                 enrollment (ON STRENGTH), and STAND DOWN back off — the
                 Usership/AI tag is never touched by any of it.
               M3 EXIT: the load engine computes what ships each issue by
                 cadence; the COGS ceiling and margin floor are asserted in
                 code, not just doctrine; a CEO can dispatch a real issue
                 from the admin panel and the next one auto-schedules;
                 a printable manifest card is one click away.
             MANIFEST update deferred to the Sunday self-assembly pass per
             §06 SUNDAY SELF-ASSEMBLY PROTOCOL in LOT-MANIFEST.md — this
             report stands as the BEST-candidate record for this branch.

================================================================================
LEDGER ENTRY
20260705-01 | ENGINEERING | BASICS TAB — LOT-FM-001 M1+M2+M3 in one session: ledger, upgrade state machine, load engine + fulfillment | GREEN | (pending commit) | LINES: +393 across 14 files
================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260705-01
================================================================================
