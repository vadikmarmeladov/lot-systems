================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260720-02
TITLE:    Story digest on Public Profile + monthly email (Usership-gated)
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  09b52d32
BRANCH:   claude/youthful-ritchie-4kgi2c
DATE:     2026-07-20
TIME:     22:10 UTC
RESULT:   GREEN
================================================================================

--------------------------------------------------------------------------------
00 // PREFLIGHT
--------------------------------------------------------------------------------
REPO:        OK        DOCS WRITABLE: OK
REMOTE:      origin    REACHABLE
LAST GREEN:  09b52d32 (this session's prior commit, LOT-SR-20260720-01)

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        S-2 directive — "make sure the story displays to paid users,
                  3 planned surfaces: Public Profile page, the monthly email,
                  on-demand /story in the Log"
CLASSIFICATION:  ENGINEERING
ACTION TAKEN:    Surface 3 (/story on-demand) already shipped in SR-20260720-01.
                  Read the Public Profile route/type/component and the monthly
                  email job/template before writing anything (public-api.ts
                  GET /profile/:userIdOrUsername already had a Usership-gated
                  memoryStory precedent to mirror exactly; scheduled-jobs.ts
                  already had a Usership-gated monthly-email-sender job with
                  its own summary generator in monthly-summary.ts). Added
                  storyDigest to both, reusing existing gates rather than
                  inventing new ones.
ROUTED TO:       docs/benchmark/   (this report); code changes in src/

--------------------------------------------------------------------------------
02 // CHECK A        03 // BUILD        04 // CHECK B (gate)
--------------------------------------------------------------------------------
COMMAND                                A        B
-------                                --       --
npx tsc --project tsconfig.server.json --noEmit   PASS     PASS  (0 errors)
npx tsc --project tsconfig.json --noEmit (client) 89 pre-existing, 89 pre-
                                                 existing — diff is only line-
                                                 number shifts from the code
                                                 added (same method as SR-01:
                                                 non-gating extra check, repo
                                                 has no client typecheck script)
npm run server:build                   PASS     PASS
npm run client:build                   PASS     PASS
BUILD:                                 npm run build -> OK (both halves green)
GATE:                                  GREEN
FIX LOG:                               (none — clean gate on first pass)

--------------------------------------------------------------------------------
05 // CHANGES THIS SESSION
--------------------------------------------------------------------------------
R1 PUBLIC PROFILE TYPE   shared/types/index.ts: PublicProfile.storyDigest?
                        { text, period, periodNumber?, tone?, dominantMood?,
                        generatedAt } — new optional field, additive only.
R2 PROFILE ROUTE         public-api.ts GET /profile/:id: new block directly
                        after the existing memoryStory block, gated on the
                        SAME condition (`privacy.showMemoryStory &&
                        hasUsershipTag`) rather than a new privacy flag —
                        deliberate: adding a dedicated toggle would need
                        Settings.tsx UI + a migration default for existing
                        users, disproportionate to this task. Reads
                        user.metadata.{weekly,monthly,yearly}Story (written by
                        Job 24/38/39), picks whichever has the newest
                        generatedAt, returns it as profile.storyDigest. Absent
                        for non-Usership users and Usership users with no
                        story yet generated (graceful, no error).
R3 PROFILE RENDER        PublicProfile.tsx: new Block "Story: {PERIOD}
                        {N}" right after the existing Memory Story block,
                        same blockView/whitespace-pre-wrap treatment, tone +
                        dominantMood shown as a dim caption line. Gated on
                        `privacySettings.showMemoryStory && profile.
                        storyDigest` client-side (defense in depth — server
                        already omits it for non-Usership/disabled-toggle).
                        Confirmed via research: PublicProfile.tsx has no
                        concept of viewer identity (unauthenticated fetch by
                        URL id/username) — gating is entirely server-side on
                        the PROFILE OWNER's Usership tag, correct for a public
                        page, so no client-side "is this my own profile"
                        logic was needed or added.
R4 MONTHLY EMAIL         monthly-summary.ts: MonthlySummary.storyDigest? and
                        MonthlyEmailData.storyDigest? (both optional,
                        additive). New "This Month, Compressed" section in
                        both generateMonthlyEmailBody (plain text) and
                        generateMonthlyEmailHtml, positioned directly after
                        the existing Memory Story section, same escapeHtml/
                        whitespace-pre-wrap pattern.
R5 EMAIL DIGEST COMPUTE  scheduled-jobs.ts: new composeMonthlyStoryDigest(logs)
                        helper, called inline in executeMonthlyEmailJob using
                        the logs it has ALREADY fetched (up to 1000, per
                        user) — deliberately NOT read from user.metadata.
                        monthlyStory. Reason: Job 38 (writes monthlyStory) and
                        the email job both fire on day 1 of the month but
                        Job 38 is hour-gated (18:00 UTC) while the email job
                        has no hour gate at all (fires whenever the hourly
                        scheduler first sees day===1) — reading the metadata
                        field would risk showing last month's story if the
                        email job runs before Job 38 that day. Computing
                        inline from already-fetched logs removes the
                        ordering dependency entirely; same template-
                        compression shape as Job 38 (mood/tone derivation,
                        closing lines), duplicated in ~45 lines rather than
                        shared, to avoid touching Job 24/38's tested code for
                        a cross-cutting refactor outside this session's scope.
                        Existing Usership + 3-month-tenure + 60%-engagement
                        gates on the email job are untouched — story digest
                        rides the same audience filter, no new gate added.

--------------------------------------------------------------------------------
05b // FILES CHANGED
--------------------------------------------------------------------------------
PATH                                        STATUS
src/shared/types/index.ts                   MODIFIED  (PublicProfile.storyDigest)
src/server/routes/public-api.ts             MODIFIED  (profile storyDigest block)
src/client/components/PublicProfile.tsx     MODIFIED  (Story: block render)
src/server/utils/monthly-summary.ts         MODIFIED  (storyDigest fields + sections)
src/server/scheduled-jobs.ts                MODIFIED  (composeMonthlyStoryDigest + wiring)
docs/benchmark/LOT-SR-20260720-02.md        ADDED

--------------------------------------------------------------------------------
06 // SELF-ASSEMBLY
--------------------------------------------------------------------------------
LEDGER:      appended (20260720-02)
LEXICON:     0 minted. STORY-PERIOD candidate (from SR-20260720-01) now
             recurs a 2nd time (Public Profile + email both read the
             day/week/month/year-scoped story shape) — still short of the
             3-appearance minting threshold, remains PROVISIONAL.
DOCTRINE:    Extended the "Period-Scoped Compression" PROVISIONAL note (not
             yet a numbered clause — 2 sessions is not 3) with the ordering-
             dependency finding: any surface that consumes a scheduled job's
             metadata output on the SAME day the job runs must either match
             the job's hour gate or recompute from source, never assume the
             job has already run. Recorded as an observation attached to the
             existing note, not a new clause.
WORDS:       ~480 (session net-new prose, this report). No new token minted
             this session; compression ratio not computed (consistent with
             SR-20260720-01's honesty rule — N/A rather than fabricated).

--------------------------------------------------------------------------------
07 // PUSH
--------------------------------------------------------------------------------
COMMIT:      BENCHMARK: ENGINEERING — story digest on Public Profile + monthly email [VM]
HASH:        (filled after commit)
TAG:         benchmark-20260720-02 (local only — SR-20260720-01 already
             recorded that tag pushes 403 in this session's git remote;
             not retried here, same permission-scoped denial)
PUSH:        origin/claude/youthful-ritchie-4kgi2c PUSHED

--------------------------------------------------------------------------------
08 // POST-PUSH VERIFICATION
--------------------------------------------------------------------------------
RESULT:      GREEN
NOTES:       All 3 planned story surfaces are now live: (1) Public Profile —
             GET /api/public/profile/:id, Usership-gated, no auth needed to
             view since it's a public page; (2) monthly email — Usership +
             3mo tenure + 60% engagement gated, existing audience unchanged;
             (3) on-demand /story — shipped in SR-20260720-01. All three read
             or derive from the same day/week/month/year compression shape
             established in Job 24/38/39. Follow-up for a future session: if
             a 3rd surface consumes {weekly,monthly,yearly}Story metadata,
             extract composeMonthlyStoryDigest-equivalent into a shared
             module and fold Period-Scoped Compression into a real doctrine
             clause + mint the STORY-PERIOD token.

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260720-02
================================================================================
