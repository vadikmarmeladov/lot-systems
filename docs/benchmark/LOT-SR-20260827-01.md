================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260827-01
TITLE:    Calendar Widget — Time Tracking + Military-Grade Alert Notifications
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  98971f2
BRANCH:   claude/dreamy-babbage-ugtork
DATE:     2026-08-27
TIME:     23:56 UTC
RESULT:   GREEN
================================================================================

--------------------------------------------------------------------------------
00 // PREFLIGHT
--------------------------------------------------------------------------------
REPO:        OK        DOCS WRITABLE: OK
REMOTE:      origin    REACHABLE
LAST GREEN:  NONE — no benchmark-* tags found on origin (prior sessions logged
             "(pending tag)" in the LEDGER; see 07/08 below — this session
             confirmed why: tag pushes 403 against this token, commit pushes
             don't). Plan B was unavailable this run; not needed — gate is
             GREEN. The rollback lattice remains unanchored on origin; local
             tags accumulate each session but never reach the remote.

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        Scheduled task: continue Calendar widget development — keep
                 the minimalist UI, make it a reliable time-tracker that logs
                 into Log with military-grade event notifications.
CLASSIFICATION:  ENGINEERING
ACTION TAKEN:    Added optional time-of-day to Calendar entries; sort/filter by
                 date+time; new CalendarAlertToast watches timed entries and
                 fires T-15 / T-0 / OVERDUE alerts (tactical chime + toast +
                 durable Log entry); Logs.tsx renders the new event; server
                 displayableEvents whitelist updated so the alert actually
                 surfaces in the Log feed (would otherwise be silently dropped
                 — same class of gap DOCTRINE already flags for formatLog()).
ROUTED TO:       docs/technical/   (no dedicated calendar/ folder exists)

--------------------------------------------------------------------------------
02 // CHECK A        03 // BUILD        04 // CHECK B (gate)
--------------------------------------------------------------------------------
COMMAND                                A        B
-------                                --       --
npm run client:build (esbuild+css)     PASS     PASS
npm run server:build (tsc typecheck)   PASS     PASS
BUILD:                                 npm run build -> OK
GATE:                                  GREEN
FIX LOG:                               1) CalendarAlertToast initially used
                                        decorative ◢◤◥◣ glyphs — caught against
                                        LOT-LEXICON "MILITARY PURITY" (no
                                        decoration) on doctrine review before
                                        push; replaced with the plain
                                        "ALERT [WINDOW]" bracket form already
                                        used by CAL:/BIO:/PLAN: handlers.
                                        2) node_modules was absent in this
                                        environment; yarn hit ECONNRESET
                                        against the registry, `npm ci
                                        --legacy-peer-deps` succeeded (702
                                        packages) — pre-existing
                                        nanostores/@nanostores/react peer
                                        conflict, unrelated to this change.

--------------------------------------------------------------------------------
05 // FILES CHANGED
--------------------------------------------------------------------------------
PATH                                              STATUS
src/client/components/CalendarAlertToast.tsx      ADDED
src/client/components/CalendarWidget.tsx          MODIFIED
src/client/components/Logs.tsx                    MODIFIED
src/client/components/System.tsx                  MODIFIED
src/client/stores/intentionEngine.ts              MODIFIED
src/client/utils/sovietKeyboard.ts                MODIFIED
src/server/routes/api.ts                          MODIFIED

--------------------------------------------------------------------------------
06 // SELF-ASSEMBLY
--------------------------------------------------------------------------------
LEDGER:      appended
LEXICON:     4 reused (MILITARY PURITY, GREEN-GATE, QIE, displayableEvents
             pattern), 0 minted this round — "calendar_alert" is a first
             appearance, held per the 3-report earn-in rule.
DOCTRINE:    0 clauses folded — the formatLog()/displayableEvents silent-drop
             pattern already has standing doctrine; this session is one more
             confirming instance, not yet a third distinct one.
WORDS:       ~510 (median 542, n=73) — trend ↓

--------------------------------------------------------------------------------
07 // PUSH
--------------------------------------------------------------------------------
COMMIT:      BENCHMARK: ENGINEERING — Calendar time tracking + military-grade
             alert notifications [VM]
HASH:        99a0cae
TAG:         benchmark-20260827-01 — created locally, `git push origin
             benchmark-20260827-01` returned HTTP 403 on every retry (4
             attempts, exponential backoff). The commit push itself
             succeeded, so this is a tag-ref-specific permission gap in the
             session token, not a network fault — retrying further would not
             help. This explains the "(pending tag)" entries already present
             throughout LOT-LEDGER.md: every prior session likely hit the
             same wall. Tag exists locally only; not on origin.
PUSH:        origin/claude/dreamy-babbage-ugtork PUSHED (commit only, no tags)

--------------------------------------------------------------------------------
08 // POST-PUSH VERIFICATION
--------------------------------------------------------------------------------
RESULT:      GREEN
NOTES:       For the next session: (1) calendar_entry and calendar_alert have
             no explicit case in any of the three server formatLog()
             implementations (memory.ts, question-generator.ts,
             story-generator.ts) — Memory Engine cannot reference scheduled or
             fired calendar events in generated questions/stories; this is a
             pre-existing gap (calendar_entry already lacked it), not a
             regression, but worth closing together. (2) Tag pushes return
             HTTP 403 with this session's GitHub credentials — commit/branch
             pushes work fine, only `refs/tags/*` writes are rejected. This is
             very likely why every prior "(pending tag)" ledger line was ever
             written: not forgetfulness, a standing token-scope limit. Whoever
             manages the GitHub App/PAT for this environment should widen its
             scope to include tag refs, or the rollback lattice this protocol
             depends on will stay permanently empty on origin.

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260827-01
================================================================================
