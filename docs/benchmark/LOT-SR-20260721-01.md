================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260721-01
TITLE:    BASICS TAB — MONTH 1: OPEN TAB ledger + doctrine, live
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  add997e
BRANCH:   claude/beautiful-johnson-bg0nxh
DATE:     2026-07-21
TIME:     02:22 UTC
RESULT:   GREEN
================================================================================

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        LOT-FM-001 SELF-ASSEMBLY DIRECTIVE — BASIC (RATION) MODULE,
                  90-DAY BUILD, scheduled/unattended session. Directive specifies
                  a 3-month build (Month 1: ledger+doctrine, Month 2: upgrade+
                  roster, Month 3: issue+fulfillment) and house terminal spec
                  (LiberationMono-Bold, white ground/black ink, inversion-only
                  hierarchy, 2px rules, square corners, IBM 3270 register).
ORIENT FINDING:  LOT-MANIFEST.md §01 claimed a "Basics Tab" BEST candidate on
                  branch beautiful-johnson-56p7ov (8/8 iterations, +293 lines,
                  "OPEN TAB live"). That branch no longer exists on the remote.
                  grep across src/ and docs/ found zero ration/OPEN TAB/BASIC
                  ledger code anywhere in master or this branch — only stray
                  prose mentions in About.tsx (wiki text) and
                  docs/corporate/LOT-FEATURE-INVENTORY-2026.md ("Basics Tab —
                  Physical supply subscription layer (LOT-FM-001) — IN-DEV").
                  The manifest's BEST claim did not reflect live repo state.
                  Built Month 1 from zero rather than resuming phantom work.
                  DISCOVERY: Layout.tsx nav already carried a disabled
                  { label: 'Basics' } placeholder (no route) in both the
                  logged-in and logged-out nav arrays — including on the
                  login screen — confirming this tab was pre-reserved as a
                  public-facing surface, not an authenticated-only feature.
CLASSIFICATION:  ENGINEERING
ACTION TAKEN:    Built the Basics tab client-side (new component + routing)
                  and server-side (public route registration). Corrected the
                  stale MANIFEST entry.
ROUTED TO:       docs/benchmark/  (session reports + manifest live here on
                  this repo, not docs/assembly/ as the skill's generic orient
                  script assumes — docs/assembly/ is this repo's namespace
                  for self-assembly *engine* version logs, a different series)

--------------------------------------------------------------------------------
02 // CHECK A  (pre-build baseline)
--------------------------------------------------------------------------------
COMMAND                              RESULT
-------                              ------
npm run server:build (tsc)           FAIL — pre-existing, unrelated to this
                                      session. tsconfig.server.json uses
                                      moduleResolution=node10 + baseUrl, both
                                      flagged as errors (TS5107/TS5101) by the
                                      installed TypeScript version. Confirmed
                                      pre-existing by `git stash` + re-run on
                                      unmodified tree — identical failure.
node_modules                         MISSING at session start. This sandbox
                                      had no dependencies installed at all
                                      (fresh container). `yarn install`
                                      blocked — registry.yarnpkg.com denied by
                                      proxy policy (403). Resolved via
                                      `npm install --legacy-peer-deps`
                                      (registry.npmjs.org is allowlisted);
                                      702 packages installed. The nanostores
                                      0.9.5 vs @nanostores/react peer-range
                                      (^0.7.0) conflict is a pre-existing
                                      lockfile/package.json mismatch, not
                                      introduced this session.

--------------------------------------------------------------------------------
03 // BUILD
--------------------------------------------------------------------------------
COMMAND:    npm run build  (client:css:build + client:js:build, then
            server:build via tsc + esm-import fix)
RESULT:     OK
NOTES:      New component src/client/components/Basics.tsx (219 lines) —
            the OPEN TAB public ledger. Wired into router.ts (new `basics`
            route -> /basics), Layout.tsx nav (activated the pre-existing
            disabled placeholder), app.tsx TabPanels (authenticated view),
            login.tsx (logged-out/public view — renders <Basics/> instead of
            the login form when route === 'basics', so a stranger sitting at
            the login screen can open it without an account), and
            server/index.ts KNOWN_CLIENT_ROUTES (added '/basics' alongside
            the existing '/settings' /'/sync' /'/log' pattern — same public/
            authed branch logic, no new auth surface). Content verified
            present in the correct shared esbuild chunk
            (chunk-V6CLZ5CB.js, code-split and reachable from both the `app`
            and `login` entries) via grep for "BASIC RATION" / "LOT-FM-001"
            post-build.

--------------------------------------------------------------------------------
04 // CHECK B  (green gate)
--------------------------------------------------------------------------------
COMMAND                              RESULT
-------                              ------
npm run server:build (tsc)           PASS (after node_modules install)
npm run client:js:build (esbuild)    PASS
npm run client:css:build (postcss)   PASS
npm run build (full)                 PASS
GATE:                                GREEN
FIX LOG:                             1) node_modules absent -> npm install
                                      --legacy-peer-deps (environment fix,
                                      zero code changes). 2) No other fixes
                                      needed — Basics.tsx compiled clean on
                                      first pass.
RUNTIME NOTE:                        Server smoke-start (node dist/server/
                                      server/index.js) exits immediately on
                                      "Missing required environment variable:
                                      DB_HOST" — this sandbox has no database
                                      configured, so a live browser
                                      golden-path check of /basics was not
                                      possible here. Verified instead via (a)
                                      clean compile/bundle of all touched
                                      files, (b) grep-confirmed presence of
                                      the new content in the correct output
                                      chunk reachable from both entries. This
                                      is build+bundle verification, not a
                                      substitute for an in-browser check —
                                      flagged honestly per house policy.

--------------------------------------------------------------------------------
05 // FILES CHANGED
--------------------------------------------------------------------------------
PATH                                          STATUS
----                                          ------
src/client/components/Basics.tsx              ADDED    (+219)
src/client/stores/router.ts                   MODIFIED (+2)   new `basics` route
src/client/components/ui/Layout.tsx           MODIFIED (+3/-3) activated Basics nav item
src/client/entries/app.tsx                    MODIFIED (+4)   Basics TabPanel (authed)
src/client/entries/login.tsx                  MODIFIED (+14)  Basics view (public/logged-out)
src/server/index.ts                           MODIFIED (+1/-1) '/basics' in KNOWN_CLIENT_ROUTES
docs/benchmark/LOT-MANIFEST.md                MODIFIED         corrected stale Basics Tab entry
docs/benchmark/LOT-DOCTRINE.md                MODIFIED         + Basics/LOT-FM-001 terminal-spec clause

--------------------------------------------------------------------------------
06 // SELF-ASSEMBLY
--------------------------------------------------------------------------------
LEDGER:          appended  -> docs/benchmark/LOT-LEDGER.md
LEXICON:         0 tokens reused, 0 minted. OPEN TAB / ON STRENGTH / BASIC
                 RATION are first-appearance concepts this session — below
                 the 3-report minting threshold. Left as plain words per
                 SELF-ASSEMBLY.md minting rule (earn, don't decree).
DOCTRINE:        1 new clause folded: "Basics Tab / LOT-FM-001 terminal
                 spec" — records the fixed white-ground/black-ink,
                 inversion-only, 2px-rule house style as a reusable token
                 set (TERMINAL const in Basics.tsx) for Month 2/3 builds to
                 import rather than re-derive.
COMPRESSION:     ~640 raw words (this report's net-new knowledge in full
                 prose) -> ~55 retained tokens (MANIFEST line + LEDGER line +
                 1 doctrine clause + TERMINAL const name) — ratio ~11.6:1.
                 First-session ratio for a new feature line; expected to
                 rise as Month 2/3 cite this clause instead of restating it.

--------------------------------------------------------------------------------
07 // PUSH
--------------------------------------------------------------------------------
COMMIT:          BENCHMARK: ENGINEERING — Basics Tab M1: OPEN TAB ledger,
                 doctrine, price line live [VM]
HASH:            69d9061
TAG:             benchmark-20260721-01   (Plan-B rollback target — first tag
                 in this repo's history; prior 100+ ledger GREEN entries were
                 never tagged, this session establishes the tag per protocol)
REMOTE:          origin/claude/beautiful-johnson-bg0nxh   PUSHED

--------------------------------------------------------------------------------
08 // POST-PUSH VERIFICATION
--------------------------------------------------------------------------------
RESULT:          GREEN
NOTES:           Month 1 exit condition met: a stranger (authenticated or
                 not, including at the login screen) can open the Basics tab
                 and read the doctrine statement, the 23-item ration
                 nomenclature+cadence ledger (COGS withheld), the price line
                 (USD 100/mo), and a status line stating build stage —
                 read-only, no roster, no billing, no upgrade control. That
                 is intentionally out of scope until Month 2. Next session:
                 UPGRADE control + USERSHIP/AI -> PENDING -> ON STRENGTH ->
                 STEADY STATE state machine, roster intake, recurring
                 billing, STAND DOWN downgrade. NODE_MODULES + DB_HOST are
                 not present in this sandbox by default — any future session
                 here should expect to re-run `npm install --legacy-peer-deps`
                 and will still be unable to smoke-test the live server
                 without DB credentials; note this for whoever configures
                 the next environment if a real browser check is wanted.

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260721-01
================================================================================
