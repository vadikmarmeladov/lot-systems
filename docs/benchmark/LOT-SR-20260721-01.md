================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260721-01
TITLE:    LOT® Mail — third ship attempt, ported onto current master, GREEN
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  add997e6
BRANCH:   claude/determined-turing-w17wnq
DATE:     2026-07-21
TIME:     04:20 UTC
RESULT:   GREEN
================================================================================

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        Scheduled routine (mission text identical to SR-20260612-01
                 and SR-20260718-01 — see 06 below for full recurrence history)
CLASSIFICATION:  ENGINEERING
ACTION TAKEN:    Cherry-picked commit 19f7906e (the 2026-06-12 LOT Mail build,
                 branch determined-turing-f6bw7r) onto this branch's current
                 master base (add997e6, ~5 weeks newer than the source branch).
                 Hand-resolved 3 conflicting files against master's intervening
                 refactors (COCKPIT-RULE log handlers, chat-gating, BLANK-STRIP,
                 render-isolation sseMessages pattern). Added a server-side
                 participation gate to all 4 mail endpoints (the carried-over
                 code had none — chat has one, mail didn't, doctrine calls for
                 uniform gating). Fixed one latent routing bug (below).
ROUTED TO:       docs/benchmark/ (this report); code merged into working tree
                 on claude/determined-turing-w17wnq

MISSION (verbatim, scheduled):
  Design, develop, and deploy the simplest LOT® Email system in LOT® style.
  Compose a message in Log with a command "/email to Hitomi."
  New messages appear in Sync.
  Incorporate with Lot Chat (Sync) and Cohort Dating through LOT Community.
  Push a full .MD report after each session.

--------------------------------------------------------------------------------
02 // CHECK A  (pre-build baseline)
--------------------------------------------------------------------------------
COMMAND                               RESULT
-------                               ------
node_modules present                  FAIL — absent, this session's container
                                       had no install; `npm install
                                       --legacy-peer-deps` run first (702 pkgs;
                                       matches precedent commit "chore: update
                                       yarn.lock after npm install
                                       --legacy-peer-deps [VM]")
npm run server:build (tsc typecheck)  PASS (post-install)
npm run client:build (esbuild+css)    PASS (post-install)

--------------------------------------------------------------------------------
03 // BUILD
--------------------------------------------------------------------------------
COMMAND:    npm run build  (client:css:build + client:js:build + server:build)
RESULT:     OK
NOTES:      Clean client bundle + CSS emit; tsc server typecheck 0 errors (only
            pre-existing tsconfig deprecation notices, moduleResolution/baseUrl
            — unrelated to this change). No new npm dependency required — the
            ported feature uses only already-present packages (sequelize,
            fastify, react-query).

--------------------------------------------------------------------------------
04 // CHECK B  (green gate)
--------------------------------------------------------------------------------
COMMAND                                RESULT
-------                                ------
npm run client:build                   PASS
npm run server:build                   PASS
npm run build (full)                   PASS
supplementary: npx tsc --noEmit        INFORMATIONAL — whole-repo strict
  -p tsconfig.json                     pass surfaces ~50 pre-existing errors
                                        across unrelated files (this config is
                                        not part of the repo's declared gate;
                                        client ships via esbuild, not tsc). Ran
                                        it anyway as an extra check on the
                                        carried-over code specifically and it
                                        caught one real bug (see FIX LOG).
GATE:                                   GREEN
FIX LOG:
  - ChatCatalystWidget.tsx: stores.goTo('log') -> stores.goTo('logs'). 'log'
    is not a Routes key (router.ts:19 defines 'logs' -> path '/log'); the
    esbuild-only client build does not type-check JSX prop calls so this was
    a silent no-op (Mail button in cohort catalyst never navigated). Same bug
    independently caught by SR-20260718-01 in its own port of the same source
    commit — two independent sessions hit and fixed the identical defect,
    which is a decent signal it's real and worth a doctrine line (06).
  - Sync.tsx: HEAD had refactored `messages` from raw useState into a derived
    useMemo (sseMessages + fetchedMessages merge, render-isolation pattern)
    since the source branch was cut; kept HEAD's derived-state architecture,
    layered branch's `tab`/`newMailEvents` state on top, dropped the branch's
    now-redundant `messages` useState and unused `hasInitiallyLoaded` ref.
  - Sync.tsx: kept HEAD's `canAccessChat` early-return gate (added after the
    source branch was cut) ahead of the branch's `unreadCount` computation.
  - Logs.tsx: two `else if` chains (trigger dispatch effect, JSX result
    blocks) had unrelated triggers added on both sides since June 12 (system-
    help, story-mode, how-checkin, breathe, silent-mode, etc.) — merged as
    sibling branches/blocks, no logic dropped from either side.
  - logTriggers.ts: unioned the LogTrigger type and RULES array entries from
    both sides (no overlap, pure append).
  - Added server-side canAccessChat + suspended gate to POST /mail, GET
    /mail/inbox, GET /mail/unread-count, PUT /mail/:id/read (api.ts) — the
    carried-over code only gated the UI tab, not the API. SR-20260719-01
    established "chat gating: uniform participation gate" for likes; this
    session extends it to mail, closing a bypass where a non-Usership account
    could hit /api/mail directly.
  - Logs.tsx /system help text: added a `/email to X` line (was missing from
    both the June 12 and July 18 ports).

--------------------------------------------------------------------------------
05 // FILES CHANGED
--------------------------------------------------------------------------------
PATH                                                     STATUS
----                                                     ------
migrations/20260612120000_add-lot-mail.cjs               ADDED
src/client/components/ChatCatalystWidget.tsx              MODIFIED
src/client/components/EmailInbox.tsx                      ADDED
src/client/components/Logs.tsx                             MODIFIED
src/client/components/Sync.tsx                             MODIFIED
src/client/queries.ts                                       MODIFIED
src/client/utils/logTriggers.ts                              MODIFIED
src/server/models/index.ts                                    MODIFIED
src/server/models/lot-mail.ts                                  ADDED
src/server/routes/api.ts                                        MODIFIED
src/shared/constants/index.ts                                     MODIFIED
src/shared/types/index.ts                                          MODIFIED
docs/benchmark/LOT-SR-20260721-01.md                                 ADDED
docs/benchmark/LOT-MANIFEST.md                                        MODIFIED
docs/benchmark/LOT-LEDGER.md                                          MODIFIED
docs/benchmark/LOT-DOCTRINE.md                                        MODIFIED

--------------------------------------------------------------------------------
06 // RECURRENCE — this is the third ship attempt, not the first
--------------------------------------------------------------------------------
The scheduled mission text is byte-identical across three sessions:

  SR-20260612-01  branch determined-turing-f6bw7r  self-marked SHIPPED
                   -> never merged; LOT-MANIFEST.md kept it at BEST for
                      5 weeks; zero LOT Mail code ever reached master.
  SR-20260718-01  branch determined-turing-9i4u15  marked GREEN
                   -> re-ported the same source commit onto July-18 master,
                      explicitly flagged the branch-push constraint (push
                      pinned to an isolated per-session branch, not master;
                      Ship Mode needs S-2 authorization to touch master
                      directly) as the reason it could not close the loop.
                      Also never merged.
  SR-20260721-01  branch determined-turing-w17wnq  this report
                   -> same source commit, re-ported a third time onto
                      today's master (add997e6). Same push constraint holds:
                      this session's harness pins pushes to
                      claude/determined-turing-w17wnq, not master. The code
                      is GREEN and ready; merging it is a deliberate action
                      only S-2 can take (open a PR from this branch, or run
                      Ship Mode with explicit master-push authorization).

Net effect: three independent, correct engineering passes have now produced
functionally the same ~570-line feature, each abandoned on its own branch
after the session ended, because the scheduled task has no mechanism to
either (a) detect the feature already exists unmerged and stop re-doing the
work, or (b) actually complete the merge to master itself. This session's
LOT-MANIFEST.md edit (07) records the true state so a fourth run doesn't
repeat the pattern blind — but the manifest alone does not solve it; only a
PR/merge action does. Flagged to S-2 outside this report via the routine's
notification channel.

--------------------------------------------------------------------------------
07 // SELF-ASSEMBLY
--------------------------------------------------------------------------------
LEDGER:      appended (20260721-01)
LEXICON:     0 minted this session (no concept yet at 3+ recurrence threshold
             for a new token). 2 reused: GREEN-GATE, COHORT (rev A, existing
             — LOT Mail's cohort-catalyst hook is the COHORT token's action
             surface, not a new concept).
DOCTRINE:    rev N->O. 1 principle added: STALE-SHIP (a scheduled/recurring
             mission must check LOT-MANIFEST.md + search remote branches for
             an existing BEST/SHIPPED entry matching its own mission text
             before rebuilding from scratch — and porting via cherry-pick
             onto the freshest available base beats re-inventing, but does
             not by itself close the loop unless the result reaches master).
             1 principle extended: Ship Mode Discipline — now records that a
             scheduled session's branch-push constraint (no master-write
             authorization) is a structural reason Ship Mode's last step can
             silently never fire across repeated runs; each run must say so
             explicitly rather than re-mark STATUS: SHIPPED aspirationally
             (as SR-20260612-01 did).
COMPRESSION: ~650 raw words (this session's net-new finding: 3rd unmerged
             LOT-Mail attempt + branch-push-constraint pattern) -> 2 doctrine
             clause references + 1 MANIFEST row (ratio ~9:1)

--------------------------------------------------------------------------------
08 // PUSH
--------------------------------------------------------------------------------
COMMIT:      BENCHMARK: ENGINEERING — LOT Mail ship attempt 3/3, server-side
             mail gating, goTo routing fix [VM]
HASH:        9adcd276
TAG:         benchmark-20260721-01
REMOTE:      origin/claude/determined-turing-w17wnq  PUSHED

--------------------------------------------------------------------------------
09 // POST-PUSH VERIFICATION
--------------------------------------------------------------------------------
RESULT:      GREEN
NOTES:       Code is complete and green on claude/determined-turing-w17wnq.
             NOT on master. NOT deployed. NOT running any migration — no
             DATABASE_URL configured in this session's container, so
             migrations/20260612120000_add-lot-mail.cjs has not been executed
             anywhere; it will run via `npm run migrations:up` whenever this
             branch (or its merge) reaches an environment with a live DB.
             Closing this out for real requires: (1) S-2 opens/approves a PR
             from claude/determined-turing-w17wnq (or a Ship Mode session
             explicitly authorized to write master), (2) migrations run once
             post-merge, (3) a follow-up CHECK C against the deployed state.

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260721-01
================================================================================
