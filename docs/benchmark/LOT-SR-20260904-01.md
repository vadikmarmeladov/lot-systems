================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260904-01
TITLE:    LOT MAIL — restore /email trigger onto current ship branch
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  98971f20 (pre-cherry-pick) -> see 07 // PUSH for post-commit hash
BRANCH:   claude/determined-turing-ogha6p
DATE:     2026-09-04
TIME:     04:30 UTC
RESULT:   GREEN
================================================================================

--------------------------------------------------------------------------------
00 // PREFLIGHT
--------------------------------------------------------------------------------
REPO:        OK        DOCS WRITABLE: OK
REMOTE:      origin    REACHABLE
LAST GREEN:  benchmark-20260805-01

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        S-2 directive — "simplest LOT Email system": /email trigger in
                 Log, delivery via Sync, integration with Cohort/Community.
CLASSIFICATION:  ENGINEERING
ACTION TAKEN:    LOT-MANIFEST.md already listed LOT Mail as BEST (11/11 iter,
                 branch determined-turing-f6bw7r, hash fa622a25) — a prior
                 session designed and iterated this exact feature but it was
                 never carried onto a ship branch. Cherry-picked its single
                 self-contained feat commit (19f7906e) onto the current branch
                 instead of re-implementing from scratch. Resolved 3 merge
                 conflicts against ~3 months of intervening master history
                 (Logs.tsx, Sync.tsx, logTriggers.ts — all additive, no logic
                 collisions). The other 9 files applied clean.
ROUTED TO:       docs/benchmark/ (session report); feature code lands in
                 src/client + src/server per existing flat layout.

--------------------------------------------------------------------------------
02 // CHECK A        03 // BUILD        04 // CHECK B (gate)
--------------------------------------------------------------------------------
COMMAND                              A        B
-------                              --       --
npm install --legacy-peer-deps       PASS     PASS  (yarn registry blocked by
                                                       egress policy; npm to
                                                       registry.npmjs.org OK)
tsc --project tsconfig.server.json   n/a      PASS  (0 errors)
npm run client:js:build              n/a      PASS  (2 pre-existing dup-key
                                                       warnings in badges.ts,
                                                       unrelated to this diff)
npm run client:css:build             n/a      PASS
npm run server:build                 n/a      PASS
BUILD:                yarn run build -> OK
GATE:                  GREEN
FIX LOG:               logTriggers.ts: merged LogTrigger union + RULES entry
                        (both branches only appended, straight union).
                        Sync.tsx: kept HEAD's sseMessages SSE-merge state +
                        canAccessChat gate, added tab/newMailEvents state and
                        the Chat|Mail tab strip beneath it.
                        Logs.tsx: kept HEAD's /breathe·/silent·/freeze·/fast·
                        /phys·/system·/how·/story handlers, appended
                        email-compose handler as a new else-if arm; same
                        pattern for the inline result-panel JSX (story panel
                        closed properly, mail panel appended as sibling).
                        Added "/email to [Name] [msg]" line to /system help.

--------------------------------------------------------------------------------
05 // FILES CHANGED
--------------------------------------------------------------------------------
PATH                                            STATUS
migrations/20260612120000_add-lot-mail.cjs      ADDED
src/client/components/EmailInbox.tsx            ADDED
src/server/models/lot-mail.ts                   ADDED
src/client/components/ChatCatalystWidget.tsx    MODIFIED (✉ Mail button on
                                                 cohort-connect suggestions)
src/client/components/Logs.tsx                  MODIFIED (/email trigger +
                                                 compose panel, merge-resolved)
src/client/components/Sync.tsx                  MODIFIED (Chat|Mail tabs,
                                                 unread badge, merge-resolved)
src/client/queries.ts                           MODIFIED (useLotMailInbox,
                                                 useLotMailUnreadCount,
                                                 useSendLotMail, useMarkMailRead)
src/client/utils/logTriggers.ts                 MODIFIED (email-compose trigger,
                                                 merge-resolved)
src/server/models/index.ts                      MODIFIED (register LotMail)
src/server/routes/api.ts                        MODIFIED (POST /mail, GET
                                                 /mail/inbox, GET
                                                 /mail/unread-count, PUT
                                                 /mail/:id/read; SSE lot_mail
                                                 routed to toUserId only)
src/shared/constants/index.ts                   MODIFIED (MAX_LOT_MAIL_LENGTH)
src/shared/types/index.ts                       MODIFIED (LotMail,
                                                 LotMailRecord,
                                                 LotMailSyncPayload)
                                                 12 files, +554/-5

--------------------------------------------------------------------------------
06 // SELF-ASSEMBLY
--------------------------------------------------------------------------------
LEDGER:      appended
LEXICON:     0 reused, 0 minted (LOT MAIL is a first appearance in a report on
             this ship branch — the concept itself was already earned via 11
             prior iteration reports on determined-turing-f6bw7r that predate
             this lexicon's coverage; not re-minting from a cold count. Will
             mint on 3rd occurrence under this branch's own report lineage.)
DOCTRINE:    0 clauses folded this run — no new stable pattern emerged beyond
             what CROSS-DEVICE-SYNC / RENDER-ISOLATION already cover.
WORDS:       520 (median 540) — trend =

--------------------------------------------------------------------------------
07 // PUSH
--------------------------------------------------------------------------------
COMMIT:      BENCHMARK: ENGINEERING — LOT Mail /email trigger restored onto
             ship branch [VM]
HASH:        (recorded post-commit — see repo `git log -1`)
TAG:         benchmark-20260904-01
PUSH:        origin/claude/determined-turing-ogha6p PUSHED

--------------------------------------------------------------------------------
08 // POST-PUSH VERIFICATION
--------------------------------------------------------------------------------
RESULT:      GREEN
NOTES:       No local Postgres in this sandbox — migration
             20260612120000_add-lot-mail.cjs was reviewed (matches the
             LotMail Sequelize model field-for-field, FK cascade on both
             fromUserId/toUserId, indexed on (toUserId, read, createdAt) and
             (fromUserId, createdAt)) but NOT executed against a live DB this
             session. Runs automatically via `yarn migrations:up` on next
             deploy alongside the rest of the migration chain — order is
             safe because sequelize-cli tracks applied-vs-pending by name in
             SequelizeMeta, not by timestamp ordering against already-run
             migrations. Feature is functionally complete and green-gated;
             not yet merged to master (that is a separate S-2-triggered SHIP
             MODE action per LOT-MANIFEST.md, still queued there as BEST).

FEATURE SUMMARY — what shipped to this branch:
  /email to [Name] [message]  — typed in Log, launches inline compose panel
    (also ✉️ / 📧 / /mail keyword aliases)
  Recipient resolved by first/last name match against LOT Community's User
    table (case-insensitive); 404 with a "not in LOT Community" hint if no
    match, 400 if sending to self.
  Delivery is realtime: POST /api/mail creates a LotMail row, then
    sync.emit('lot_mail', ...) pushes it over the existing SSE bus — the
    server only forwards to the connected client whose id equals toUserId.
  Sync gained a Chat | Mail tab strip; Mail tab shows an inbox
    (EmailInbox.tsx) with an unread-count badge, backed by
    GET /api/mail/inbox, GET /api/mail/unread-count, PUT /api/mail/:id/read.
  Cohort integration: ChatCatalystWidget's cohort-connect suggestions gained
    a secondary "✉ Mail" button beside the existing primary action — clicking
    it jumps to Log and pre-fills "/email to <cohort member name> ", so a
    Cohort Connect match can be emailed in one step without leaving the
    catalyst card.

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260904-01
================================================================================
