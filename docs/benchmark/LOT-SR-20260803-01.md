================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260803-01
TITLE:    Basics Tab — LOT-FM-001 BASIC ration, Month 1: OPEN TAB live
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  benchmark-20260727-01
BRANCH:   claude/beautiful-johnson-4dkv61
DATE:     2026-08-03
TIME:     01:10 UTC
RESULT:   GREEN
================================================================================

--------------------------------------------------------------------------------
00 // PREFLIGHT
--------------------------------------------------------------------------------
REPO:        OK        DOCS WRITABLE: OK
REMOTE:      origin    REACHABLE
LAST GREEN:  benchmark-20260727-01
NODE_MODULES: absent at session start (fresh container) — `npm install
             --legacy-peer-deps` run to restore a checkable environment (yarn
             registry blocked by gateway policy; npm via registry.npmjs.org
             succeeded, 702 packages).

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        Scheduled task — "Basics Tab" self-assembly directive
                  (LOT-FM-001 / SELF-ASSEMBLY DIRECTIVE, MODULE: BASIC
                  (RATION), 90-DAY BUILD). No prior LOT-FM-001 manual or
                  Basics Tab code existed in the repo — About.tsx carried a
                  Row claiming a "Basics Tab" feature branch
                  (nifty-allen-jWyOe, 6 iterations, BEST, 24 files,
                  +1725 lines) that does not exist locally or on origin.
                  Treated as documentation drift, not ground truth — see 05c.
CLASSIFICATION:  PRODUCT (new surface) + ENGINEERING (build/ship) + DOCS
                  (drift correction)
ACTION TAKEN:    Authored LOT-FM-001 manual (doctrine, 23-item ration load,
                  visual spec, price line, upgrade state-machine sketch).
                  Built Month 1 scope only per the directive's own envelope:
                  OPEN TAB public ledger, read-only, live at /basics.
ROUTED TO:       docs/corporate/ (manual), docs/benchmark/ (this report),
                  src/shared|client|server (code)

--------------------------------------------------------------------------------
02 // CHECK A        03 // BUILD        04 // CHECK B (gate)
--------------------------------------------------------------------------------
COMMAND                              A        B
-------                              --       --
npx tsc --noEmit -p tsconfig.json    RED*     RED*
npm run client:build (esbuild+css)   n/a      PASS
npm run server:build (tsc typecheck) n/a      PASS
BUILD:                               client + server -> OK, dist/client/js/
                                      basics.js present, dist/server built
GATE:                                GREEN
FIX LOG:                             none required on new files

* tsc --noEmit -p tsconfig.json is NOT the repo's build gate (no npm script
  runs it; client ships via esbuild transpile, server via tsc against
  tsconfig.server.json only). Ran it anyway as an extra check: 99 pre-
  existing errors on master BEFORE this session's changes, 99 AFTER —
  confirmed by `git stash` diff. Zero errors attributable to
  basicsRation.ts / BasicsPage.tsx / entries/basics.tsx / the
  server/public-api.ts or server/index.ts edits. Pre-existing red left
  untouched — out of scope for this session, not introduced by it.
  tsc --project tsconfig.server.json (the real server gate): clean, exit 0.

--------------------------------------------------------------------------------
05 // BUILT THIS SESSION — MONTH 1 OF 3 (OPEN TAB, per directive envelope)
--------------------------------------------------------------------------------
R1 MANIFEST DATA     src/shared/constants/basicsRation.ts — canonical
                     23-item RATION_MANIFEST (code/category/nomenclature/
                     cadence), RATION_DOCTRINE (5 lines), RATION_PRICE_USD
                     ($100), RATION_COGS_CEILING_USD ($40, internal-only,
                     never serialized to any public response),
                     RationStrengthStatus type (scaffold for Month 2).
                     Runtime assertion pins the manifest at exactly 23 lines.
R2 PUBLIC API        GET /api/public/basics-manifest (public-api.ts) —
                     returns module id, price, doctrine, and the 23-item
                     manifest (nomenclature + cadence only). COGS withheld
                     at the API layer, not just the UI — "the ledger is the
                     marketing" enforced server-side.
R3 OPEN TAB PAGE     src/client/components/BasicsPage.tsx — public,
                     read-only ledger UI. House style per directive:
                     LiberationMono-Bold (fallback stack), white ground /
                     black ink, inversion-only hierarchy (no color), 2px
                     black rules, border-radius 0 everywhere, IBM-3270-style
                     status lines, quartermaster voice. Deliberately
                     separate visual system from the main app's rounded/
                     colored chrome — scoped inline <style>, no shared Page/
                     Block components.
R4 ROUTE + BUILD     GET /basics (server/index.ts, mirrors existing /status
                     pattern) serves generic-spa with scriptName "basics";
                     entries/basics.tsx added to client.build.ts entryPoints.
R5 MANUAL            docs/corporate/LOT-FM-001-RATION-MANUAL.md — doctrine,
                     full 23-item table by category, economic envelope,
                     formal visual spec, OPEN TAB spec, Month 2 state-
                     machine sketch, Month 3 fulfillment sketch, build
                     envelope table. Manual is descriptive; basicsRation.ts
                     is canonical — manual points back to the constant so
                     the two cannot silently drift.
R6 DOCS DRIFT FIX    About.tsx "Basics Tab" Row rewritten from a fabricated
                     completed-branch claim to the actual state: Month 1
                     live at /basics, Months 2-3 not yet built.

--------------------------------------------------------------------------------
05b // FILES CHANGED (this session)
--------------------------------------------------------------------------------
PATH                                                     STATUS
src/shared/constants/basicsRation.ts                     ADDED
src/client/components/BasicsPage.tsx                     ADDED
src/client/entries/basics.tsx                             ADDED
src/server/routes/public-api.ts                          MODIFIED
src/server/index.ts                                       MODIFIED
scripts/build/client.build.ts                             MODIFIED
src/client/components/About.tsx                          MODIFIED
docs/corporate/LOT-FM-001-RATION-MANUAL.md                ADDED
docs/benchmark/LOT-SR-20260803-01.md                      ADDED

--------------------------------------------------------------------------------
05c // DOCS DRIFT — NOTE FOR S-2
--------------------------------------------------------------------------------
About.tsx previously described a "Basics Tab" as already shipped on a
feature branch (nifty-allen-jWyOe) with a specific file/line count and BEST
status. No such branch exists locally or on origin, and no Basics code
existed anywhere in the repo before this session. Flagging per the honest-
engineering rule (fabricated precision is worse than an honest gap) — this
was not verified before landing at some earlier point and is corrected in
this session (R6). Recommend spot-checking other About.tsx "feature branch"
rows against actual git history if their accuracy matters going forward.

--------------------------------------------------------------------------------
06 // SELF-ASSEMBLY
--------------------------------------------------------------------------------
LEDGER:      appended (20260803-01)
LEXICON:     0 minted this session. OPEN-TAB (public read-only ledger
             surface, ledger data doubles as the public pitch, no separate
             marketing layer) recorded as CANDIDATE — first appearance,
             needs 2 more recurrences before it earns a token.
DOCTRINE:    1 clause added — Ledger-Is-The-Marketing: a public surface that
             quotes price/doctrine/manifest must source those fields from
             the same constant the private/internal code uses; no
             hand-copied duplicate content on the public page.
WORDS:       this report ~520 words — first LOT-FM-001 report, no trend yet.
CANDIDATES:  OPEN-TAB (see above). RATION-MANIFEST-PIN (build-time assertion
             that a manifest array matches an expected count) — provisional,
             one recurrence.

--------------------------------------------------------------------------------
07 // PUSH
--------------------------------------------------------------------------------
COMMIT:      BENCHMARK: PRODUCT — Basics Tab / LOT-FM-001 BASIC ration, Month 1 [Claude]
HASH:        255c06a4
BRANCH:      claude/beautiful-johnson-4dkv61
TAG:         benchmark-20260803-01
PUSH:        origin/claude/beautiful-johnson-4dkv61 PUSHED

--------------------------------------------------------------------------------
08 // POST-PUSH VERIFICATION
--------------------------------------------------------------------------------
RESULT:      GREEN
NOTES:       Month 1 EXIT condition met: a stranger with no LOT account can
             load /basics and read the full 23-item ration ledger, the
             doctrine, and the $100/month price line — read-only, no auth,
             no upsell. Months 2 (UPGRADE + roster) and 3 (fulfillment) are
             explicitly NOT built this session — scaffolding only
             (RationStrengthStatus type). Next session picks up Month 2 per
             the directive's own 90-day envelope.

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260803-01
================================================================================
