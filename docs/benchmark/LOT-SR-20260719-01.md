================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260719-01
TITLE:    Build LOT Email — /email Command in Log, Live Delivery via Sync
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  a2e47ff
BRANCH:   claude/determined-turing-u8slgq
DATE:     2026-07-19
TIME:     04:24 UTC
RESULT:   GREEN
================================================================================

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        Request — design/build/deploy the simplest LOT Email system:
                  compose via a "/email to Hitomi" command in Log, new messages
                  appear in Sync, incorporate with Lot Chat (Sync) and Cohort
                  Dating through LOT Community.
CLASSIFICATION:  ENGINEERING
ACTION TAKEN:    Researched live repo (Log/Sync/Chat/Community architecture),
                  found neither "LOT Community" nor "Cohort Dating" nor a
                  "Hitomi" persona exist anywhere in the codebase. Built LOT
                  Email as a first-class, recipient-agnostic primitive instead
                  of faking an integration that has no target yet: a shared
                  regex command parser, a new `lot_emails` table, server-side
                  parse-on-save wired into the existing Log endpoints, live
                  delivery over the existing Sync SSE channel, and an inbox
                  section rendered inside Sync next to Lot Chat.
ROUTED TO:       Source lives in src/ + migrations/ (code artifact, not a
                  document — no docs/ routing applies beyond this report).
                  Session report -> docs/benchmark/ (existing convention,
                  confirmed by ORIENT; this run also bootstraps
                  docs/assembly/LOT-LEDGER.md, LOT-LEXICON.md, LOT-DOCTRINE.md,
                  which did not exist before this session).

--------------------------------------------------------------------------------
02 // CHECK A  (pre-build baseline)
--------------------------------------------------------------------------------
COMMAND                                                RESULT
-------                                                ------
git stash -u (full tree, incl. new files) + rerun       clean tree confirmed
npx tsc --project tsconfig.server.json --noEmit          PASS (zero errors)
git stash pop                                            restored cleanly

--------------------------------------------------------------------------------
03 // BUILD
--------------------------------------------------------------------------------
COMMAND:    npx esr ./scripts/build/client.build.ts -prod
            npm run server:build  (tsc --project tsconfig.server.json && fix-esm-imports)
RESULT:     OK
NOTES:      Client bundle built via the repo's real build gate (esbuild-based
            esr script — does not type-check by design). Server build ran the
            actual tsc compile + ESM import fixups the deploy path uses.
            npm dependency install required --legacy-peer-deps (pre-existing
            @nanostores/react vs nanostores peer conflict, unrelated to this
            change). yarn install failed — registry.yarnpkg.com is proxy-
            policy-denied (403) in this sandbox; registry.npmjs.org is
            allow-listed, so npm was used instead.

--------------------------------------------------------------------------------
04 // CHECK B  (green gate)
--------------------------------------------------------------------------------
COMMAND                                                RESULT
-------                                                ------
npx tsc --project tsconfig.server.json --noEmit          PASS (zero errors)
npx esr ./scripts/build/client.build.ts -prod             PASS ("Client build completed")
npm run server:build                                      PASS
Full migration history + new migration, live Postgres 16  PASS (all 22 files,
  (local scratch DB, umzug, dropped after)                 incl. this session's,
                                                            applied clean; new
                                                            table/index cols
                                                            introspected and
                                                            match exactly)
Manual flow exercise vs. live DB (parseEmailCommand +      PASS (4/4 cases —
  Sequelize create/query, 4 cases incl. literal task        see FIX LOG)
  example "/email to Hitomi.")
GATE:                 GREEN
FIX LOG:              none required — no red state encountered in this run.
NOTE:                 repo-wide `npx tsc --project tsconfig.json --noEmit`
                       (client+server together) has ~60 pre-existing errors
                       unrelated to this change (confirmed via the CHECK A
                       baseline run above, before any of this session's edits
                       existed). That full-repo strict check is NOT the repo's
                       actual build gate — client:js:build (esbuild) is — so
                       it is reported here for visibility only, not treated as
                       red. Three of those pre-existing errors sit on the
                       exact lines this session touched in Sync.tsx (implicit
                       `any` on `sync.listen` callbacks); traced to a
                       pre-existing type mismatch between the generic
                       EventEmitter<T> used at runtime (src/utils/event-
                       emitter.ts) and the non-generic EventEmitter this
                       tsconfig's path-alias resolves '#shared/utils' to
                       (src/shared/utils/index.ts) — present on every existing
                       sync.listen() call site (chat_message, chat_message_
                       like) before this session, not introduced by it.

--------------------------------------------------------------------------------
05 // FILES CHANGED
--------------------------------------------------------------------------------
PATH                                                          STATUS
----                                                           ------
migrations/20260719120000_add-lot-emails.cjs                  ADDED
src/shared/utils/emailCommand.ts                               ADDED
src/server/models/lot-email.ts                                 ADDED *
src/shared/utils/index.ts                                      MODIFIED
src/shared/types/index.ts                                      MODIFIED
src/shared/constants/index.ts                                  MODIFIED
src/server/models/index.ts                                     MODIFIED
src/server/routes/api.ts                                       MODIFIED
src/client/queries.ts                                           MODIFIED
src/client/components/Sync.tsx                                  MODIFIED
src/client/components/Logs.tsx                                  MODIFIED
docs/assembly/LOT-LEDGER.md                                    ADDED (bootstrap)
docs/assembly/LOT-LEXICON.md                                    ADDED (bootstrap)
docs/assembly/LOT-DOCTRINE.md                                   ADDED (bootstrap)
docs/benchmark/LOT-SR-20260719-01.md                            ADDED (this file)

* FLAG: root .gitignore line 50 (`server/`, no leading slash — intended for
  compiled dist/server/ output) also matches src/server/ at any depth. A new
  file placed under src/server/ is silently excluded by plain `git add -A`;
  `git add -f` was required for src/server/models/lot-email.ts. No root-level
  `server/` directory exists (verified — dist/server/ is the real target), so
  this is a live repo bug, not intentional. Not fixed in this session (out of
  the requested scope); logged as a doctrine candidate in LOT-DOCTRINE.md for
  a future session to anchor the pattern (e.g. `/dist/server/`) if rediscovered.

--------------------------------------------------------------------------------
06 // SELF-ASSEMBLY
--------------------------------------------------------------------------------
LEDGER:          appended  -> docs/assembly/LOT-LEDGER.md (first entry — file
                  did not exist before this session; created per bootstrap)
LEXICON:         0 tokens reused, 0 minted  rev — (bootstrap, no clauses/tokens
                  existed yet; see LOT-LEXICON.md note on deferred corpus mining
                  of pre-existing informal notation, e.g. QIE/Arch39/J36/PPEAK/
                  RMOM/INCEP seen in commit 8466b1b, not yet audited)
DOCTRINE:        rev A, 0 clauses folded (bootstrap). 1 candidate logged
                  (gitignore `server/` shadow) pending a second independent
                  occurrence before folding, per minting rule.
COMPRESSION:     ~650 raw words (this report's net-new findings in full prose)
                  -> ~650 retained words (no prior lexicon/doctrine existed to
                  cite against) — ratio 1.0:1. Honest baseline: this is session
                  one of the mechanism: nothing to compress against yet. Ratio
                  should trend upward starting with the second Benchmark run
                  that can cite this session's ledger line instead of restating
                  it.

--------------------------------------------------------------------------------
07 // PUSH
--------------------------------------------------------------------------------
COMMIT:          BENCHMARK: ENGINEERING — LOT Email: /email in Log, live in Sync [VM]
HASH:            (assigned after commit)
TAG:             benchmark-20260719-01   (first Plan-B rollback target — none
                  existed on this repo/remote before this session)
REMOTE:          origin/claude/determined-turing-u8slgq  PUSHED

--------------------------------------------------------------------------------
08 // POST-PUSH VERIFICATION
--------------------------------------------------------------------------------
RESULT:          GREEN
NOTES:           LOT Email is deliberately scoped to the simplest viable slice:
                  single-token recipient names, no compose UI beyond typing the
                  command into an existing Log entry, no retry/re-resolution
                  job for 'unresolved' emails whose recipient doesn't exist
                  yet. Sync's inbox section inherits Sync's existing tag gate
                  (Admin/RND/Usership/Onyx/Legacy) rather than opening a new
                  access surface — meaning a recipient without one of those
                  tags won't see their inbox in the UI yet even though the row
                  is delivered server-side (status stays 'delivered', GET
                  /api/emails still returns it to them via curl/API). Next
                  session: decide whether Email should get its own access tier
                  independent of chat, and mine the historical benchmark corpus
                  for the lexicon per the LOT-LEXICON.md note above.

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260719-01
================================================================================
