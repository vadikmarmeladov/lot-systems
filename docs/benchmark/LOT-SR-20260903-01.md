================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260903-01
TITLE:    Widget health scan — wiring, formatLog gaps, visibility-gate, lag
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  benchmark-20260805-01
BRANCH:   claude/charming-albattani-x8bdzk
DATE:     2026-09-03
TIME:     03:50 UTC
RESULT:   GREEN
================================================================================

--------------------------------------------------------------------------------
00 // PREFLIGHT
--------------------------------------------------------------------------------
REPO:        OK        DOCS WRITABLE: OK
REMOTE:      origin    REACHABLE
LAST GREEN:  benchmark-20260805-01
ENV NOTE:    node_modules absent on session start (fresh container) — installed
             via `npm install --legacy-peer-deps` (yarn blocked: registry.
             yarnpkg.com 403 at the proxy gateway; npmjs.org allowlisted).

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        S-2 scheduled routine — full widget health scan across wiring,
                 data storage, LOT AI / System / Portrait, QIE / Memory / Story,
                 widget->log context sync, the 15-month UI reveal buildup, load
                 speed, lag, and platform widget-usage logging.
CLASSIFICATION:  ENGINEERING
ACTION TAKEN:    Full wiring inventory (37 widgets) + log-sync/perf audit via
                 two parallel Explore agents; 4 concrete bugs fixed; 1 gap
                 flagged (judgment boundary, not fixed); 1 false alarm ruled out.
ROUTED TO:       docs/benchmark/   (report); code fixes committed on this branch

--------------------------------------------------------------------------------
02 // CHECK A        03 // BUILD        04 // CHECK B (gate)
--------------------------------------------------------------------------------
COMMAND                              A        B
-------                              --       --
npm run server:build (tsc typecheck) FAIL*    PASS
npm run client:build (esbuild+css)   PASS     PASS
BUILD:                                npm run build -> OK
GATE:                                 RED->fix-and-recheck(1)->GREEN
FIX LOG:                              *False alarm, not a real fix: CHECK A's
                                       first tsc run used a global/cached tsc
                                       6.0.2 (no local node_modules yet) and
                                       reported ignoreDeprecations:"5.0" as
                                       invalid, suggesting "6.0". Installing
                                       deps resolved the project's pinned
                                       TypeScript 5.9.3, which requires "5.0"
                                       (the value already in tsconfig.server.
                                       json). Net diff on that file: none.

--------------------------------------------------------------------------------
05 // WIDGET WIRING INVENTORY (37 components: 36 *Widget*.tsx + QuantumEngineWidgets)
--------------------------------------------------------------------------------
RESULT: all 37 are imported AND rendered as JSX — zero orphaned widget files.
All 37 render exclusively inside System.tsx (imports L30-81, JSX L444-1062).

LOT AI          AIFeedbackWidget + ContextualPromptsWidget, System-tab only.
                No `aiEngineManager` module exists in code (only in About.tsx
                marketing prose) — the real fallback chain lives in
                src/server/utils/ai-engines.ts, selected server-side.
SYSTEM TAB      Renders all 37 widgets. The sole surface that does.
PORTRAIT        No Portrait.tsx/Profile.tsx file exists. Settings.tsx is the
                de facto profile page (`useMyMemoryStory()` -> GET
                /api/memory/story, Settings.tsx:34,599-630, explicit copy:
                "the system builds your portrait from what you share") but
                renders GrowthMilestones + BadgeUnlockFeed only — ZERO of the
                37 *Widget*.tsx components. Widget-driven insight never
                reaches the profile surface; flagged, not fixed (design
                question, not a mechanical bug).
QIE             QuantumEngineWidgets/QuantumStateWidget/PatternRecognitionWidget
                read intentionEngine store; QuantumSignWidget reads stores.me;
                QuantumRandomWidget is self-contained. All System-tab only.
MEMORY          MemoryWidget: GET /api/memory, POST /api/memory/answer ->
                writes Log event 'answer'/'medical_record'/'weekly_summary_
                response' (api.ts:2420-2463). See bugs below — one of those
                three event names had no formatLog() case.
STORY           NarrativeWidget is client-side flavor text only. The real
                composed story is server-generated (story-generator.ts ->
                generateMemoryStory) and surfaces in Settings/"Portrait", not
                in any widget — a second instance of the Portrait gap above.

--------------------------------------------------------------------------------
05b // WIDGET-TO-LOG SYNC ("military communications" Log)
--------------------------------------------------------------------------------
Log system confirmed real: Logs.tsx (journal UI) + logTriggers.ts (14 slash
commands: /synth /radio /night /prayer /freeze /scan /qi /assembly /silent
/breathe /fast /qos /phys /sil) + GET/POST /api/logs. Every log entry is
auto-stamped with weather + astro/time context server-side (getLogContext,
logs.ts:25) — ambient context sync is real and already wired, not a gap.
`usersOnline` presence is a live SSE-backed store (state.ts:19) surfaced in
System.tsx/About.tsx and consumed by the ambient sound engine; correctly NOT
logged per-widget since it's global ambient state, not a per-action event.

LOGS TO /api/logs:      PlannerWidget, RecipeWidget, CalendarWidget.
PASSIVE (correctly quiet): TimeWidget, SystemPulseWidget, MonthlyPulseWidget,
                        CosmicUpdateWidget.
GAP (flagged, not fixed): IntentionsWidget never calls useCreateLog — it calls
                        recordSignal() into the client-only intentionEngine
                        store. But hasIntention/todayIntentionCount
                        (useLogContext.ts:100,174) and the CQGS directive
                        generator read Log rows with event:'intention', which
                        are written only server-side by scheduled-jobs.ts/
                        admin-api.ts — never by the widget. A user can set an
                        intention all day and system context still reads "no
                        intention set." Two legitimate fixes exist (log the
                        event client-side, or read the QIE store instead) and
                        picking one changes analytics elsewhere — judgment
                        boundary, surfaced to S-2 rather than guessed.

--------------------------------------------------------------------------------
05c // PLATFORM WIDGET-USAGE LOG
--------------------------------------------------------------------------------
No dedicated widget-usage/analytics log exists (no per-widget view/click
instrumentation, no Mixpanel/Amplitude/PostHog-style event stream). The
platform's only "data log of all widget usage" is the life-event Log above,
which records what widgets WRITE (intentions, plans, recipes, calendar
entries), not that a widget was viewed or clicked. Honest finding, not a bug:
recording it as fact rather than inventing a usage-analytics feature that
does not exist.

--------------------------------------------------------------------------------
05d // 15-MONTH UI REVEAL / BUILD-UP
--------------------------------------------------------------------------------
No literal 15-calendar-month reveal schedule exists in docs or code. The real
mechanism is InterfaceEvolutionWidget + evolution.ts (CQGS): feature unlocks
(advancedMemory, plannerTemplates, customThemes, widgetArrange, ...), layout
density (5-tier: breathable->instrument), and badge tiers are all driven by
`calculateEvolutionState(achievements, level, streak, totalEntries, ...)` —
activity-earned, not calendar-gated. This is the closest real analog to the
described "month-to-month UI reveal" and it is functioning (wired into
System.tsx, computed reactively). Recorded as PROVISIONAL mapping, not a
confirmed literal 15-month schedule — none was found to audit against.

--------------------------------------------------------------------------------
05e // LOADING SPEED / LAG
--------------------------------------------------------------------------------
queries.ts still matches the SR-20260719-01 fix (staleTime 2-60min,
refetchInterval 120s + refetchIntervalInBackground:false, useLogs staleTime
5min, GET /logs limit 500 — no regression). SystemProgressWidget,
ChakraErgonomicsWidget, ContextualPromptsWidget, SystemPulseWidget all still
gate their setInterval on document.hidden/isRouteActive.

BUG FOUND: QuantumRandomWidget was the one widget left ungated — a 1s tick
(x2, via useQuantumNumber called twice) and a 10s tick ran continuously in
background/hidden tabs. The 1s tick also calls recordSignal() on every
countdown expiry (1-72s cadence) — local-store + localStorage write only, no
network, but still unnecessary background churn and the sole visibility-gate
gap in the widget set. FIXED (see 05f).

public/sw.js CACHE_VERSION is 2026-07-25 (~40 days stale) but JS/CSS already
use network-first fetch, so it isn't serving stale bundles — not a bug.
badges.ts has two duplicate object keys (quarter_drop, elixir_found) flagged
by esbuild — pre-existing, unrelated to widgets, out of scope this session.

--------------------------------------------------------------------------------
05f // BUGS FIXED (this session)
--------------------------------------------------------------------------------
B1 FORMATLOG GAP     question-generator.ts:844-845 — formatLog() case
                     'answer'/'medical_record' extended to include
                     'weekly_summary_response', which api.ts:2443-2444 writes
                     but which had no case. Fell to default (empty body),
                     filtered out by buildPrompt()'s .filter(Boolean) —
                     silently invisible to the AI. Confirms existing doctrine
                     clause (Widget->Memory Compression Loop, rule 1) with a
                     live instance.
B2 DEAD EVENT FILTER api.ts:5533 (/story endpoint) filtered selfCareLogs on
                     Log.event === 'memory_answer' — no writer anywhere
                     produces that event name (writers use 'answer'/
                     'medical_record'/'weekly_summary_response'). Also
                     matched 'self_care_checkin'/'energy_checkin', neither of
                     which any writer produces either. Narrowed to
                     'medical_record' — the one real event whose metadata
                     shape (question+answer) matches what the map() below it
                     expects; the others would always have produced empty
                     strings and been filtered anyway.
B3 LOGS.TSX DISPLAY  Logs.tsx:1894-1905 rendered a 'weekly_summary_response'
                     block reading metadata.weekNumber/metadata.response —
                     neither field is ever written (writer uses questionId/
                     answerId/question/options/answer, same shape as
                     'answer'). Every weekly-summary entry in the Log feed
                     rendered as an empty "MEM [W-]:" block. Now renders the
                     real question+answer fields, same pattern as the
                     sibling 'answer'/'medical_record' blocks.
B4 VISIBILITY GATE   QuantumRandomWidget.tsx — both setInterval ticks (1s,
                     10s) gated on document.hidden, matching the convention
                     established across 4 other widgets since SR-20260719-01.

--------------------------------------------------------------------------------
05g // FILES CHANGED
--------------------------------------------------------------------------------
PATH                                                     STATUS
src/server/utils/memory/question-generator.ts            MODIFIED
src/server/routes/api.ts                                 MODIFIED
src/client/components/Logs.tsx                           MODIFIED
src/client/components/QuantumRandomWidget.tsx             MODIFIED
docs/benchmark/LOT-SR-20260903-01.md                     ADDED
docs/benchmark/LOT-LEDGER.md                             MODIFIED
docs/benchmark/LOT-DOCTRINE.md                            MODIFIED

--------------------------------------------------------------------------------
06 // SELF-ASSEMBLY
--------------------------------------------------------------------------------
LEDGER:      appended (20260903-01)
LEXICON:     0 minted. 1 reused (GREEN-GATE). WRITER-READER-DRIFT (event/field
             name silently diverges between writer and reader; 3 instances
             this session — B1, B2, B3) recorded as CANDIDATE, PROVISIONAL —
             first-named appearance, earns a token only on 3+ recurrence.
DOCTRINE:    rev N->O. Widget->Memory Compression Loop extended with a live
             B1 citation. New clause folded: Interval Visibility Gate (5th
             established instance across the widget set, first named as its
             own clause).
WORDS:       1359 (median 540) — trend ↑ (widget-scan breadth this session;
             expect ↓ on the next narrower-scope session per compression
             trend).

--------------------------------------------------------------------------------
07 // PUSH
--------------------------------------------------------------------------------
COMMIT:      BENCHMARK: ENGINEERING — widget health scan: formatLog/log-filter/
             display fixes, visibility-gate [VM]
HASH:        (pending — see git log)
TAG:         benchmark-20260903-01
PUSH:        origin/claude/charming-albattani-x8bdzk PUSHED

--------------------------------------------------------------------------------
08 // POST-PUSH VERIFICATION
--------------------------------------------------------------------------------
RESULT:      GREEN
NOTES:       Portrait-surface gap (05, 05b) and the IntentionsWidget->Log
             'intention' gap (05b) are both real, both left unfixed by
             design — they require a product decision (what belongs on the
             profile page; which system is authoritative for "intention
             set" state) rather than a mechanical correction. Surfacing both
             to S-2 for a call before the next widget-touching session.

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260903-01
================================================================================
