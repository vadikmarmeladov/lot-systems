================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260903-01
TITLE:    BASICS TAB — LOT-FM-001 MONTH 1 REBUILT ON CURRENT MASTER
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  98971f20
BRANCH:   claude/beautiful-johnson-c4imn2
DATE:     2026-09-03
TIME:     01:05 UTC
RESULT:   GREEN
================================================================================

--------------------------------------------------------------------------------
00 // PREFLIGHT
--------------------------------------------------------------------------------
REPO:        OK        DOCS WRITABLE: OK
REMOTE:      origin    REACHABLE
LAST GREEN:  NONE (no benchmark-* tags exist on this checkout's tag list)

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        LOT-FM-001 / SELF-ASSEMBLY DIRECTIVE, MODULE: BASIC (RATION) —
                 90-DAY BUILD. Scheduled task: build the (Basics) tab, the
                 hardware/physical layer of the LOT System. Doctrine, 23-item
                 load, visual spec, OPEN TAB and UPGRADE state machine to be
                 sourced from the manual; no such manual text existed in the
                 repo, so nomenclature/cadence for the 23-item load is drafted
                 here and marked DRAFT pending supplier confirmation.
CLASSIFICATION:  SELF-ASSEMBLY (feature build against a house-style directive)
ACTION TAKEN:    Checked docs/assembly + docs/benchmark + LOT-MANIFEST.md for
                 prior BASICS progress before assuming Month 1 was unstarted
                 (per directive's own instruction). Found a MANIFEST row —
                 "Basics Tab | beautiful-johnson-56p7ov | 6815f550 | BEST" —
                 already claiming Month 1 done. Read that commit in full
                 before building anything (see 03 // BUILD for what it found).
                 Built Month 1 fresh against current master, folding in the
                 one design lesson the stale branch's mistake taught.
ROUTED TO:       src/client/, src/server/, docs/benchmark/ (existing folders,
                 no fallback needed)

--------------------------------------------------------------------------------
02 // CHECK A        03 // BUILD        04 // CHECK B (gate)
--------------------------------------------------------------------------------
PRE-EXISTING STATE: node_modules was absent (fresh container). `npm ci` hit
an unrelated peer-dependency conflict (nanostores 0.9.5 vs @nanostores/react's
^0.7.0 peer range); resolved with --legacy-peer-deps, matching how this repo's
own mixed npm/yarn lockfiles already tolerate that mismatch elsewhere. `yarn
install` failed outright — the agent network policy blocks registry.yarnpkg.com
while allow-listing registry.npmjs.org directly, so npm is the working
installer in this environment. Neither is a defect in this session's changes.

STALE-BRANCH FINDING (before writing code): beautiful-johnson-56p7ov's Month 1
commit (6815f550, 2026-06-12) is a real, working Basics tab — but it: (a) sits
~150K lines behind current master (that branch's full diff against master
touches 317 files; the single Basics commit itself only touches 6, so it was
inspected directly rather than judged by the branch's overall staleness), and
(b) wired /basics only as an authenticated client-side SPA route inside
entries/app.tsx. app.tsx's own getMe() failure path redirects to /login, and
/basics was never added to the server's KNOWN_CLIENT_ROUTES or given its own
route — so a logged-out stranger hitting /basics got a 404, not a ledger. That
directly contradicts the directive's Month 1 EXIT bar ("a stranger can read
what LOT issues") and its doctrine ("no layer between public and manifest").
Folded into LOT-DOCTRINE.md (Manifest Hygiene) as a citation rather than
copied forward.

BUILD (this session, fresh, on current master):
- src/client/components/Basics.tsx — standalone-capable ledger component.
  Self-contained scoped stylesheet (`.lot-ration` prefix): LiberationMono/
  Courier fallback stack, bold weight, fixed white/black palette independent
  of the app's acc/bac theme tokens (deliberate — the ration module carries
  its own fixed identity per directive, not the app's rounded/themed one),
  2px rule borders, zero border-radius, inversion-only section heads (black
  bg / white text). Renders: doctrine statement (3 short paragraphs), price
  line (USD 100.00/mo), the 23-item manifest as a NO/NOMENCLATURE/CADENCE
  table (COGS withheld, footnoted DRAFT — PENDING SUPPLIER CONFIRMATION),
  margin envelope line (≤$40 landed, ≥60% margin, verified at Month 3), terms
  list (Usership→Basic upgrade path, Stand Down, read-only notice), and a
  live status-line bar (SYSTEM/DOCUMENT/MODULE/STATUS/STAMP fields, ticking).
- src/client/entries/basics.tsx — standalone entry, mirrors about.tsx/
  status.tsx: `render(<Basics />)`, no Layout/router dependency.
- scripts/build/client.build.ts — added the basics.tsx entry point.
- src/server/index.ts — `fastify.get('/basics', ...)` registered at the same
  top-level, pre-auth-hook position as the existing `/status` route: always
  public, no req.user branching, matching the one pattern in this codebase
  that is actually proven to bypass login. This is the fix for the stale
  branch's gap — a stranger hitting /basics now gets the ledger, live.
- src/client/stores/router.ts, src/client/components/ui/Layout.tsx,
  src/client/entries/app.tsx — added a client-side `basics` SPA route so a
  logged-in user gets the tab-switch experience the directive describes
  ("when the user clicks on the tab") from inside the app shell, reusing the
  same Basics component — one manifest, two surfaces, no data duplication.
  Known rough edge, not fixed this session: a logged-in user who hard-refreshes
  while on /basics hits the server's public route and loses app chrome for
  that load (same class of gap KNOWN_CLIENT_ROUTES already has for any path
  not in its list). Acceptable for Month 1's read-only bar; worth closing in
  Month 2 alongside the real auth-aware roster work.
- docs/benchmark/LOT-MANIFEST.md — Basics Tab row repointed to this branch,
  old row marked superseded with a one-line reason (not deleted — the old
  branch and commit remain the provenance record).
- docs/benchmark/LOT-DOCTRINE.md — Manifest Hygiene clause got a fourth
  citation: verify a BEST branch against current master before reusing it,
  not just its MANIFEST summary line.

COMMAND                            RESULT
-------                            ------
npm ci --legacy-peer-deps          OK (702 packages; pre-existing peer conflict
                                    worked around, not introduced by this diff)
npx run-p client:css:build
  client:js:build                  OK — app.js and basics.js both emit; only
                                    warnings are two pre-existing duplicate
                                    object keys in badges.ts (unrelated)
npx tsc --project
  tsconfig.server.json             OK — zero errors
node scripts/fix-esm-imports.js    OK
GATE:                               GREEN
FIX LOG:                            none required — clean build first pass
NOT RUN: full server boot / curl smoke test. This container has no configured
Postgres (sequelize dialect is hard-set to postgres; .env.example expects a
managed DB host) and provisioning one was out of scope for this session. The
/basics route was verified by static inspection against the exact pattern
/status already uses successfully in production, plus a clean build — not by
a live HTTP round-trip. Flagged here rather than glossed over.

--------------------------------------------------------------------------------
05 // FILES CHANGED
--------------------------------------------------------------------------------
PATH                                          STATUS
src/client/components/Basics.tsx              ADDED
src/client/entries/basics.tsx                 ADDED
scripts/build/client.build.ts                 MODIFIED
src/server/index.ts                           MODIFIED
src/client/stores/router.ts                   MODIFIED
src/client/components/ui/Layout.tsx           MODIFIED
src/client/entries/app.tsx                    MODIFIED
docs/benchmark/LOT-MANIFEST.md                MODIFIED
docs/benchmark/LOT-DOCTRINE.md                MODIFIED

--------------------------------------------------------------------------------
06 // SELF-ASSEMBLY
--------------------------------------------------------------------------------
LEDGER:      appended
LEXICON:     0 reused, 0 minted — OPEN TAB / ON STRENGTH / STAND DOWN / RATION
             are first appearances in this corpus; per lexicon rule a token
             mints only after 3+ prior reports or two doctrine folds. Tracking
             starts now; expect a mint on the Month 2 report if the vocabulary
             holds.
DOCTRINE:    Manifest Hygiene (rev unchanged, citation appended)
WORDS:       1179 (median 576) — trend ↑ (stale-branch inspection findings
             folded in full; standard build reports run shorter)

--------------------------------------------------------------------------------
07 // PUSH
--------------------------------------------------------------------------------
COMMIT:      BENCHMARK: SELF-ASSEMBLY — Basics Tab LOT-FM-001 Month 1 rebuilt on current master [VM]
HASH:        57625e70
TAG:         benchmark-20260903-01
PUSH:        origin/claude/beautiful-johnson-c4imn2 PUSHED

--------------------------------------------------------------------------------
08 // POST-PUSH VERIFICATION
--------------------------------------------------------------------------------
RESULT:      GREEN
NOTES:       For the next session (Month 2 — UPGRADE & ROSTER): (1) build the
             USERSHIP/AI → PENDING → ON STRENGTH → STEADY STATE state machine
             and wire it into the in-app Basics tab (the standalone /basics
             page should stay read-only/public — do not add auth-only UI to
             it, or Month 1's doctrine bar regresses); (2) resolve the hard-
             refresh-on-/basics rough edge noted in 03 // BUILD, ideally as
             part of the same auth-aware pass; (3) confirm real supplier
             quotes against the manifest before Month 3 — the 23-item load
             in Basics.tsx is explicitly DRAFT; (4) this branch's Basics Tab
             MANIFEST row is BEST, not SHIPPED — no Ship Mode run has moved
             it to master yet, that is a separate, explicit trigger.

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260903-01
================================================================================
