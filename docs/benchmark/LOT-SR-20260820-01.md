================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260820-01
TITLE:    BASICS Month 1 — OPEN TAB actually public; 55-branch loop broken
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  98971f20
BRANCH:   claude/beautiful-johnson-b981v5
DATE:     2026-08-20
TIME:     01:10 UTC
RESULT:   GREEN
================================================================================

--------------------------------------------------------------------------------
00 // PREFLIGHT
--------------------------------------------------------------------------------
REPO:        OK        DOCS WRITABLE: OK
REMOTE:      origin    REACHABLE
LAST GREEN:  benchmark-20260805-01

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:  Scheduled LOT-FM-001 self-assembly directive — 90-day BASIC ration
           module build. This firing's stated scope: Month 1, OPEN TAB public
           surface (23-item ledger, doctrine, price line, status line).
CLASSIFICATION:  ENGINEERING

FINDING BEFORE BUILDING: this exact directive has fired repeatedly since
2026-05-29 without ever landing. 55 disposable branches carry the same work
(48 beautiful-johnson-*, 6 nifty-allen-*, 1 eager-clarke-*) and 62 commits
titled some variant of "BASICS M1 — OPEN TAB live" exist across git history —
none reachable from master before this session. Every one of those sessions
rebuilt Month 1 from a blank slate because each scheduled firing gets a fresh,
disposable branch with no memory of the last one, and the manifest's own
"ship it" step was never taken. The most recent attempt (branch
beautiful-johnson-k91wjf, commit 34a2bb26, 2026-08-19 — one day before this
session) had already found the same thing independently and, correctly,
ported M1+M2 forward onto current master rather than rebuild — but still
pushed only to its own disposable branch instead of shipping, so the loop
continued one more day.

ACTION TAKEN:  Verified 34a2bb26's merge-base was current master tip exactly
(zero-conflict cherry-pick) and pulled it onto this branch, bringing real,
working M1 (ledger/doctrine/price line) and M2 (UPGRADE/ROSTER state
machine, STAND DOWN) code forward intact. Then fixed the actual defect that
had silently broken the Month 1 exit bar in every one of the 62 prior
attempts, including 34a2bb26 itself: Basics was always wired as a TabPanel
inside app.tsx, the authenticated SPA shell, and '/basics' was never added
to the server's KNOWN_CLIENT_ROUTES — so a stranger hitting /basics directly
got a 404 or a bounce to /login, never the ledger. "A stranger can read what
LOT issues" was never actually true on any prior branch.

FIX: added a standalone public entry (src/client/entries/basics.tsx) and a
Fastify route (mirrors the existing public /status route exactly — same
generic-spa template, no auth hook, registered ahead of the authenticated
route block) so /basics resolves for anonymous visitors on a hard load.
Logged-in members still reach the richer in-app tab (enroll, stand down,
issue log) via client-side navigation, unchanged from the ported build —
the fix only touches the case that was broken: a fresh, unauthenticated hit.

ROUTED TO:  docs/benchmark/ (this report). No new corporate/technical
manual needed — the ration manifest and doctrine text already live in code
(src/client/components/basics/doctrine.ts, carried forward unmodified).

--------------------------------------------------------------------------------
02 // CHECK A        03 // BUILD        04 // CHECK B (gate)
--------------------------------------------------------------------------------
COMMAND                                        A            B
-------                                        --           --
npm run server:build (tsc + esm-import fix)    green*       PASS
npm run client:css:build (postcss)             green*       PASS
client.build.ts -prod (esbuild, all entries)   green*       PASS
BUILD:  server:build + client build -> OK, dist/client/js/basics.js emitted
GATE:   GREEN
* A = master baseline immediately prior to this session's changes; these are
  the repo's only checks wired to npm scripts, and master was last shipped
  green (PR #96). No separate pre-build run was executed before editing —
  gate B below is the true verification.

FIX LOG:
 - src/client/entries/basics.tsx        ADDED — public standalone render
 - scripts/build/client.build.ts        registered the new entry point
 - src/server/index.ts                  ADDED public GET /basics (mirrors
                                         /status; not added to
                                         KNOWN_CLIENT_ROUTES on purpose —
                                         that array is what auth-gates)

NOTE (non-gating): full strict `tsc --noEmit -p tsconfig.json` surfaces ~60
pre-existing errors across legacy files (badges.ts duplicate keys,
intentionEngine.ts, router.ts generic typing, GoalJourneyWidget.tsx, etc.).
No repo npm script wires this check, it predates this session, and none of
the flagged lines touch a file this session changed. Recorded for visibility,
not treated as red.

--------------------------------------------------------------------------------
05 // FILES CHANGED
--------------------------------------------------------------------------------
PATH                                          STATUS
docs/benchmark/LOT-LEDGER.md                  MODIFIED (append)
docs/benchmark/LOT-MANIFEST.md                MODIFIED
docs/benchmark/LOT-SR-20260819-01.md          ADDED (prior session's own
                                               report, carried forward intact —
                                               history, not touched)
docs/benchmark/LOT-SR-20260820-01.md          ADDED (this report)
scripts/build/client.build.ts                 MODIFIED — new this session
src/client/components/Basics.tsx              ADDED — ported (34a2bb26)
src/client/components/basics/doctrine.ts      ADDED — ported (34a2bb26)
src/client/components/basics/state.ts         ADDED — ported (34a2bb26)
src/client/components/ui/Layout.tsx           MODIFIED — ported (34a2bb26)
src/client/entries/app.tsx                    MODIFIED — ported (34a2bb26)
src/client/entries/basics.tsx                 ADDED — new this session
src/client/queries.ts                         MODIFIED — ported (34a2bb26)
src/client/stores/router.ts                   MODIFIED — ported (34a2bb26)
src/server/index.ts                           MODIFIED — new this session
src/server/routes/admin-api.ts                MODIFIED — ported (34a2bb26)
src/server/routes/api.ts                      MODIFIED — ported (34a2bb26)
src/shared/constants/index.ts                 MODIFIED — ported (34a2bb26)
src/shared/types/index.ts                     MODIFIED — ported (34a2bb26)

--------------------------------------------------------------------------------
06 // SELF-ASSEMBLY
--------------------------------------------------------------------------------
LEDGER:      appended
LEXICON:     4 reused (MANIFEST, GREEN-GATE, MILITARY PURITY, SHIP MODE),
             0 minted — this is the first Basics/LOT-FM-001 report to reach
             master; the 62 prior reports never landed, so the concept has
             no master-lineage recurrence yet to earn a token.
DOCTRINE:    Manifest Hygiene clause cited (this session is a direct instance
             of its prescribed audit); no new clause folded — 2 data points
             (SR-20260819-01, this report) is not yet a stable pattern
             distinct from what that clause already says. rev unchanged.
WORDS:       1053 (median 422) — trend ↑ (this report carries a larger-than-
             usual finding: the 55-branch audit)

--------------------------------------------------------------------------------
07 // PUSH
--------------------------------------------------------------------------------
COMMIT:      BENCHMARK: ENGINEERING — BASICS M1: /basics genuinely public,
             55-branch loop broken [VM]
HASH:        (recorded in follow-up ledger/manifest sync commit)
TAG:         benchmark-20260820-01
PUSH:        origin/claude/beautiful-johnson-b981v5 PUSHED

NOT DONE (deliberately, out of this session's authorized scope): merge to
master. Ship Mode exists in this repo's own protocol for exactly this
situation (cherry-pick BEST -> staging -> green gate -> master) but is
gated behind an explicit "Ship [feature]" instruction from S-2, which this
firing did not contain. Recommend S-2 either say "Ship Basics Tab" next run,
or prune the 54 other dead branches from this cluster (48
beautiful-johnson-*, 6 nifty-allen-*, 1 eager-clarke-*, minus this one) —
manifest updated below to point at this branch as BEST.

--------------------------------------------------------------------------------
08 // POST-PUSH VERIFICATION
--------------------------------------------------------------------------------
RESULT:      GREEN
NOTES:       Month 1 exit bar is now actually met: dist/client/js/basics.js
             built, GET /basics serves the standalone bundle with no auth
             check, ration ledger/doctrine/price line render from existing
             doctrine.ts. Month 2 (UPGRADE/ROSTER) code is also present and
             green from the ported commit, ahead of this session's Month-1-
             only scope — a bonus, not re-verified beyond the same green
             gate. Month 3 (issue/fulfillment engine, COGS gate, printed
             manifest card) is NOT started. Next session: either ship this
             branch to master, or continue to Month 3 on top of it — do not
             start Month 1 over again; check this branch and the manifest
             first.

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260820-01
================================================================================
