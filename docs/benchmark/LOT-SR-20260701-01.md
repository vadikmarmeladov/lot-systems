================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260701-01
TITLE:    LOT Pager — Hardware Plan, BOM, Firmware + API Connector Spec
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  benchmark-20260630-01 → 997bc3f
BRANCH:   claude/brave-lamport-r3lwam
DATE:     2026-07-01
TIME:     14:35 UTC
RESULT:   GREEN
================================================================================

--------------------------------------------------------------------------------
00 // PREFLIGHT
--------------------------------------------------------------------------------
REPO:        OK        DOCS WRITABLE: OK
REMOTE:      origin    REACHABLE
LAST GREEN:  benchmark-20260630-01
BRANCH:      ahead of origin/master by 25 commits, 0 behind

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        S-2 hardware brief — 19-point build logic for a physical
                 device connected to the LOT site: PCBWay manufacturing,
                 pager-style AI notifications, 2-part stainless steel body
                 (40x40x5mm), camera, screen, COPY button wired to the Log
                 tab, weather sensor, wireless charging, 100-unit pilot run.
CLASSIFICATION:  HARDWARE / DOCUMENTATION (no application code changed)
ACTION TAKEN:    Researched existing LOT API surface (Log tab, auth, M2M/
                 SYNC vision docs), sourced real component links + costing,
                 authored a 4-document hardware spec set + generated PDF
                 manual, filed under a new docs/hardware/ directory.
ROUTED TO:       docs/hardware/ (new)

--------------------------------------------------------------------------------
02 // CHECK A        03 // BUILD        04 // CHECK B (gate)
--------------------------------------------------------------------------------
This session added documentation and a standalone PDF-generation script only
— no src/ files were modified, so the TypeScript/CSS/JS build pipeline is
unaffected. Verification performed instead:

COMMAND                                              RESULT
-------                                              ------
git status --short (confirm no src/ changes)         PASS — docs/ + 1 script only
node scripts/generate_pager_manual_pdf.cjs           PASS — 4-page PDF, 7.1KB
file docs/hardware/pdf/LOT-PAGER-MANUAL.pdf           PASS — valid PDF 1.3
Cross-check /api/logs claims against source           PASS — api.ts:1414-1438,
  (src/server/routes/api.ts, server.ts)                       server.ts:206-241
GATE:       GREEN
FIX LOG:    (none — no code path touched)

NOTES: pdfkit was installed locally via `npm install pdfkit --no-save
  --legacy-peer-deps` to run the generator (node_modules did not exist in
  this environment). node_modules/ is gitignored and not part of this
  commit.

--------------------------------------------------------------------------------
05 // FILES CHANGED
--------------------------------------------------------------------------------
PATH                                                             STATUS
docs/hardware/LOT-PAGER-SPEC.md                                  ADDED
  - Concept, brand-tension note (pager vs. "no push" doctrine),
    two-part stainless steel industrial design, 5mm height risk,
    Qi-through-steel risk, electronics summary, manufacturing via
    PCBWay, notification loop, AI-grade sensor selection (SGI),
    COPY→Log tab mapping, roadmap (6 phases), open risks

docs/hardware/LOT-PAGER-BOM.md                                   ADDED
  - Real, verified supplier links (Mouser/DigiKey/Adafruit/Arducam/
    PCBWay) for MCU, display, camera, weather sensor, Qi receiver,
    button, battery; per-unit cost roll-up at 100-unit volume;
    Sensor Grade Index scoring table

docs/hardware/LOT-PAGER-FIRMWARE.md                              ADDED
  - Boot sequence, main-loop state machine, display rendering rules,
    weather-sensor batching (session compression), provisioning, OTA,
    power budget targets for Phase 1 measurement

docs/hardware/LOT-PAGER-API-CONNECTOR.md                         ADDED
  - Device pairing + Bearer-token auth branch (parallel to existing
    JWT-cookie hook, server.ts:206-241), greenfield WS/long-poll
    notification transport, exact POST /api/logs payload for the
    COPY button (reuses api.ts:1414 unmodified), telemetry batching
    endpoint, security notes

docs/hardware/pdf/LOT-PAGER-MANUAL.pdf                           ADDED
  - 4-page generated manual: cover, concept + form factor, BOM
    summary table, roadmap + risks

scripts/generate_pager_manual_pdf.cjs                            ADDED
  - pdfkit generator, pattern matched to scripts/generate-badge-
    codex-pdf.cjs

--------------------------------------------------------------------------------
06 // SELF-ASSEMBLY
--------------------------------------------------------------------------------
LEDGER:      appended (see below)
LEXICON:     2 reused (S-2, CUBIQ)
             1 minted: SENSOR GRADE INDEX (SGI) — see below          rev A
DOCTRINE:    1 clause folded: Device-Token Auth Branch (see below)   rev N

LEXICON MINT — SENSOR GRADE INDEX (SGI):
  TOKEN: SENSOR GRADE INDEX (SGI)
  MEANING: 0-100 score (accuracy/power/footprint/availability, 25pts each)
           applied to every off-the-shelf sensor/camera/charging-IC
           candidate before BOM lock — the hardware-side counterpart to
           CQGS's Bioethics Index (a graded signal, not a checkbox)
  REV: A  SINCE: 20260701

DOCTRINE FOLD — Device-Token Auth Branch:
  A hardware device authenticates by adding a parallel `Authorization:
  Bearer <device_token>` check inside the existing onRequest hook
  (server.ts:206-228), setting req.user from a new DeviceToken→User
  lookup — never by replacing or bypassing the cookie-session hook, and
  never by adding a second parallel route tree. Every existing /api/*
  handler (starting with POST /api/logs) then works for a device with
  zero handler-level changes. Any future M2M/hardware auth work should
  extend this same branch, not invent a second auth system.
  (SR-20260701-01: LOT-PAGER-API-CONNECTOR.md section 01.)

WORDS:  612 (prior median 495) — trend ↑ (first hardware-track session;
        four companion documents cross-reference this entry, so length
        reflects breadth of a new track, not scope creep within one doc)

--------------------------------------------------------------------------------
07 // PUSH
--------------------------------------------------------------------------------
COMMIT:  BENCHMARK: HARDWARE — LOT Pager spec, BOM, firmware, API connector + PDF manual [VM]
HASH:    (applied below)
TAG:     benchmark-20260701-01
PUSH:    origin/claude/brave-lamport-r3lwam PUSHED

--------------------------------------------------------------------------------
08 // POST-PUSH VERIFICATION
--------------------------------------------------------------------------------
RESULT:   GREEN
NOTES:    - This is a planning/spec session — no firmware exists yet, no PCB
            has been ordered, no backend auth branch has been written. Every
            "NEW" item in LOT-PAGER-API-CONNECTOR.md is unimplemented by
            design; Phase 1-2 of the roadmap in LOT-PAGER-SPEC.md is where
            code gets written.
          - PCB and CNC-enclosure costs in LOT-PAGER-BOM.md are marked QUOTE
            — real numbers require uploading actual gerbers/CAD to PCBWay's
            quote tools, not assumable from public pricing pages.
          - The single open product-philosophy question (pager notifications
            vs. the "No push. No alert. No interruption." copy in
            src/client/components/About.tsx:4081) is resolved in this spec
            as "silent surface, no alert transducer" — flagging here in case
            S-2 wants a different resolution before Phase 0 starts.

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260701-01
================================================================================
