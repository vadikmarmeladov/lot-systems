<!--
  LOT SYSTEMS CORPORATION
  Vadim Marmeladov — CEO, Owner LOT®
  Kuzya Cosmo Marmeladov — CEO, Owner COSMO®
  LOT® Founded 7 April 2016 | COSMO® Founded 1 July 2024
  Made in the USA | brand.lot-systems.com
-->

```
================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260824-01
TITLE:    Widget health scan — wiring, storage, lag, load speed
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
VERSION:  benchmark-20260805-01 (98971f2)
BRANCH:   claude/charming-albattani-o40p5s
DATE:     2026-08-24
TIME:     03:41 UTC
RESULT:   GREEN
================================================================================

--------------------------------------------------------------------------------
00 // PREFLIGHT
--------------------------------------------------------------------------------
REPO:        OK        DOCS WRITABLE: OK
REMOTE:      origin    REACHABLE
LAST GREEN:  benchmark-20260805-01

ENV NOTE:    node_modules absent on session start. `yarn install` fails —
             registry.yarnpkg.com is 403 at the agent proxy; only
             registry.npmjs.org is on the allowlist. `npm ci` fails ERESOLVE
             (@nanostores/react@0.4.1 peer nanostores ^0.7.0 vs installed
             ^0.9.5). Install path that works in this environment:
             `npm ci --legacy-peer-deps` (702 packages, 9s).
             This is a PRE-EXISTING lockfile conflict, not introduced here.

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        S-2 widget-health program — wiring, data storing, QIE/Memory/
                 Story wirings, lag test, load speed, usage logging
CLASSIFICATION:  ENGINEERING
ACTION TAKEN:    Full 38-widget audit; 2 defects fixed; 1 doc drift corrected;
                 1 content collision HELD for S-2 judgment
ROUTED TO:       docs/technical/   (WIDGETS.md corrected)

--------------------------------------------------------------------------------
02 // CHECK A        03 // BUILD        04 // CHECK B (gate)
--------------------------------------------------------------------------------
COMMAND                           A        B
-------                           --       --
npm run build                     PASS     PASS
tsc -p tsconfig.server.json       PASS     PASS   (0 errors — strict gate)
tsc -p tsconfig.json (client)     104 err  104 err (pre-existing, unchanged)

BUILD:   npm run build -> OK (client 2.48s, server 5.73s)
GATE:    GREEN
DELTA:   0 new type errors. Only diff is a line-number shift of one
         pre-existing RecipeWidget error (261 -> 263) from a 2-line insert.
FIX LOG: (no gate failures — no fix loop required)

NOTE ON CLIENT TSC: the 104 client-side errors are NOT in the build path.
`npm run build` bundles the client with esbuild (no typecheck) and only runs
tsc against tsconfig.server.json. The client tsc figure is tracked here as a
standing debt baseline, not as a gate. Do not "fix to zero" opportunistically;
it is a separate scoped job.

--------------------------------------------------------------------------------
05 // FILES CHANGED
--------------------------------------------------------------------------------
PATH                                          STATUS
src/client/components/IntegrityWidget.tsx     MODIFIED
src/client/components/RecipeWidget.tsx        MODIFIED
docs/technical/WIDGETS.md                     MODIFIED
docs/benchmark/LOT-SR-20260824-01.md          ADDED

================================================================================
WIDGET HEALTH SCAN — FINDINGS
================================================================================

--------------------------------------------------------------------------------
A // WIRING INTEGRITY — GREEN
--------------------------------------------------------------------------------
38 *Widget.tsx files diffed against System.tsx imports AND JSX render sites.

  ORPHANED FILES (exist, never rendered):     0
  IMPORTED BUT UNREACHABLE:                   0
  UNWRAPPED BY WidgetErrorBoundary:           0

Every widget is imported, rendered, and wrapped in a real class-component
error boundary (getDerivedStateFromError + componentDidCatch + mount timing)
in BOTH the free and pro System.tsx layouts. Verified by render-site, not by
import presence alone.

--------------------------------------------------------------------------------
B // QIE WIRING — 1 DEFECT FOUND + FIXED
--------------------------------------------------------------------------------
DEFECT B1 — IntegrityWidget stale-signal bug          SEVERITY: MODERATE  FIXED

IntegrityWidget.tsx never subscribed to the intentionEngine atom. It read
`intentionEngine.get()` inside a useMemo keyed on `[logs]`:

    const integrity = React.useMemo(() => {
      const state = intentionEngine.get()      // <- untracked read
      return analyzeIntegrity(state.signals)
    }, [logs])                                 // <- wrong dependency

recordSignal() writes to the atom independently of the /api/logs query. With
logs staleTime at 5 min, the Verdict/Fractures views could show a reading up
to five minutes stale — every plan_set, intention_set and mood signal landed
in the engine without recomputing the integrity verdict.

Every sibling in the same Biofield stack subscribes correctly:
  QuantumStateWidget:27  PatternRecognitionWidget:49
  SignalStreamWidget:22  AIFeedbackWidget:26
IntegrityWidget was the lone outlier.

FIX: added `const engine = useStore(intentionEngine)`; memo now keys on
`[engine.signals, logs]`. The two render-body `intentionEngine.get()` reads
(timeline + field views) now read `engine` so one render pass is internally
consistent.

--------------------------------------------------------------------------------
C // DATA STORING / USAGE LOGGING — 1 DEFECT FOUND + FIXED
--------------------------------------------------------------------------------
Signal-emission verified per widget against WIDGETS.md claims:

  PlannerWidget:53        plan_set             OK
  IntentionsWidget:83     intention_set        OK
  GoalJourneyWidget:48    goals_viewed         OK
  MemoryWidget:147        answer signals       OK
  RecipeWidget            recipe_viewed        DEFECT -> FIXED

DEFECT C1 — recipe_viewed signal never emitted     SEVERITY: MODERATE  FIXED

`recordRecipeViewedSignal()` (intentionEngine.ts:5372) was fully built and
plumbed: the `recipe_viewed` event is wired into the server displayableEvents
whitelist and into the analytics jobs. But the function had ZERO call sites in
the client — its only repo references were changelog *display strings* inside
SystemProgressWidget. RecipeWidget emitted only `recipe_acknowledged` /
`fasting_acknowledged` on dismiss, never on view.

Net effect: the whole view-side nutrition telemetry path was dark. Downstream
analytics consumed an event that was never produced. Silent, no error.

FIX: RecipeWidget now calls recordRecipeViewedSignal(mealTime, recipe) at the
deduped view moment, inside the existing loggedRecipesRef guard, so it fires
once per meal-time+recipe combo — same cadence as the existing /api/logs row.

DOCTRINE ECHO: this is the SAME failure class as SR-20260630-01 (plan_set
missing from formatLog). A typed helper that exists, compiles, and is
referenced in prose is NOT a wired signal. Build the helper and the call site
in the same commit, or the telemetry is decorative.

--------------------------------------------------------------------------------
D // LAG / POLLING — GREEN, 2026-07-19 FIXES INTACT
--------------------------------------------------------------------------------
Re-verified every fix from SR-20260719-01 against current HEAD:

  queries.ts:136   staleTime 5*60*1000 on logs           INTACT
  queries.ts       refetchInterval 120000 x7 stats blocks INTACT
  api.ts           logs route limit: 500                  INTACT
  System.tsx       nav active-tab before:!hidden          INTACT

NO regressions. No new sub-5s network polling anywhere in src/client.

All remaining sub-5s setInterval calls are UI-only and issue no fetch:
  500ms  loading-dots animation
  1s     QuantumRandomWidget countdown tick
  100ms  breathe-mode animation

SystemPulseWidget (the historical worst offender) is now well-behaved:
10s interval, and it hard-skips the fetch when `document.hidden` OR when the
user is not on the system route — so a backgrounded tab costs zero queries.

--------------------------------------------------------------------------------
E // LOAD SPEED — MEASURED, ONE STANDING RISK
--------------------------------------------------------------------------------
Production bundle, this build:

  app.js                712.8 KB     <- system tab entry
  about.js              431.2 KB
  chunk-PGO3GO7R.js     232.5 KB
  chunk-OZ3TPDLV.js     190.3 KB
  chunk-IYKCWZP6.js     145.3 KB
  index.css              31.2 KB
  (14 further chunks, all < 50 KB)

OBSERVATION (not yet actioned): app.js at 712.8 KB uncompressed is the single
largest lever on first-paint for the System tab. badges.ts alone is 7,500+
lines and is pulled into that entry. A badge-catalog split — lazy-loading the
codex body and keeping only the unlock-check logic in the entry — is the
obvious next optimization. NOT attempted this session: it is a real
architectural change and belongs in its own green-gated pass, not bolted onto
a health scan.

Build times are healthy and not a bottleneck: client 2.48s, server 5.73s.

--------------------------------------------------------------------------------
F // RENDER-BLOCKING — GREEN
--------------------------------------------------------------------------------
The three highest-frequency re-renderers all memoize their sort/filter/map
chains correctly:
  SignalStreamWidget:45-46,83-85    QuantumStateWidget:94,103,111
  SystemPulseWidget (10s + hidden-tab guard)

One unmemoized Object.entries().sort().map() at QuantumStateWidget:199 — its
input is itself memoized and bounded to ~17 signal-source keys. Below the
noise floor. Not actioned, recorded for completeness.

--------------------------------------------------------------------------------
G // HELD FOR S-2 — BADGE ID COLLISION (NOT FIXED BY DESIGN)
--------------------------------------------------------------------------------
The build emits two duplicate-object-key warnings in badges.ts. These are not
cosmetic: the later definition silently overwrites the earlier one, so a badge
S-2 authored is unreachable in the shipped catalog.

COLLISION 1 — `quarter_drop`   REAL CONTENT LOSS
  :3214  ○→◉  "Unlock your first easter egg badge of any type"
               rarity uncommon / category achievement_rpg      <- LOST
  :5501  ¢·¢  "Check in between midnight and 1:00 AM local time"
               rarity rare / category behavioral               <- WINS

  These are two SEMANTICALLY DIFFERENT badges sharing one id. The
  first-easter-egg badge is currently unwinnable. Resolving it means renaming
  one badge id and deciding which keeps the name "Quarter Drop" — that is
  catalog authorship, S-2's call, not a mechanical fix.

COLLISION 2 — `elixir_found`   BENIGN SUPERSET
  :4825  ∘∿∘    trigger: "elixir"                  uncommon, hidden
  :6883  ∘·●·∘  trigger: "elixir"/"the gift"/
                "treasure found"/"the boon"        rare       <- WINS

  Same concept; the winner is a strict trigger superset. Reads as an intended
  consolidation where the old entry was never deleted. Safe to delete :4825,
  but confirm the `hidden: true` drop was deliberate.

PER SKILL JUDGMENT BOUNDARY: intake/classification and catalog content are
judgment, not mechanism. HELD rather than guessed.

--------------------------------------------------------------------------------
H // DOC DRIFT — CORRECTED
--------------------------------------------------------------------------------
WIDGETS.md described SystemPulseWidget as "1-second polling / polled every
second." Code moved to 10s on 2026-07-19 (commit ee88f4c8, "was 1s — reduced
to prevent DB overload") and the doc never followed. Corrected, and the
hidden-tab/route pause behavior — previously undocumented — is now recorded.

The System tab, Portrait profile page and Memory/Story wirings were walked as
part of the 38-widget sweep; no wiring breaks found beyond B1 and C1 above.

--------------------------------------------------------------------------------
06 // SELF-ASSEMBLY
--------------------------------------------------------------------------------
LEDGER:      appended
LEXICON:     1 minted — DARK-HELPER  rev B
             Earned on doctrine fold 2 per the 2+ rule (SR-20260630-01 +
             this report), NOT decreed. Existing tokens reused, none altered.
DOCTRINE:    1 clause folded — "The Dark Helper Rule", generalizing the
             SR-20260630-01 Widget->Memory Compression Loop clause into both
             directions (no producer / no consumer)  rev N (fold, no bump)
WORDS:       1522 (median 541, n=72) — trend ↑
             Longest report to date. Justified: this is a full-surface audit
             carrying measured bundle figures and a HELD item that must brief
             completely. Expect regression toward the median on the next
             narrow-scope pass; if audit reports stay this long, split the
             findings body out and keep the Terminal Grid thin.

--------------------------------------------------------------------------------
07 // PUSH
--------------------------------------------------------------------------------
COMMIT:      BENCHMARK: ENGINEERING — widget health scan: IntegrityWidget QIE
             subscription, recipe_viewed wiring, pulse doc drift [VM]
TAG:         benchmark-20260824-01
PUSH:        origin/claude/charming-albattani-o40p5s PUSHED

--------------------------------------------------------------------------------
08 // POST-PUSH VERIFICATION
--------------------------------------------------------------------------------
RESULT:      GREEN
NOTES:       Three items carried to next session:
             1. HELD: quarter_drop badge id collision needs S-2 authorship
                decision (see G). One badge is currently unwinnable.
             2. app.js 712.8 KB — badge-catalog lazy-split is the next real
                load-speed lever. Own pass, own gate.
             3. Client tsc debt stands at 104 errors, flat. Not in the build
                path. Scope it deliberately or leave it; do not chip at it.
             ENV: this container needs `npm ci --legacy-peer-deps`. Plain
             `yarn install` cannot resolve — yarnpkg registry is proxy-blocked.

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260824-01
================================================================================
```
