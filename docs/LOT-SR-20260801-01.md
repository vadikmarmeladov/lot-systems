================================================================================
LOT SYSTEMS / SESSION REPORT
DOCUMENT: LOT-SR-20260801-01
TITLE:    LOT® Computer — hardware plan, BOM, roadmap, firmware + software spec
CLASS:    RESTRICTED // S-2 EYES
S-2:      VADIK MARMELADOV
BRANCH:   claude/brave-lamport-qksqde
DATE:     2026-08-01
RESULT:   GREEN (docs-only session — no runtime code changed)
================================================================================

--------------------------------------------------------------------------------
00 // PREFLIGHT
--------------------------------------------------------------------------------
REPO:        OK        DOCS WRITABLE: OK
REMOTE:      origin    REACHABLE
BRANCH:      claude/brave-lamport-qksqde — new session, no prior local commits
NETWORK:     Outbound web blocked by this session's network policy for
             brand.lot-systems.com, lot-systems.com, institute.lot-systems.com,
             and every commerce/component vendor host tested (PCBWay,
             Digi-Key, Mouser, Adafruit, SparkFun, LCSC) — confirmed via
             the proxy status endpoint (`connect_rejected`, policy denial,
             all hosts). Disclosed in full in Section 00 of the hardware
             plan document rather than worked around.

--------------------------------------------------------------------------------
01 // INTAKE
--------------------------------------------------------------------------------
ARTIFACT:        Scheduled S-2 directive (automated, no live operator watching
                 this session) — "build a hardware computer connected to
                 the LOT site," a 19-point brief covering PCB fabrication
                 (PCBWay), a pager-style AI notification device, a
                 2-part stainless steel body (one polished face, one
                 instrument face with camera/screen/button), a 40mm x
                 40mm x 5mm silver base plate, a wireless charger, a
                 weather sensor, a "Copy" button wired to the Log tab on
                 lot-systems.com, PDF manuals, firmware/software as
                 separate documents, session compression, and a 100-unit
                 production run.

CLASSIFICATION:  CORPORATE (hardware plan + BOM + roadmap) / ENGINEERING
                 SPEC (firmware + software connector — no code executed,
                 specification only)

ACTION TAKEN:    Read the existing hardware/brand corpus first — the
                 LOT-MANIFEST.md "COSMO Hardware" (brave-lamport-t9z5u8)
                 feature-line entry this branch's name continues, the
                 CUBIQ™ Quantum Cube v.0 spec, the COSMO® robotics ethics
                 register, and the NODE-0 rig-spec BOM convention — then
                 wrote 3 new documents that scope LOT® Computer as a
                 third, textually distinct hardware line (a stationary
                 pager-terminal, not COSMO®'s mobile robot and not
                 CUBIQ™'s hopping notification cube).

ROUTED TO:       docs/corporate/ (plan) + docs/technical/ (firmware,
                 software connector, both per brief item 11 — kept
                 separate)

--------------------------------------------------------------------------------
02 // CHECK A        03 // BUILD        04 // CHECK B (gate)
--------------------------------------------------------------------------------
No source files (.ts/.tsx/.js) were modified this session — 3 new markdown
files only. `npm run build` was attempted for verification and failed at
the dependency-resolution step (`registry.yarnpkg.com` 403 — outside this
session's network allowlist, which is a pre-existing environment
constraint unrelated to any change made here, not a regression). Because
zero executable code changed, the build gate does not apply to this
session's diff; documented here rather than silently skipped.

GATE:  GREEN (docs-only; no code path exists that could be broken)

--------------------------------------------------------------------------------
05 // FILES CHANGED
--------------------------------------------------------------------------------
PATH                                                       STATUS
docs/corporate/LOT_COMPUTER_HARDWARE_PLAN.md              ADDED
  — Physical design (2-part 316L stainless body, polished + instrument
  faces, 40x40x5mm base plate), BOM with component families + vendor
  homepage links, cost rollup (prototype/pilot/100-unit), v0-v2 +
  production roadmap, notification-language table, ethics inheritance
  from COSMO®.
docs/technical/LOT_COMPUTER_FIRMWARE_SPEC.md              ADDED
  — ESP32-S3 platform choice + rationale, pairing/provisioning, display
  state machine, camera pipeline (operator-triggered only, no standing
  capture), weather sensor polling, session-compression buffer, power
  management, OTA + QC test jig, offline behavior, security.
docs/technical/LOT_COMPUTER_SOFTWARE_CONNECTOR.md         ADDED
  — Device pairing endpoints, SSE-based notification push channel
  (reuses existing SSE infra, no new transport), Copy-button -> existing
  POST /logs mapping with the required formatLog() case called out
  explicitly, session-ingestion endpoint, fleet/device admin panel with
  human-gated OTA approval, PDF manual generation plan (reuses the
  existing scripts/generate_badge_pdf* pipeline shape, deferred until a
  physical pilot unit exists).
docs/LOT-SR-20260801-01.md                                ADDED (this file)

--------------------------------------------------------------------------------
06 // SELF-ASSEMBLY
--------------------------------------------------------------------------------
LEDGER:      appended (Section 07)
MANIFEST:    "COSMO Hardware" feature-line row updated — new branch
             continuing the brave-lamport cluster, scope note added
             distinguishing LOT Computer from the earlier (lost) COSMO
             Cube v1.0 spec so the two are never conflated in a future
             session.
LEXICON:     0 minted this session — LOT Computer / CUBIQ™ / COSMO®
             naming distinction is documented in-line in all 3 new files
             rather than as a lexicon token, since it is a one-time
             disambiguation, not a recurring operational term.
DOCTRINE:    0 clauses touched — this session applies existing doctrine
             (Widget->Memory Compression Loop's formatLog rule, the
             agent-ledger human-gate model) to new surface rather than
             establishing new doctrine.

--------------------------------------------------------------------------------
07 // PUSH
--------------------------------------------------------------------------------
COMMIT:      docs: LOT Computer hardware plan + BOM + roadmap + firmware/software connector specs [VM]
PUSH:        origin/claude/brave-lamport-qksqde

--------------------------------------------------------------------------------
08 // POST-PUSH VERIFICATION
--------------------------------------------------------------------------------
RESULT:      GREEN
NOTES:       Pure planning/spec intake session, no runtime code changed.
             Three documents establish LOT Computer as a plan, BOM, and
             roadmap ready for a procurement-capable session to price
             PCBWay/CNC/component quotes and for an ESP-IDF-capable
             session to begin firmware v0. Manual generation and CNC
             enclosure work are explicitly gated on a physical pilot
             unit existing (hardware plan Section 06, v2) and were not
             fabricated ahead of that gate. Network policy in this
             session blocked all external brand and vendor research —
             disclosed rather than worked around; a follow-up session
             with commerce-network access should complete the
             procurement step named in the hardware plan, Section 04.

================================================================================
AUTHORIZED BY: S-2 // VADIK MARMELADOV
END LOT-SR-20260801-01
================================================================================
