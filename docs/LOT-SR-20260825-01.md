```
╔══════════════════════════════════════════════════════════════════════╗
║                  LOT SYSTEMS — TERMINAL SESSION REPORT               ║
╠══════════════════════════════════════════════════════════════════════╣
║  ID       : LOT-SR-20260825-01                                       ║
║  DATE     : 2026-08-25                                                ║
║  CLASS    : PLANNING / DOCUMENTATION (no code touched, no build run)  ║
║  SUBJECT  : COSMO® TILE (CT-1) — physical LOT hardware computer       ║
║  S-2      : VADIK MARMELADOV                                          ║
╚══════════════════════════════════════════════════════════════════════╝
```

## INTAKE

```
INPUT    : Scheduled task — "build a hardware computer connected to the LOT
           site," 19-item brief (PCBWay, pager-like notification, 2-part
           stainless steel body, flat silver square 40x40x5mm, camera, LOT
           API connector, PDF manuals, session compression, firmware docs,
           software-to-firmware docs, separate documents, charger, 100-unit
           run, weather sensor, off-the-shelf AI-grade sensors, "Copy"
           button -> Log tab signal, polished face + instrumented face,
           wireless charging), signed COSMO® CIA / Vadik / Inventor. Closing
           instruction: "Push a full .MD report after each session."
CLASS    : PLANNING — from plan, to components buying list with links, to a
           roadmap. Not an engineering build session; no source under src/
           was created or modified.
ROUTE    : docs/corporate/ (plan) + docs/hardware/ (BOM, firmware, software,
           manual, PDF) + docs/README.md (index) + docs/ (this report)
```

## ORIENT

```
REPO ROOT    : /home/user/LOT-Computer
BRANCH       : claude/brave-lamport-tkbyla (fresh branch off master, HEAD
               98971f2 at session start; working tree was clean)
PRIOR ART    : docs/benchmark/LOT-MANIFEST.md records a branch series named
               brave-lamport-t9z5u8 — "COSMO® Cube — complete hardware
               computer design v1.0," BEST, 7 files, +2610 lines — pruned
               from the remote before 2026-06-27 without reaching master.
               No COSMO hardware spec exists anywhere in the current tree.
               That prior work was lost, not shipped. This session's branch
               reuses the same cluster name (brave-lamport) under a new
               scheduled run; see LOT-COSMO-TILE-v1.md Section 00a for the
               naming decision this produced.
EGRESS CHECK : WebFetch attempted against brand.lot-systems.com,
               lot-systems.com/about, and institute.lot-systems.com/cqgs.html
               (all three named in the brief) — all three blocked by this
               session's network egress policy (EGRESS_BLOCKED, confirmed at
               request time). Plan built instead on the repo's own local
               snapshots of that material (LOT_ROBOTICS_COSMO.md,
               CQGS-WHITE-PAPER-SNAPSHOT.md, README.md), per the pattern
               already established for CQGS-WHITE-PAPER-SNAPSHOT.md. Flagged
               explicitly in the new document's reading log for re-diff by
               a future session with open egress.
```

## DISCOVERY — WHAT ALREADY EXISTED

```
docs/corporate/LOT-CUBIQ-QUANTUM-CUBE-v0.md   — prior hardware-spec format
                                                  precedent (reading log,
                                                  numbered sections, honest
                                                  gates); this session's
                                                  documents follow its
                                                  structure.
docs/corporate/LOT_ROBOTICS_COSMO.md          — COSMO® brand definition,
                                                  Phase 3 hardware pricing
                                                  ($2,500-$5,000/unit,
                                                  2028-2029).
docs/corporate/CQGS-WHITE-PAPER-SNAPSHOT.md   — local snapshot of the CQGS
                                                  white paper; "Quantum Cube
                                                  Hardware ... PLANNED" row.
docs/corporate/LOT_Autonomous_AI_Server.md    — already owns the name
                                                  "LOT Computer" for the
                                                  self-hosted inference rack
                                                  (NODE-0). New device named
                                                  distinctly (COSMO® TILE)
                                                  to avoid collision.
LOT_PRODUCT_BENCHMARK.md                       — confirms a "Log tab" exists
                                                  in the live app (a fix
                                                  reference), used as the
                                                  write-back target for the
                                                  device's COPY button.
```

## BUILD — DOCUMENTS PRODUCED

```
docs/corporate/LOT-COSMO-TILE-v1.md
  Master plan. Reading log (Section 00/00a resolves the brave-lamport
  naming gap against CUBIQ and against the lost COSMO Cube manifest
  entry), physical form incl. an explicit height-budget analysis (the
  40x40x5mm brief target does not close with a camera + screen at
  commodity component thickness — v0 ships at 9mm reference height,
  v1 roadmap item closes toward 5mm; stated as a gap, not rounded away),
  electronics stack, notification language + example payloads, the COPY
  button / Log-tab write-back mechanism, the LOT API connector + session
  compression logic, PCBWay-first manufacturing plan (100-unit pilot,
  4-stage structure), roadmap v0-v3 with pass/fail gates, brand section.

docs/hardware/COSMO-TILE-BOM.md
  Components buying list. Per-unit electronics table (10 lines, ESP32-S3
  / camera / display / BME280 / Qi PMIC / battery buffer / button / PCB /
  SMT / passives), per-unit mechanical table (stainless shell pair, CNC +
  polish + bead-blast, standoffs, lens/window), charging-puck accessory
  table, 100-unit rollup (BOM + NRE + labor), named suppliers (PCBWay,
  Mouser, DigiKey, Adafruit, McMaster-Carr) linked to their homepages
  only — no fabricated deep product-page URLs. Six open items flagged
  explicitly as [QUOTE NEEDED] before any PO is cut.

docs/hardware/COSMO-TILE-FIRMWARE.md
  Firmware spec, kept separate from the software/server doc per the
  brief's item 11. ESP-IDF + LVGL toolchain, full state machine diagram
  (BOOT -> PAIRING -> IDLE -> DISPLAY -> ACK-SEND), poll/render loop,
  button handling (single function only), signed A/B OTA, camera capture
  path (power-gated, no local storage), weather telemetry batching,
  4-row failure-mode table, planned firmware/ repo layout.

docs/hardware/COSMO-TILE-SOFTWARE.md
  Server-side connector spec, kept separate per item 10/11. New table
  schema (devices, device_notifications, device_telemetry), 5-route API
  surface under /api/device/:deviceId/..., device-scoped token auth
  (distinct from the operator's session cookie), the session-compression
  background job (item 8 of the brief — full Index of Systems state in,
  one <=64-char disposable line out, nothing about the compression logic
  ever reaches the device), Log-tab write-back mechanics, rate limits,
  planned src/server/ file layout mirroring the existing public-api.ts
  convention.

docs/hardware/COSMO-TILE-MANUAL.md + docs/hardware/pdf/COSMO-TILE-MANUAL.pdf
  Operator-facing manual (plain language, no engineering jargon) covering
  box contents, physical layout (ASCII diagram of both faces), setup,
  daily use, care, a 4-row troubleshooting table, and pointers to the
  four engineering documents above. PDF generated via reportlab
  (title page, styled headers, wrapped troubleshooting table, monospace
  diagram block, page footer) — satisfies the brief's "Result in PDF
  manuals" requirement directly, not as a future TODO.

docs/README.md
  Added a "Hardware — COSMO® TILE (CT-1)" category linking all five
  documents above, following the existing docs-index convention (new
  category added + linked, per that file's own update instructions).
```

## CHECK — WHAT WAS ACTUALLY VERIFIED

```
PDF validity   : `file docs/hardware/pdf/COSMO-TILE-MANUAL.pdf` reports
                 "PDF document, version 1.4, 4 page(s)." Header/trailer
                 structure inspected directly (%PDF-1.4 ... %%EOF, 4
                 distinct /Type /Page objects) — PASS.
Cross-links    : Every new document's footer links the other three;
                 docs/README.md links all five — spot-checked by hand,
                 not by an automated link checker (none exists in this
                 repo for docs/).
No code touched: `git status` / diff scope for this session is limited to
                 docs/ and docs/README.md. No file under src/, migrations/,
                 or firmware/ was created — those are specified, not
                 implemented, in this session (explicitly marked as
                 "implementation phase — not yet created" in both the
                 firmware and software documents).
Manifest       : docs/benchmark/LOT-MANIFEST.md was read but NOT edited —
                 its BEST/iteration/hash columns are benchmark-pipeline-
                 owned state this session did not produce, and hand-editing
                 it here would misrepresent a verification that did not
                 run. LOT-COSMO-TILE-v1.md Section 00a records, in prose,
                 that the old brave-lamport-t9z5u8 manifest row should be
                 marked SUPERSEDED the next time that file is regenerated
                 by the benchmark pipeline.
GATE           : This is a documentation-only session. There is no
                 TypeScript build, test suite, or benchmark run to report
                 GREEN/RED against — that gate applies to code sessions,
                 not this one, and is not claimed here.
```

## FILES PRODUCED

```
docs/corporate/LOT-COSMO-TILE-v1.md            — master plan + roadmap
docs/hardware/COSMO-TILE-BOM.md                — components buying list
docs/hardware/COSMO-TILE-FIRMWARE.md           — firmware specification
docs/hardware/COSMO-TILE-SOFTWARE.md           — software / API specification
docs/hardware/COSMO-TILE-MANUAL.md             — operator manual (source)
docs/hardware/pdf/COSMO-TILE-MANUAL.pdf        — operator manual (PDF, 4pp)
docs/README.md                                 — hardware section added
docs/LOT-SR-20260825-01.md                     — this report
```

## OPEN ITEMS FOR THE NEXT SESSION

```
1. Re-diff LOT-COSMO-TILE-v1.md's reading log against the three brief-named
   URLs once egress to brand.lot-systems.com / lot-systems.com/about /
   institute.lot-systems.com/cqgs.html is available from a session.
2. Get real PCBWay quotes for the three [QUOTE NEEDED] lines in
   COSMO-TILE-BOM.md before treating the ~$8,500-$11,600 pilot estimate as
   a budget rather than a planning number.
3. Camera-module z-height must be confirmed against physical samples before
   the v0 9mm reference height (LOT-COSMO-TILE-v1.md Section 02) is treated
   as locked.
4. Battery buffer (LiPo pouch inside a sealed metal shell) needs a
   UN38.3 / IEC 62133 compliance review before Stage 4 of the 100-unit
   pilot (COSMO-TILE-BOM.md Section 6).
5. When a future session runs the full benchmark pipeline, regenerate
   docs/benchmark/LOT-MANIFEST.md's COSMO Hardware row to point at this
   branch/document set and mark the old brave-lamport-t9z5u8 entry
   SUPERSEDED, rather than hand-editing it here.
```

## POST-PUSH VERIFICATION

```
Branch     : claude/brave-lamport-tkbyla
Status     : Documentation set complete — 6 new files, 1 file updated
             (docs/README.md). No code, migrations, or firmware source
             created this session; all three are specified for a
             subsequent implementation session.
```

---
AUTHORIZED BY: S-2 // VADIK MARMELADOV
